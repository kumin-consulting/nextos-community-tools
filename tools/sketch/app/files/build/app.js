// Generated by lib/apps/native/build.ts - do not edit. Rebuild from src/ with apps_build.
var __appExports = (function () {
  "use strict";
  var modules = Object.create(null);
  var cache = Object.create(null);
  var resolveMap = {"src/index.tsx":{"./app.css":"src/app.css","./canvas/Canvas":"src/canvas/Canvas.tsx","./ui/Chrome":"src/ui/Chrome.tsx","./ui/Toolbar":"src/ui/Toolbar.tsx","./ui/Properties":"src/ui/Properties.tsx","./ui/FileSwitcher":"src/ui/FileSwitcher.tsx","./ui/ExportDialog":"src/ui/ExportDialog.tsx","./ui/Shortcuts":"src/ui/Shortcuts.tsx","./ui/SlashPalette":"src/ui/SlashPalette.tsx","./ui/Minimap":"src/ui/Minimap.tsx","./ui/Hints":"src/ui/Hints.tsx","./state/store":"src/state/store.ts","./state/view":"src/state/view.ts","./lib/layout":"src/lib/layout.ts","./lib/document":"src/lib/document.ts","./lib/svg":"src/lib/svg.ts","./lib/types":"src/lib/types.ts","./lib/palette":"src/lib/palette.ts","./state/files":"src/state/files.ts"},"src/app.css":{},"src/canvas/Canvas.tsx":{"../lib/types":"src/lib/types.ts","../lib/geometry":"src/lib/geometry.ts","../lib/snap":"src/lib/snap.ts","../lib/binding":"src/lib/binding.ts","../lib/textLayout":"src/lib/textLayout.ts","../lib/align":"src/lib/align.ts","./render":"src/canvas/render.ts","../state/store":"src/state/store.ts"},"src/ui/Chrome.tsx":{"./icons":"src/ui/icons.tsx","../state/store":"src/state/store.ts","../state/view":"src/state/view.ts","../lib/history":"src/lib/history.ts"},"src/ui/Toolbar.tsx":{"./icons":"src/ui/icons.tsx","../state/store":"src/state/store.ts"},"src/ui/Properties.tsx":{"../lib/types":"src/lib/types.ts","../lib/palette":"src/lib/palette.ts","./icons":"src/ui/icons.tsx","../state/store":"src/state/store.ts"},"src/ui/FileSwitcher.tsx":{"./Modal":"src/ui/Modal.tsx","./icons":"src/ui/icons.tsx","../state/store":"src/state/store.ts","../state/files":"src/state/files.ts"},"src/ui/ExportDialog.tsx":{"./Modal":"src/ui/Modal.tsx","./icons":"src/ui/icons.tsx","../lib/types":"src/lib/types.ts","../lib/geometry":"src/lib/geometry.ts","../lib/svg":"src/lib/svg.ts","../lib/align":"src/lib/align.ts","../lib/document":"src/lib/document.ts","../canvas/png":"src/canvas/png.ts","../state/store":"src/state/store.ts","../state/view":"src/state/view.ts"},"src/ui/Shortcuts.tsx":{"./Modal":"src/ui/Modal.tsx","../state/store":"src/state/store.ts"},"src/ui/SlashPalette.tsx":{"./Modal":"src/ui/Modal.tsx","./icons":"src/ui/icons.tsx","../lib/library":"src/lib/library.ts","../state/store":"src/state/store.ts","../state/view":"src/state/view.ts"},"src/ui/Minimap.tsx":{"../lib/geometry":"src/lib/geometry.ts","../lib/types":"src/lib/types.ts","../state/store":"src/state/store.ts","../state/view":"src/state/view.ts"},"src/ui/Hints.tsx":{"./icons":"src/ui/icons.tsx","../state/store":"src/state/store.ts"},"src/state/store.ts":{"../lib/types":"src/lib/types.ts","../lib/document":"src/lib/document.ts","../lib/history":"src/lib/history.ts","../lib/palette":"src/lib/palette.ts","../lib/geometry":"src/lib/geometry.ts","../lib/binding":"src/lib/binding.ts","../lib/align":"src/lib/align.ts","../lib/library":"src/lib/library.ts","./files":"src/state/files.ts"},"src/state/view.ts":{"../lib/types":"src/lib/types.ts"},"src/lib/layout.ts":{"./types":"src/lib/types.ts","./document":"src/lib/document.ts","./binding":"src/lib/binding.ts","./text":"src/lib/text.ts"},"src/lib/document.ts":{"./types":"src/lib/types.ts","./geometry":"src/lib/geometry.ts"},"src/lib/svg.ts":{"./types":"src/lib/types.ts","./document":"src/lib/document.ts","./geometry":"src/lib/geometry.ts","./shapes":"src/lib/shapes.ts","./textLayout":"src/lib/textLayout.ts","./text":"src/lib/text.ts"},"src/lib/types.ts":{},"src/lib/palette.ts":{},"src/state/files.ts":{"../lib/document":"src/lib/document.ts"},"src/lib/geometry.ts":{"./types":"src/lib/types.ts"},"src/lib/snap.ts":{"./types":"src/lib/types.ts"},"src/lib/binding.ts":{"./types":"src/lib/types.ts","./geometry":"src/lib/geometry.ts"},"src/lib/textLayout.ts":{"./types":"src/lib/types.ts","./text":"src/lib/text.ts","./binding":"src/lib/binding.ts"},"src/lib/align.ts":{"./types":"src/lib/types.ts","./geometry":"src/lib/geometry.ts"},"src/canvas/render.ts":{"../lib/types":"src/lib/types.ts","../lib/palette":"src/lib/palette.ts","../lib/document":"src/lib/document.ts","../lib/geometry":"src/lib/geometry.ts","../lib/shapes":"src/lib/shapes.ts","../lib/textLayout":"src/lib/textLayout.ts","../lib/snap":"src/lib/snap.ts"},"src/ui/icons.tsx":{},"src/lib/history.ts":{"./types":"src/lib/types.ts"},"src/ui/Modal.tsx":{"./icons":"src/ui/icons.tsx"},"src/canvas/png.ts":{"../lib/types":"src/lib/types.ts","../lib/palette":"src/lib/palette.ts","./render":"src/canvas/render.ts"},"src/lib/library.ts":{"./types":"src/lib/types.ts","./document":"src/lib/document.ts","./layout":"src/lib/layout.ts"},"src/lib/text.ts":{"./types":"src/lib/types.ts"},"src/lib/shapes.ts":{"./types":"src/lib/types.ts","./geometry":"src/lib/geometry.ts"}};
  var bareMap = {
    "react": function () { return window.__kuminSdk && window.__kuminSdk.react; },
    "react-dom": function () { return window.__kuminSdk && window.__kuminSdk.reactDom; },
    "react/jsx-runtime": function () { return window.__kuminSdk && window.__kuminSdk.reactJsxRuntime; },
    "zustand": function () { return window.__kuminSdk && window.__kuminSdk.zustand; },
    "@kumin/sdk": function () { return window.__kuminSdk && window.__kuminSdk.sdk("sketch"); }
  };
  function req(fromPath, spec) {
    if (Object.prototype.hasOwnProperty.call(bareMap, spec)) return bareMap[spec]();
    var to = resolveMap[fromPath] && resolveMap[fromPath][spec];
    if (!to) throw new Error('Unresolved import "' + spec + '" from "' + fromPath + '"');
    return runModule(to);
  }
  function runModule(path) {
    if (cache[path]) return cache[path].exports;
    var m = { exports: {} };
    cache[path] = m;
    modules[path](m, m.exports, function (spec) { return req(path, spec); });
    return m.exports;
  }
  modules["src/index.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.intents = exports.tools = void 0;
exports.onInstall = onInstall;
const jsx_runtime_1 = require("react/jsx-runtime");
// Sketch - an infinite canvas for diagrams and thinking, as a native
// NextOS app.
//
// This file is the shell: it mounts the canvas and the chrome, owns the
// keyboard map (every action in the app is reachable from here without a
// mouse), keeps the store's idea of the theme in step with the OS's, and
// exports the app's real API - `tools` for agents and `intents` for other
// apps. Everything else lives in src/lib (pure, tested), src/canvas
// (drawing and pointers), src/state (the store and the VFS) and src/ui.
const react_1 = __importStar(require("react"));
const sdk_1 = __importDefault(require("@kumin/sdk"));
require("./app.css");
const Canvas_1 = require("./canvas/Canvas");
const Canvas_2 = require("./canvas/Canvas");
const Chrome_1 = require("./ui/Chrome");
const Toolbar_1 = require("./ui/Toolbar");
const Properties_1 = require("./ui/Properties");
const FileSwitcher_1 = require("./ui/FileSwitcher");
const ExportDialog_1 = require("./ui/ExportDialog");
const Shortcuts_1 = require("./ui/Shortcuts");
const SlashPalette_1 = require("./ui/SlashPalette");
const Minimap_1 = require("./ui/Minimap");
const Hints_1 = require("./ui/Hints");
const store_1 = require("./state/store");
const view_1 = require("./state/view");
const layout_1 = require("./lib/layout");
const document_1 = require("./lib/document");
const svg_1 = require("./lib/svg");
const palette_1 = require("./lib/palette");
const files_1 = require("./state/files");
const TOOL_KEYS = {
    v: 'select',
    h: 'hand',
    r: 'rect',
    d: 'diamond',
    o: 'ellipse',
    a: 'arrow',
    l: 'line',
    p: 'draw',
    t: 'text',
    n: 'sticky',
    f: 'frame',
    e: 'eraser',
    '1': 'select',
    '2': 'hand',
    '3': 'rect',
    '4': 'diamond',
    '5': 'ellipse',
    '6': 'arrow',
    '7': 'line',
    '8': 'draw',
    '9': 'text',
    '0': 'sticky',
};
const SketchApp = () => {
    const isDark = sdk_1.default.theme.useIsDark();
    const ready = (0, store_1.useSketch)((s) => s.ready);
    const dialogs = (0, store_1.useSketch)((s) => s.dialogs);
    const corrupt = (0, store_1.useSketch)((s) => s.corrupt);
    const setDark = (0, store_1.useSketch)((s) => s.setDark);
    (0, react_1.useEffect)(() => {
        setDark(isDark);
    }, [isDark, setDark]);
    (0, react_1.useEffect)(() => {
        void store_1.useSketch.getState().boot();
        const flush = () => {
            void store_1.useSketch.getState().flush();
        };
        window.addEventListener('blur', flush);
        document.addEventListener('visibilitychange', flush);
        return () => {
            window.removeEventListener('blur', flush);
            document.removeEventListener('visibilitychange', flush);
            flush();
        };
    }, []);
    const onKeyDown = (0, react_1.useCallback)((e) => {
        const state = store_1.useSketch.getState();
        if ((0, Canvas_2.isTypingTarget)(e.target))
            return;
        const mod = e.metaKey || e.ctrlKey;
        const key = e.key;
        const lower = key.length === 1 ? key.toLowerCase() : key;
        if (key === 'Escape') {
            const open = Object.keys(state.dialogs).find((k) => k !== 'minimap' && state.dialogs[k]);
            if (open)
                state.setDialog(open, false);
            else if (state.selectedIds.length)
                state.setSelection([]);
            else
                state.setTool('select');
            return;
        }
        if (key === '?' || (key === '/' && e.shiftKey)) {
            e.preventDefault();
            state.setDialog('shortcuts', !state.dialogs.shortcuts);
            return;
        }
        if (key === '/' && !mod) {
            e.preventDefault();
            state.setDialog('slash', true);
            return;
        }
        if (mod) {
            switch (lower) {
                case 'z':
                    e.preventDefault();
                    if (e.shiftKey)
                        state.redo();
                    else
                        state.undo();
                    return;
                case 'y':
                    e.preventDefault();
                    state.redo();
                    return;
                case 'a':
                    e.preventDefault();
                    state.selectAll();
                    return;
                case 'c':
                    e.preventDefault();
                    state.copySelection(false);
                    return;
                case 'x':
                    e.preventDefault();
                    state.copySelection(true);
                    return;
                case 'v':
                    e.preventDefault();
                    state.paste((0, view_1.sceneCenter)());
                    return;
                case 'd':
                    e.preventDefault();
                    state.duplicateSelected();
                    return;
                case 'g':
                    e.preventDefault();
                    if (e.shiftKey)
                        state.ungroup();
                    else
                        state.group();
                    return;
                case 'l':
                    if (e.shiftKey) {
                        e.preventDefault();
                        state.toggleLock();
                    }
                    return;
                case 'e':
                    e.preventDefault();
                    state.setDialog('export', true);
                    return;
                case 'o':
                    e.preventDefault();
                    state.setDialog('files', true);
                    return;
                case 'n':
                    e.preventDefault();
                    void state.newDocument('Untitled');
                    return;
                case 's':
                    e.preventDefault();
                    void state.flush();
                    state.setStatus('Saved');
                    return;
                case ']':
                    e.preventDefault();
                    state.order(e.shiftKey ? 'front' : 'forward');
                    return;
                case '[':
                    e.preventDefault();
                    state.order(e.shiftKey ? 'back' : 'backward');
                    return;
                case '=':
                case '+':
                    e.preventDefault();
                    state.zoomTo(state.viewport.zoom * 1.2, [(0, view_1.canvasSize)().width / 2, (0, view_1.canvasSize)().height / 2]);
                    return;
                case '-':
                    e.preventDefault();
                    state.zoomTo(state.viewport.zoom / 1.2, [(0, view_1.canvasSize)().width / 2, (0, view_1.canvasSize)().height / 2]);
                    return;
                case '0':
                    e.preventDefault();
                    state.zoomTo(1, [(0, view_1.canvasSize)().width / 2, (0, view_1.canvasSize)().height / 2]);
                    return;
                default:
                    return;
            }
        }
        if (e.shiftKey) {
            if (key === '!' || key === '1') {
                e.preventDefault();
                state.zoomToFit((0, view_1.canvasSize)(), false);
                return;
            }
            if (key === '@' || key === '2') {
                e.preventDefault();
                state.zoomToFit((0, view_1.canvasSize)(), true);
                return;
            }
            if (lower === 'h') {
                e.preventDefault();
                state.flip('horizontal');
                return;
            }
            if (lower === 'v') {
                e.preventDefault();
                state.flip('vertical');
                return;
            }
            if (lower === 's') {
                e.preventDefault();
                state.toggleSnap();
                return;
            }
        }
        if (key === 'Delete' || key === 'Backspace') {
            e.preventDefault();
            state.deleteSelected();
            return;
        }
        if (key === 'Enter' && state.selectedIds.length === 1) {
            e.preventDefault();
            state.requestEdit(state.selectedIds[0]);
            return;
        }
        if (key.startsWith('Arrow')) {
            e.preventDefault();
            const step = e.shiftKey ? 10 : 1;
            const dx = key === 'ArrowLeft' ? -step : key === 'ArrowRight' ? step : 0;
            const dy = key === 'ArrowUp' ? -step : key === 'ArrowDown' ? step : 0;
            state.nudge(dx, dy);
            return;
        }
        if (lower === 'g') {
            e.preventDefault();
            state.setBackground(state.background === 'grid' ? 'dots' : state.background === 'dots' ? 'plain' : 'grid');
            return;
        }
        if (lower === 'm') {
            e.preventDefault();
            state.setDialog('minimap', !state.dialogs.minimap);
            return;
        }
        const tool = TOOL_KEYS[lower];
        if (tool) {
            e.preventDefault();
            state.setTool(tool);
        }
    }, []);
    (0, react_1.useEffect)(() => {
        window.addEventListener('keydown', onKeyDown);
        return () => window.removeEventListener('keydown', onKeyDown);
    }, [onKeyDown]);
    return ((0, jsx_runtime_1.jsxs)("div", { className: `sk-root${isDark ? ' sk-dark' : ''}`, children: [(0, jsx_runtime_1.jsx)(Canvas_1.Canvas, { onReady: (handle) => (view_1.viewRef.current = handle) }), (0, jsx_runtime_1.jsx)(Hints_1.EmptyHint, {}), (0, jsx_runtime_1.jsxs)("div", { className: "sk-chrome sk-chrome-top", children: [(0, jsx_runtime_1.jsx)(Chrome_1.DocumentBar, {}), (0, jsx_runtime_1.jsx)(Toolbar_1.Toolbar, {}), (0, jsx_runtime_1.jsx)(Chrome_1.ActionBar, {})] }), (0, jsx_runtime_1.jsx)("div", { className: "sk-chrome sk-chrome-left", children: (0, jsx_runtime_1.jsx)(Properties_1.Properties, {}) }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-chrome sk-chrome-bottom", children: [(0, jsx_runtime_1.jsx)(Chrome_1.ZoomBar, {}), dialogs.minimap ? (0, jsx_runtime_1.jsx)(Minimap_1.Minimap, {}) : null] }), (0, jsx_runtime_1.jsx)(Chrome_1.StatusToast, {}), dialogs.files ? (0, jsx_runtime_1.jsx)(FileSwitcher_1.FileSwitcher, {}) : null, dialogs.export ? (0, jsx_runtime_1.jsx)(ExportDialog_1.ExportDialog, {}) : null, dialogs.shortcuts ? (0, jsx_runtime_1.jsx)(Shortcuts_1.Shortcuts, {}) : null, dialogs.slash ? (0, jsx_runtime_1.jsx)(SlashPalette_1.SlashPalette, {}) : null, corrupt ? (0, jsx_runtime_1.jsx)(Hints_1.CorruptNotice, {}) : null, !ready ? (0, jsx_runtime_1.jsx)("div", { className: "sk-booting", children: "Opening your boards\u2026" }) : null] }));
};
exports.default = SketchApp;
/* --------------------------------------------------------------- tools */
const PREFIX = `${sdk_1.default.app.id.replace(/-/g, '_')}_`;
/** Finds a board by name, preferring the one already open (so an agent
 *  writing to the board a person is looking at changes what they see
 *  rather than a stale copy on disk). */
async function resolveDocument(name) {
    const state = store_1.useSketch.getState();
    const wanted = name ? (0, document_1.sanitizeName)(name) : null;
    if (!wanted || wanted.toLowerCase() === state.name.toLowerCase()) {
        if (state.path) {
            return {
                doc: {
                    type: 'sketch',
                    version: 1,
                    name: state.name,
                    createdAt: state.createdAt,
                    updatedAt: new Date().toISOString(),
                    background: state.background,
                    view: state.viewport,
                    elements: state.elements,
                },
                path: state.path,
                live: true,
            };
        }
    }
    const files = await (0, files_1.listDocuments)();
    const match = files.find((f) => f.name.toLowerCase() === (wanted ?? '').toLowerCase());
    if (!match)
        throw new Error(`No board called "${name}". Use ${PREFIX}list_documents to see what exists.`);
    const parsed = await (0, files_1.readDocumentFile)(match.path);
    if (!parsed.ok)
        throw new Error(`"${match.name}" could not be read: ${parsed.error}`);
    return { doc: parsed.doc, path: match.path, live: false };
}
async function persist(target, elements) {
    if (target.live) {
        store_1.useSketch.getState().commit(elements, { label: 'Agent edit' });
        await store_1.useSketch.getState().flush();
        return;
    }
    await (0, files_1.writeDocumentFile)(target.path, { ...target.doc, elements });
}
exports.tools = [
    {
        name: `${PREFIX}list_documents`,
        description: 'List every Sketch board in the user\'s Sketches folder, newest first, with the name to pass to the other sketch tools, the file path and when it was last changed. Also says which board is open right now.',
        inputSchema: { type: 'object', properties: {} },
        readOnly: true,
        async execute() {
            await (0, files_1.ensureFolder)();
            const files = await (0, files_1.listDocuments)();
            const open = store_1.useSketch.getState().name;
            return {
                open,
                documents: files.map((f) => ({ name: f.name, path: f.path, updatedAt: new Date(f.mtime).toISOString(), bytes: f.size })),
            };
        },
    },
    {
        name: `${PREFIX}create_diagram`,
        description: 'Draw a node-and-edge diagram on a new Sketch board and open it. Give the nodes ids and labels (shape: process, decision, start, end, database or note) and the edges the ids they run between; Sketch lays them out in layers, draws the boxes and connects them with arrows that stay attached when anything is moved afterwards.',
        inputSchema: {
            type: 'object',
            properties: {
                name: { type: 'string', description: 'Name for the new board, e.g. "Checkout flow". A number is added if that name is taken.' },
                graph: {
                    type: 'object',
                    description: 'The diagram itself.',
                    properties: {
                        nodes: {
                            type: 'array',
                            description: 'The boxes.',
                            items: {
                                type: 'object',
                                properties: {
                                    id: { type: 'string', description: 'Unique within this graph; edges refer to it.' },
                                    label: { type: 'string', description: 'The words inside the box.' },
                                    shape: { type: 'string', description: 'process (default), decision, start, end, database or note.' },
                                },
                                required: ['id', 'label'],
                            },
                        },
                        edges: {
                            type: 'array',
                            description: 'The arrows.',
                            items: {
                                type: 'object',
                                properties: {
                                    from: { type: 'string' },
                                    to: { type: 'string' },
                                    label: { type: 'string', description: 'Optional words on the arrow, e.g. "yes".' },
                                },
                                required: ['from', 'to'],
                            },
                        },
                        direction: { type: 'string', description: 'TB (top to bottom, the default) or LR (left to right).' },
                    },
                    required: ['nodes'],
                },
            },
            required: ['name', 'graph'],
        },
        async execute(input) {
            const { name, graph } = (input ?? {});
            if (!graph || !Array.isArray(graph.nodes) || graph.nodes.length === 0)
                throw new Error('graph.nodes must list at least one node.');
            const state = store_1.useSketch.getState();
            const palette = (0, palette_1.paletteFor)(state.isDark);
            const elements = (0, layout_1.buildDiagram)(graph, {
                stroke: palette.defaultStroke,
                fill: palette.fills[5]?.value ?? '#d6e6fb',
                fillStyle: 'solid',
                textColor: palette.defaultText,
                stickyFill: palette.stickyFill,
                stickyText: palette.stickyText,
                fontSize: 20,
            });
            const unique = await (0, files_1.freeName)((0, document_1.sanitizeName)(name || 'Diagram'));
            const doc = (0, document_1.createDocument)(unique, elements);
            const path = (0, files_1.pathFor)(unique);
            await (0, files_1.writeDocumentFile)(path, doc);
            await state.openDocument(path);
            store_1.useSketch.getState().zoomToFit((0, view_1.canvasSize)(), false);
            return {
                name: unique,
                path,
                nodes: graph.nodes.length,
                edges: (graph.edges ?? []).length,
                elements: elements.length,
                opened: true,
            };
        },
    },
    {
        name: `${PREFIX}add_elements`,
        description: 'Append elements to a Sketch board. Each element is { type, x, y, w, h, text?, stroke?, fill?, points? }; type is rect, ellipse, diamond, line, arrow, draw, text, sticky or frame. Coordinates are board coordinates - read them back with sketch_describe first when adding next to something that already exists.',
        inputSchema: {
            type: 'object',
            properties: {
                name: { type: 'string', description: 'Which board. Defaults to the one that is open.' },
                elements: {
                    type: 'array',
                    description: 'The elements to add.',
                    items: {
                        type: 'object',
                        properties: {
                            type: { type: 'string', description: 'rect, ellipse, diamond, line, arrow, draw, text, sticky or frame.' },
                            x: { type: 'number' },
                            y: { type: 'number' },
                            w: { type: 'number' },
                            h: { type: 'number' },
                            text: { type: 'string' },
                            stroke: { type: 'string', description: 'A hex colour.' },
                            fill: { type: 'string', description: 'A hex colour, or "transparent".' },
                            points: { type: 'array', description: 'For line/arrow/draw: [[x,y], ...] relative to x,y.', items: { type: 'array', items: { type: 'number' } } },
                        },
                        required: ['type'],
                    },
                },
            },
            required: ['elements'],
        },
        async execute(input) {
            const { name, elements } = (input ?? {});
            if (!Array.isArray(elements) || !elements.length)
                throw new Error('elements must be a non-empty array.');
            const target = await resolveDocument(name);
            const added = [];
            for (const raw of elements) {
                const el = (0, document_1.coerceElement)(raw);
                if (el)
                    added.push(el);
            }
            if (!added.length)
                throw new Error('None of those elements could be read - each needs at least a type, and a line needs points.');
            await persist(target, [...target.doc.elements, ...added]);
            return { name: target.doc.name, added: added.length, total: target.doc.elements.length + added.length };
        },
    },
    {
        name: `${PREFIX}export_svg`,
        description: 'Return a Sketch board as SVG text - real shapes and real text, ready to paste into a document or a web page.',
        inputSchema: {
            type: 'object',
            properties: {
                name: { type: 'string', description: 'Which board. Defaults to the one that is open.' },
                background: { type: 'boolean', description: 'Include the paper colour behind the drawing. Default false.' },
            },
        },
        readOnly: true,
        async execute(input) {
            const { name, background } = (input ?? {});
            const target = await resolveDocument(name);
            const palette = (0, palette_1.paletteFor)(store_1.useSketch.getState().isDark);
            const svg = (0, svg_1.elementsToSvg)(target.doc.elements, {
                background: background ? palette.background : null,
                title: target.doc.name,
                frameStroke: palette.frameStroke,
            });
            return { name: target.doc.name, svg };
        },
    },
    {
        name: `${PREFIX}describe`,
        description: 'Describe what is on a Sketch board in plain words: every shape with its label and position, and every arrow with what it connects. Use this before adding to a board, or to answer questions about a diagram.',
        inputSchema: {
            type: 'object',
            properties: { name: { type: 'string', description: 'Which board. Defaults to the one that is open.' } },
        },
        readOnly: true,
        async execute(input) {
            const { name } = (input ?? {});
            const target = await resolveDocument(name);
            return { name: target.doc.name, path: target.path, elements: target.doc.elements.length, outline: (0, document_1.describeDocument)(target.doc) };
        },
    },
];
/* ------------------------------------------------------------- intents */
exports.intents = {
    /** kumin://sketch/open?file=<path or name> */
    async open(params) {
        const file = params.file ?? params.path ?? params.name;
        if (!file)
            throw new Error('open needs a "file" - a board name or a path in the Sketches folder.');
        const state = store_1.useSketch.getState();
        if (file.startsWith('/')) {
            await state.openDocument(file);
            return { opened: file };
        }
        const target = await resolveDocument(file);
        await store_1.useSketch.getState().openDocument(target.path);
        return { opened: target.path };
    },
};
/* ------------------------------------------------------------- install */
async function onInstall() {
    try {
        await (0, files_1.ensureFolder)();
    }
    catch {
        // The folder is created again on first save; an install must never fail
        // because the VFS was busy.
    }
}

  };
  modules["src/app.css"] = function (module, exports, require) {
module.exports = {};
  };
  modules["src/canvas/Canvas.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Canvas = void 0;
exports.isTypingTarget = isTypingTarget;
exports.fitToText = fitToText;
const jsx_runtime_1 = require("react/jsx-runtime");
// The board itself: one <canvas>, one pointer state machine, and a
// textarea that appears over an element while you type into it.
//
// The canvas never re-renders through React. It subscribes to the store
// once, marks itself dirty and repaints inside a requestAnimationFrame -
// so dragging 2,000 elements costs one redraw per frame and zero React
// work. Only the text overlay is React state, because it is the one
// thing that has to be a real DOM node (selection, IME, spellcheck).
const react_1 = __importStar(require("react"));
const types_1 = require("../lib/types");
const geometry_1 = require("../lib/geometry");
const snap_1 = require("../lib/snap");
const binding_1 = require("../lib/binding");
const textLayout_1 = require("../lib/textLayout");
const align_1 = require("../lib/align");
const types_2 = require("../lib/types");
const render_1 = require("./render");
const store_1 = require("../state/store");
const DEFAULT_SIZE = {
    rect: [180, 110],
    ellipse: [170, 120],
    diamond: [180, 120],
    sticky: [180, 180],
    frame: [520, 360],
    text: [0, 24],
};
const Canvas = ({ onReady }) => {
    const hostRef = (0, react_1.useRef)(null);
    const canvasRef = (0, react_1.useRef)(null);
    const ctxRef = (0, react_1.useRef)(null);
    const measureRef = (0, react_1.useRef)(null);
    const sizeRef = (0, react_1.useRef)({ width: 800, height: 600 });
    const dirtyRef = (0, react_1.useRef)(true);
    const gestureRef = (0, react_1.useRef)({ kind: 'none' });
    const spaceRef = (0, react_1.useRef)(false);
    const snapRef = (0, react_1.useRef)([]);
    const hoverBindRef = (0, react_1.useRef)(null);
    const marqueeRef = (0, react_1.useRef)(null);
    const [cursor, setCursor] = (0, react_1.useState)('default');
    const [editing, setEditing] = (0, react_1.useState)(null);
    const editRef = (0, react_1.useRef)(null);
    const toScene = (0, react_1.useCallback)((clientX, clientY) => {
        const rect = canvasRef.current?.getBoundingClientRect();
        const v = store_1.useSketch.getState().viewport;
        return (0, geometry_1.canvasToScene)([clientX - (rect?.left ?? 0), clientY - (rect?.top ?? 0)], v);
    }, []);
    /* ------------------------------------------------------- painting */
    (0, react_1.useEffect)(() => {
        const canvas = canvasRef.current;
        const host = hostRef.current;
        if (!canvas || !host)
            return;
        const ctx = canvas.getContext('2d');
        if (!ctx)
            return;
        ctxRef.current = ctx;
        measureRef.current = (0, render_1.measureWith)(ctx);
        const resize = () => {
            const rect = host.getBoundingClientRect();
            const dpr = Math.min(window.devicePixelRatio || 1, 2.5);
            const width = Math.max(1, Math.round(rect.width));
            const height = Math.max(1, Math.round(rect.height));
            sizeRef.current = { width, height };
            canvas.width = Math.round(width * dpr);
            canvas.height = Math.round(height * dpr);
            canvas.style.width = `${width}px`;
            canvas.style.height = `${height}px`;
            ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
            dirtyRef.current = true;
        };
        resize();
        const observer = new ResizeObserver(resize);
        observer.observe(host);
        let frame = 0;
        const loop = () => {
            frame = requestAnimationFrame(loop);
            if (!dirtyRef.current)
                return;
            dirtyRef.current = false;
            const s = store_1.useSketch.getState();
            const scene = {
                elements: s.elements,
                viewport: s.viewport,
                width: sizeRef.current.width,
                height: sizeRef.current.height,
                palette: s.palette,
                background: s.background,
                selectedIds: new Set(s.selectedIds),
                editingId: s.editingId,
                marquee: marqueeRef.current,
                snapLines: snapRef.current,
                bindHighlightId: hoverBindRef.current,
            };
            (0, render_1.renderScene)(ctx, scene);
        };
        frame = requestAnimationFrame(loop);
        const unsubscribe = store_1.useSketch.subscribe(() => {
            dirtyRef.current = true;
        });
        onReady?.({
            toScene,
            size: () => sizeRef.current,
            centerScene: () => {
                const v = store_1.useSketch.getState().viewport;
                return (0, geometry_1.canvasToScene)([sizeRef.current.width / 2, sizeRef.current.height / 2], v);
            },
            measure: measureRef.current,
        });
        return () => {
            cancelAnimationFrame(frame);
            observer.disconnect();
            unsubscribe();
        };
    }, [onReady, toScene]);
    /* ---------------------------------------------------------- wheel */
    (0, react_1.useEffect)(() => {
        const canvas = canvasRef.current;
        if (!canvas)
            return;
        const onWheel = (e) => {
            e.preventDefault();
            const state = store_1.useSketch.getState();
            const rect = canvas.getBoundingClientRect();
            const at = [e.clientX - rect.left, e.clientY - rect.top];
            if (e.ctrlKey || e.metaKey) {
                const factor = Math.exp(-e.deltaY * 0.01);
                state.setViewport((0, geometry_1.zoomAt)(state.viewport, state.viewport.zoom * factor, at));
                return;
            }
            const { zoom, scrollX, scrollY } = state.viewport;
            state.setViewport({ zoom, scrollX: scrollX + e.deltaX / zoom, scrollY: scrollY + e.deltaY / zoom });
        };
        canvas.addEventListener('wheel', onWheel, { passive: false });
        return () => canvas.removeEventListener('wheel', onWheel);
    }, []);
    /* ------------------------------------------------- space to pan */
    (0, react_1.useEffect)(() => {
        const down = (e) => {
            if (e.code === 'Space' && !isTypingTarget(e.target)) {
                spaceRef.current = true;
                setCursor('grab');
            }
        };
        const up = (e) => {
            if (e.code === 'Space') {
                spaceRef.current = false;
                setCursor('default');
            }
        };
        window.addEventListener('keydown', down);
        window.addEventListener('keyup', up);
        return () => {
            window.removeEventListener('keydown', down);
            window.removeEventListener('keyup', up);
        };
    }, []);
    /* --------------------------------------------------- text editing */
    const startEditing = (0, react_1.useCallback)((el, created = false) => {
        store_1.useSketch.getState().setEditing(el.id);
        setEditing({ id: el.id, value: el.type === 'frame' ? el.name : el.text, created });
    }, []);
    const finishEditing = (0, react_1.useCallback)(() => {
        const current = editing;
        if (!current)
            return;
        const state = store_1.useSketch.getState();
        const el = state.elements.find((e) => e.id === current.id);
        setEditing(null);
        state.setEditing(null);
        if (!el)
            return;
        const value = current.value;
        const isFrame = el.type === 'frame';
        if (current.created && !value.trim() && el.type === 'text') {
            // A text element nobody typed into never existed.
            state.commit(state.elements.filter((e) => e.id !== el.id), { label: 'Delete', selectedIds: [] });
            return;
        }
        const measure = measureRef.current;
        let next = isFrame ? { ...el, name: value } : { ...el, text: value };
        if (!isFrame && measure)
            next = fitToText(next, measure);
        state.commit(state.elements.map((e) => (e.id === el.id ? next : e)), { label: 'Text' });
    }, [editing]);
    (0, react_1.useLayoutEffect)(() => {
        if (editing && editRef.current) {
            const node = editRef.current;
            node.focus();
            node.setSelectionRange(node.value.length, node.value.length);
        }
    }, [editing?.id]);
    // Enter (and the agent's own "edit this" path) asks the canvas to open
    // the text editor on an element it does not otherwise know about.
    const editRequest = (0, store_1.useSketch)((s) => s.editRequest);
    (0, react_1.useEffect)(() => {
        if (!editRequest)
            return;
        const el = store_1.useSketch.getState().elements.find((item) => item.id === editRequest);
        if (el && !el.locked)
            startEditing(el);
        else
            store_1.useSketch.getState().requestEdit(null);
    }, [editRequest, startEditing]);
    /* -------------------------------------------------------- pointer */
    const onPointerDown = (0, react_1.useCallback)((e) => {
        const canvas = canvasRef.current;
        if (!canvas)
            return;
        if (editing)
            finishEditing();
        canvas.setPointerCapture(e.pointerId);
        const state = store_1.useSketch.getState();
        const scene = toScene(e.clientX, e.clientY);
        const panning = spaceRef.current || state.tool === 'hand' || e.button === 1 || e.button === 2;
        if (panning) {
            gestureRef.current = { kind: 'pan', start: [e.clientX, e.clientY], scroll: [state.viewport.scrollX, state.viewport.scrollY] };
            setCursor('grabbing');
            return;
        }
        if (e.button !== 0)
            return;
        if (state.tool === 'eraser') {
            gestureRef.current = { kind: 'erase', erased: new Set() };
            eraseAt(scene);
            return;
        }
        if (state.tool === 'select') {
            const handled = beginSelectGesture(scene, e.shiftKey, e.altKey);
            if (handled)
                return;
        }
        if (state.tool === 'text') {
            const el = (0, store_1.elementFromStyle)('text', { x: scene[0], y: scene[1], w: 0, h: state.style.fontSize * 1.25 }, state.style);
            state.addElements([el], { label: 'Text' });
            startEditing(el, true);
            gestureRef.current = { kind: 'none' };
            if (!state.keepTool)
                state.setTool('select');
            return;
        }
        if (state.tool === 'draw') {
            const el = (0, store_1.elementFromStyle)('draw', {
                x: scene[0],
                y: scene[1],
                points: [
                    [0, 0],
                    [0, 0],
                ],
                fill: 'transparent',
                fillStyle: 'none',
                startArrow: 'none',
                endArrow: 'none',
            }, state.style);
            state.addElements([el], { label: 'Draw' });
            gestureRef.current = { kind: 'draw', id: el.id, points: [[0, 0]], origin: scene };
            return;
        }
        if (state.tool === 'line' || state.tool === 'arrow') {
            const start = (0, binding_1.bindableAt)(state.elements, scene);
            const el = (0, store_1.elementFromStyle)(state.tool, {
                x: scene[0],
                y: scene[1],
                points: [
                    [0, 0],
                    [0, 0],
                ],
                startBinding: start ? (0, binding_1.makeBinding)(start, scene) : null,
            }, state.style);
            state.addElements([el], { label: state.tool === 'arrow' ? 'Arrow' : 'Line' });
            gestureRef.current = { kind: 'linear', id: el.id, origin: scene };
            return;
        }
        const type = state.tool;
        if (DEFAULT_SIZE[type]) {
            const el = (0, store_1.elementFromStyle)(type, {
                x: scene[0],
                y: scene[1],
                w: 0,
                h: 0,
                ...(type === 'sticky' ? { fill: state.palette.stickyFill, textColor: state.palette.stickyText } : {}),
                ...(type === 'frame' ? { name: `Frame ${state.elements.filter((el2) => el2.type === 'frame').length + 1}` } : {}),
            }, state.style);
            state.addElements([el], { label: 'Draw' });
            gestureRef.current = { kind: 'create', id: el.id, origin: scene, type };
        }
    }, [editing, finishEditing, startEditing, toScene]);
    const beginSelectGesture = (0, react_1.useCallback)((scene, shift, alt) => {
        const state = store_1.useSketch.getState();
        const tolerance = 8 / state.viewport.zoom;
        const selected = state.elements.filter((el) => state.selectedIds.includes(el.id));
        const unlocked = selected.filter((el) => !el.locked);
        if (unlocked.length) {
            const box = unlocked.length === 1 ? { x: unlocked[0].x, y: unlocked[0].y, w: unlocked[0].w, h: unlocked[0].h } : (0, geometry_1.boundsOfElements)(unlocked);
            const angle = unlocked.length === 1 ? unlocked[0].angle : 0;
            const grab = 10 / state.viewport.zoom;
            if (unlocked.length === 1 && (0, types_1.isLinear)(unlocked[0])) {
                const pts = (0, geometry_1.linearPathPoints)(unlocked[0]);
                for (let i = 0; i < unlocked[0].points.length; i++) {
                    const p = pts[Math.min(i, pts.length - 1)];
                    if (Math.hypot(p[0] - scene[0], p[1] - scene[1]) <= grab) {
                        gestureRef.current = { kind: 'vertex', id: unlocked[0].id, index: i };
                        return true;
                    }
                }
            }
            else {
                const rotateAt = (0, geometry_1.rotateHandlePosition)(box, angle, 28 / state.viewport.zoom);
                if (Math.hypot(rotateAt[0] - scene[0], rotateAt[1] - scene[1]) <= grab) {
                    gestureRef.current = {
                        kind: 'rotate',
                        ids: unlocked.map((el) => el.id),
                        before: unlocked,
                        center: [box.x + box.w / 2, box.y + box.h / 2],
                    };
                    return true;
                }
                for (const { handle, point } of (0, geometry_1.handlePositions)(box, angle)) {
                    if (Math.hypot(point[0] - scene[0], point[1] - scene[1]) <= grab) {
                        gestureRef.current = { kind: 'resize', handle, ids: unlocked.map((el) => el.id), before: unlocked, box };
                        return true;
                    }
                }
            }
        }
        const hit = (0, geometry_1.hitTestElements)(state.elements, scene, tolerance);
        if (!hit) {
            marqueeRef.current = { x: scene[0], y: scene[1], w: 0, h: 0 };
            gestureRef.current = { kind: 'marquee', origin: scene, additive: shift, base: shift ? state.selectedIds : [] };
            if (!shift)
                state.setSelection([]);
            return true;
        }
        let ids = state.selectedIds;
        if (shift) {
            ids = state.selectedIds.includes(hit.id) ? state.selectedIds.filter((id) => id !== hit.id) : [...state.selectedIds, hit.id];
            state.setSelection(ids);
        }
        else if (!state.selectedIds.includes(hit.id)) {
            ids = [hit.id];
            state.setSelection(ids);
        }
        const current = store_1.useSketch.getState();
        let moving = current.elements.filter((el) => current.selectedIds.includes(el.id) && !el.locked);
        if (alt && moving.length) {
            current.duplicateSelected();
            const after = store_1.useSketch.getState();
            moving = after.elements.filter((el) => after.selectedIds.includes(el.id));
        }
        if (!moving.length)
            return true;
        // A frame carries whatever sits inside it, decided once here rather
        // than recomputed every frame (otherwise a shape dragged past the
        // frame's edge would join the drag half way through it).
        const movingIds = new Set(moving.map((el) => el.id));
        for (const el of moving) {
            if (el.type !== 'frame')
                continue;
            for (const child of (0, align_1.elementsInFrame)(current.elements, el))
                if (!child.locked)
                    movingIds.add(child.id);
        }
        const before = current.elements.filter((el) => movingIds.has(el.id));
        gestureRef.current = { kind: 'move', origin: scene, ids: Array.from(movingIds), before, moved: false };
        return true;
    }, []);
    const eraseAt = (0, react_1.useCallback)((scene) => {
        const state = store_1.useSketch.getState();
        const gesture = gestureRef.current;
        if (gesture.kind !== 'erase')
            return;
        const tolerance = 10 / state.viewport.zoom;
        const hit = (0, geometry_1.hitTestElements)(state.elements, scene, tolerance);
        if (!hit || gesture.erased.has(hit.id))
            return;
        gesture.erased.add(hit.id);
        state.commit(state.elements.filter((el) => el.id !== hit.id), { label: 'Erase', coalesce: 'erase' });
    }, []);
    const onPointerMove = (0, react_1.useCallback)((e) => {
        const gesture = gestureRef.current;
        const state = store_1.useSketch.getState();
        const scene = toScene(e.clientX, e.clientY);
        if (gesture.kind === 'none') {
            if (state.tool === 'select' && !spaceRef.current) {
                const hit = (0, geometry_1.hitTestElements)(state.elements, scene, 8 / state.viewport.zoom);
                setCursor(hit ? 'move' : 'default');
            }
            return;
        }
        switch (gesture.kind) {
            case 'pan': {
                const zoom = state.viewport.zoom;
                state.setViewport({
                    zoom,
                    scrollX: gesture.scroll[0] - (e.clientX - gesture.start[0]) / zoom,
                    scrollY: gesture.scroll[1] - (e.clientY - gesture.start[1]) / zoom,
                });
                break;
            }
            case 'marquee': {
                const box = { x: gesture.origin[0], y: gesture.origin[1], w: scene[0] - gesture.origin[0], h: scene[1] - gesture.origin[1] };
                marqueeRef.current = box;
                const inside = (0, geometry_1.elementsInBounds)(state.elements, box).map((el) => el.id);
                state.setSelection(gesture.additive ? Array.from(new Set([...gesture.base, ...inside])) : inside);
                break;
            }
            case 'move': {
                let dx = scene[0] - gesture.origin[0];
                let dy = scene[1] - gesture.origin[1];
                if (e.shiftKey) {
                    if (Math.abs(dx) > Math.abs(dy))
                        dy = 0;
                    else
                        dx = 0;
                }
                const ids = new Set(gesture.ids);
                const box = (0, geometry_1.boundsOfElements)(gesture.before);
                let snapLines = [];
                if (state.snapEnabled) {
                    const others = state.elements.filter((el) => !ids.has(el.id) && el.type !== 'frame').map(geometry_1.rotatedBounds);
                    const snap = (0, snap_1.snapDrag)({ ...box, x: box.x + dx, y: box.y + dy }, others, {
                        grid: render_1.GRID_SIZE / 2,
                        threshold: 6 / state.viewport.zoom,
                    });
                    dx += snap.dx;
                    dy += snap.dy;
                    snapLines = snap.lines;
                }
                snapRef.current = snapLines;
                const beforeById = new Map(gesture.before.map((el) => [el.id, el]));
                state.commit(state.elements.map((el) => {
                    const base = beforeById.get(el.id);
                    return base ? { ...base, x: base.x + dx, y: base.y + dy } : el;
                }), { label: 'Move', coalesce: 'move' });
                gestureRef.current = { ...gesture, moved: true };
                break;
            }
            case 'resize': {
                const beforeById = new Map(gesture.before.map((el) => [el.id, el]));
                const measure = measureRef.current;
                if (gesture.before.length === 1) {
                    const el = gesture.before[0];
                    let next = (0, geometry_1.resizeElement)(el, gesture.handle, scene, e.shiftKey);
                    if (el.type === 'text' && measure)
                        next = scaleText(el, next, measure);
                    else if ((0, types_1.isContainer)(next) && measure)
                        next = { ...next, h: Math.max(next.h, (0, textLayout_1.requiredHeight)(next, measure)) };
                    state.commit(state.elements.map((item) => (item.id === el.id ? next : item)), { label: 'Resize', coalesce: 'resize' });
                }
                else {
                    const to = resizeBox(gesture.box, gesture.handle, scene, e.shiftKey);
                    state.commit(state.elements.map((item) => {
                        const base = beforeById.get(item.id);
                        return base ? (0, geometry_1.transformElement)(base, gesture.box, to) : item;
                    }), { label: 'Resize', coalesce: 'resize' });
                }
                break;
            }
            case 'rotate': {
                if (gesture.before.length === 1) {
                    const next = (0, geometry_1.rotateElementTo)(gesture.before[0], scene, e.shiftKey);
                    state.commit(state.elements.map((item) => (item.id === next.id ? next : item)), { label: 'Rotate', coalesce: 'rotate' });
                }
                else {
                    const [cx, cy] = gesture.center;
                    const start = gesture.before[0];
                    const angle = Math.atan2(scene[1] - cy, scene[0] - cx) + Math.PI / 2;
                    const delta = e.shiftKey ? Math.round(angle / (Math.PI / 12)) * (Math.PI / 12) - start.angle : angle - start.angle;
                    const beforeById = new Map(gesture.before.map((el) => [el.id, el]));
                    state.commit(state.elements.map((item) => {
                        const base = beforeById.get(item.id);
                        return base ? (0, geometry_1.rotateAbout)(base, cx, cy, delta) : item;
                    }), { label: 'Rotate', coalesce: 'rotate' });
                }
                break;
            }
            case 'create': {
                const box = (0, geometry_1.normalizeBounds)({
                    x: gesture.origin[0],
                    y: gesture.origin[1],
                    w: scene[0] - gesture.origin[0],
                    h: scene[1] - gesture.origin[1],
                });
                let { w, h } = box;
                if (e.shiftKey) {
                    const side = Math.max(w, h);
                    w = side;
                    h = side;
                }
                const x = scene[0] < gesture.origin[0] ? gesture.origin[0] - w : box.x;
                const y = scene[1] < gesture.origin[1] ? gesture.origin[1] - h : box.y;
                const step = state.snapEnabled ? render_1.GRID_SIZE / 2 : 0;
                const grid = (n) => (step ? Math.round(n / step) * step : n);
                state.commit(state.elements.map((el) => el.id === gesture.id ? { ...el, x: grid(x), y: grid(y), w: Math.max(grid(w), 1), h: Math.max(grid(h), 1) } : el), { label: 'Draw', coalesce: 'create' });
                break;
            }
            case 'draw': {
                const local = [scene[0] - gesture.origin[0], scene[1] - gesture.origin[1]];
                const points = [...gesture.points, local];
                gestureRef.current = { ...gesture, points };
                state.commit(state.elements.map((el) => (el.id === gesture.id ? (0, geometry_1.normalizeLinear)({ ...el, x: gesture.origin[0], y: gesture.origin[1], points }) : el)), { label: 'Draw', coalesce: 'draw' });
                break;
            }
            case 'linear': {
                let end = [scene[0] - gesture.origin[0], scene[1] - gesture.origin[1]];
                if (e.shiftKey) {
                    const angle = Math.atan2(end[1], end[0]);
                    const step = Math.PI / 4;
                    const snappedAngle = Math.round(angle / step) * step;
                    const length = Math.hypot(end[0], end[1]);
                    end = [Math.cos(snappedAngle) * length, Math.sin(snappedAngle) * length];
                }
                const target = (0, binding_1.bindableAt)(state.elements, scene, 18 / state.viewport.zoom, gesture.id);
                hoverBindRef.current = target?.id ?? null;
                state.commit(state.elements.map((el) => el.id === gesture.id ? (0, geometry_1.normalizeLinear)({ ...el, x: gesture.origin[0], y: gesture.origin[1], points: [[0, 0], end] }) : el), { label: 'Draw', coalesce: 'linear' });
                break;
            }
            case 'vertex': {
                const el = state.elements.find((item) => item.id === gesture.id);
                if (!el)
                    break;
                const points = el.points.map((p, i) => (i === gesture.index ? [scene[0] - el.x, scene[1] - el.y] : p));
                const target = (0, binding_1.bindableAt)(state.elements, scene, 18 / state.viewport.zoom, el.id);
                hoverBindRef.current = target?.id ?? null;
                const bindingKey = gesture.index === 0 ? 'startBinding' : gesture.index === el.points.length - 1 ? 'endBinding' : null;
                const next = (0, geometry_1.normalizeLinear)({
                    ...el,
                    points,
                    ...(bindingKey ? { [bindingKey]: target ? (0, binding_1.makeBinding)(target, scene) : null } : {}),
                });
                state.commit(state.elements.map((item) => (item.id === el.id ? next : item)), { label: 'Edit line', coalesce: 'vertex' });
                break;
            }
            case 'erase': {
                eraseAt(scene);
                break;
            }
            default:
                break;
        }
    }, [eraseAt, toScene]);
    const onPointerUp = (0, react_1.useCallback)((e) => {
        const gesture = gestureRef.current;
        const state = store_1.useSketch.getState();
        const scene = toScene(e.clientX, e.clientY);
        gestureRef.current = { kind: 'none' };
        marqueeRef.current = null;
        snapRef.current = [];
        hoverBindRef.current = null;
        canvasRef.current?.releasePointerCapture?.(e.pointerId);
        setCursor(spaceRef.current ? 'grab' : 'default');
        if (gesture.kind === 'create') {
            const el = state.elements.find((item) => item.id === gesture.id);
            if (el && (el.w < 6 || el.h < 6)) {
                const [w, h] = DEFAULT_SIZE[gesture.type] ?? [160, 100];
                state.commit(state.elements.map((item) => (item.id === el.id ? { ...item, x: gesture.origin[0] - w / 2, y: gesture.origin[1] - h / 2, w, h } : item)), { label: 'Draw', coalesce: 'create' });
            }
            const created = store_1.useSketch.getState().elements.find((item) => item.id === gesture.id);
            if (created && created.type !== 'frame' && (0, types_1.isContainer)(created)) {
                // A shape drawn with the sticky tool is ready for its note.
                if (created.type === 'sticky')
                    startEditing(created, true);
            }
        }
        if (gesture.kind === 'linear') {
            const el = state.elements.find((item) => item.id === gesture.id);
            if (el) {
                const pts = (0, geometry_1.linearPathPoints)(el);
                const tip = pts[pts.length - 1];
                const length = Math.hypot(tip[0] - pts[0][0], tip[1] - pts[0][1]);
                if (length < 6) {
                    state.commit(state.elements.filter((item) => item.id !== el.id), { label: 'Draw', selectedIds: [] });
                }
                else {
                    const target = (0, binding_1.bindableAt)(state.elements, scene, 18 / state.viewport.zoom, el.id);
                    if (target) {
                        state.commit(state.elements.map((item) => (item.id === el.id ? { ...item, endBinding: (0, binding_1.makeBinding)(target, tip) } : item)), { label: 'Bind' });
                    }
                }
            }
        }
        if (gesture.kind === 'draw') {
            const el = store_1.useSketch.getState().elements.find((item) => item.id === gesture.id);
            if (el) {
                const simplified = (0, geometry_1.simplifyPoints)(el.points, 0.7 / state.viewport.zoom);
                if (simplified.length < 2) {
                    state.commit(state.elements.filter((item) => item.id !== el.id), { label: 'Draw', selectedIds: [] });
                }
                else {
                    state.commit(state.elements.map((item) => (item.id === el.id ? (0, geometry_1.normalizeLinear)({ ...item, points: simplified }) : item)), { label: 'Draw' });
                }
            }
        }
        const finished = store_1.useSketch.getState();
        finished.seal();
        if (!finished.keepTool && finished.tool !== 'select' && finished.tool !== 'hand' && finished.tool !== 'eraser') {
            finished.setTool('select');
        }
    }, [startEditing, toScene]);
    const onDoubleClick = (0, react_1.useCallback)((e) => {
        const state = store_1.useSketch.getState();
        const scene = toScene(e.clientX, e.clientY);
        const hit = (0, geometry_1.hitTestElements)(state.elements, scene, 8 / state.viewport.zoom);
        if (hit && !hit.locked) {
            startEditing(hit);
            state.setSelection([hit.id]);
            return;
        }
        const el = (0, store_1.elementFromStyle)('text', { x: scene[0], y: scene[1], w: 0, h: state.style.fontSize * 1.25 }, state.style);
        state.addElements([el], { label: 'Text' });
        startEditing(el, true);
    }, [startEditing, toScene]);
    /* ------------------------------------------------ text overlay */
    const overlay = useOverlayStyle(editing?.id ?? null);
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-canvas-host", ref: hostRef, children: [(0, jsx_runtime_1.jsx)("canvas", { ref: canvasRef, className: "sk-canvas", style: { cursor }, onPointerDown: onPointerDown, onPointerMove: onPointerMove, onPointerUp: onPointerUp, onPointerCancel: onPointerUp, onDoubleClick: onDoubleClick, onContextMenu: (e) => e.preventDefault(), role: "application", "aria-label": "Drawing canvas" }), editing && overlay ? ((0, jsx_runtime_1.jsx)("textarea", { ref: editRef, className: "sk-text-editor", value: editing.value, "aria-label": "Element text", spellCheck: false, style: overlay, onChange: (e) => {
                    const value = e.target.value;
                    setEditing((cur) => (cur ? { ...cur, value } : cur));
                    const state = store_1.useSketch.getState();
                    const measure = measureRef.current;
                    state.commit(state.elements.map((el) => {
                        if (el.id !== editing.id)
                            return el;
                        const next = el.type === 'frame' ? { ...el, name: value } : { ...el, text: value };
                        return measure && el.type !== 'frame' ? fitToText(next, measure) : next;
                    }), { label: 'Text', coalesce: `text:${editing.id}` });
                }, onBlur: finishEditing, onKeyDown: (e) => {
                    if (e.key === 'Escape') {
                        e.preventDefault();
                        finishEditing();
                    }
                    if (e.key === 'Enter' && (e.metaKey || e.ctrlKey)) {
                        e.preventDefault();
                        finishEditing();
                    }
                    e.stopPropagation();
                } })) : null] }));
};
exports.Canvas = Canvas;
/* ----------------------------------------------------------- helpers */
function isTypingTarget(target) {
    const el = target;
    if (!el || !el.tagName)
        return false;
    return el.tagName === 'INPUT' || el.tagName === 'TEXTAREA' || el.isContentEditable === true;
}
/** Grows a shape (or a text element) to hold the text it now contains. */
function fitToText(el, measure) {
    if ((0, types_1.isLinear)(el) || el.type === 'frame')
        return el;
    const layout = (0, textLayout_1.elementTextLayout)(el, measure);
    if (!layout)
        return el;
    if (el.type === 'text') {
        let width = 0;
        for (const line of layout.lines)
            width = Math.max(width, measure(line, el.fontSize, el.fontFamily));
        return { ...el, w: Math.max(width, 8), h: layout.height };
    }
    const needed = (0, textLayout_1.requiredHeight)(el, measure);
    return needed > el.h ? { ...el, h: needed } : el;
}
/** Resizing a text element scales the type rather than re-wrapping it -
 *  what a person means when they drag the corner of a word. */
function scaleText(before, after, measure) {
    const scale = before.h === 0 ? 1 : Math.abs(after.h / before.h);
    const fontSize = (0, geometry_1.clamp)(Math.round(before.fontSize * scale), 6, 240);
    return fitToText({ ...after, fontSize, x: after.x, y: after.y }, measure);
}
function resizeBox(box, handle, pointer, keepAspect) {
    let { x, y, w, h } = box;
    let x1 = x + w;
    let y1 = y + h;
    if (handle.includes('w'))
        x = pointer[0];
    if (handle.includes('e'))
        x1 = pointer[0];
    if (handle.includes('n'))
        y = pointer[1];
    if (handle.includes('s'))
        y1 = pointer[1];
    let nw = x1 - x;
    let nh = y1 - y;
    if (keepAspect && box.w && box.h && handle.length === 2) {
        const ratio = box.h / box.w;
        const mag = Math.max(Math.abs(nw), Math.abs(nh) / ratio);
        const signW = nw < 0 ? -1 : 1;
        const signH = nh < 0 ? -1 : 1;
        nw = mag * signW;
        nh = mag * ratio * signH;
        if (handle.includes('w'))
            x = x1 - nw;
        if (handle.includes('n'))
            y = y1 - nh;
    }
    return { x, y, w: nw || 1, h: nh || 1 };
}
/** Positions the textarea exactly over the text it is editing. */
function useOverlayStyle(id) {
    const element = (0, store_1.useSketch)((s) => (id ? s.elements.find((el) => el.id === id) ?? null : null));
    const viewport = (0, store_1.useSketch)((s) => s.viewport);
    const palette = (0, store_1.useSketch)((s) => s.palette);
    return (0, react_1.useMemo)(() => {
        if (!element)
            return null;
        const zoom = viewport.zoom;
        const isFrame = element.type === 'frame';
        const box = isFrame
            ? { x: element.x, y: element.y - 28, w: Math.max(120, element.w), h: 24 }
            : element.type === 'text'
                ? { x: element.x, y: element.y, w: Math.max(element.w, 40), h: Math.max(element.h, element.fontSize * 1.3) }
                : (0, types_1.isLinear)(element)
                    ? { x: element.x + element.w / 2 - 80, y: element.y + element.h / 2 - 16, w: 160, h: 32 }
                    : { x: element.x + 8, y: element.y + 6, w: Math.max(24, element.w - 16), h: Math.max(24, element.h - 12) };
        const [left, top] = (0, geometry_1.sceneToCanvas)([box.x, box.y], viewport);
        const align = isFrame || element.type === 'text' ? element.textAlign : 'center';
        return {
            left,
            top,
            width: box.w * zoom,
            height: box.h * zoom,
            fontSize: element.fontSize * zoom,
            lineHeight: `${Math.round(element.fontSize * 1.25) * zoom}px`,
            fontFamily: types_2.FONT_STACKS[element.fontFamily],
            color: isFrame ? palette.textDim : element.textColor,
            textAlign: align,
            transform: element.angle ? `rotate(${element.angle}rad)` : undefined,
            transformOrigin: 'center',
            display: (0, types_1.isLinear)(element) || (0, types_1.isContainer)(element) ? 'flex' : 'block',
        };
    }, [element, palette, viewport]);
}

  };
  modules["src/ui/Chrome.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.StatusToast = exports.ZoomBar = exports.ActionBar = exports.DocumentBar = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// The window furniture: the document bar (name, save state, file menu),
// the action bar (undo/redo, view toggles, export, help) and the zoom
// controls. Small, but this is where most keyboard shortcuts become
// visible - every button says its own shortcut.
const react_1 = __importStar(require("react"));
const icons_1 = require("./icons");
const store_1 = require("../state/store");
const view_1 = require("../state/view");
const history_1 = require("../lib/history");
const SAVE_LABEL = {
    saved: 'Saved',
    saving: 'Saving…',
    dirty: 'Unsaved changes',
    error: 'Not saved',
};
const DocumentBar = () => {
    const name = (0, store_1.useSketch)((s) => s.name);
    const saveState = (0, store_1.useSketch)((s) => s.saveState);
    const saveError = (0, store_1.useSketch)((s) => s.saveError);
    const setDialog = (0, store_1.useSketch)((s) => s.setDialog);
    const renameDocument = (0, store_1.useSketch)((s) => s.renameDocument);
    const [editing, setEditing] = (0, react_1.useState)(null);
    const inputRef = (0, react_1.useRef)(null);
    (0, react_1.useEffect)(() => {
        if (editing !== null)
            inputRef.current?.select();
    }, [editing]);
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-island sk-docbar", children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "All boards \u2014 \u2318O", onClick: () => setDialog('files', true), "aria-keyshortcuts": "Meta+O", children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "files" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "All boards" })] }), editing !== null ? ((0, jsx_runtime_1.jsx)("input", { ref: inputRef, className: "sk-name-input", value: editing, "aria-label": "Board name", onChange: (e) => setEditing(e.target.value), onBlur: () => {
                    void renameDocument(editing);
                    setEditing(null);
                }, onKeyDown: (e) => {
                    e.stopPropagation();
                    if (e.key === 'Enter') {
                        void renameDocument(editing);
                        setEditing(null);
                    }
                    if (e.key === 'Escape')
                        setEditing(null);
                } })) : ((0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-name", title: "Rename this board", onClick: () => setEditing(name), children: [name, (0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "pencil", size: 14 })] })), (0, jsx_runtime_1.jsx)("span", { className: `sk-save sk-save-${saveState}`, title: saveError ?? undefined, role: "status", "aria-live": "polite", children: SAVE_LABEL[saveState] })] }));
};
exports.DocumentBar = DocumentBar;
const ActionBar = () => {
    const history = (0, store_1.useSketch)((s) => s.history);
    const dialogs = (0, store_1.useSketch)((s) => s.dialogs);
    const snapEnabled = (0, store_1.useSketch)((s) => s.snapEnabled);
    const background = (0, store_1.useSketch)((s) => s.background);
    const store = store_1.useSketch;
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-island sk-actionbar", role: "toolbar", "aria-label": "Board actions", children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Undo \u2014 \u2318Z", "aria-keyshortcuts": "Meta+Z", disabled: !(0, history_1.canUndo)(history), onClick: () => store.getState().undo(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "undo" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Undo" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Redo \u2014 \u2318\u21E7Z", "aria-keyshortcuts": "Meta+Shift+Z", disabled: !(0, history_1.canRedo)(history), onClick: () => store.getState().redo(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "redo" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Redo" })] }), (0, jsx_runtime_1.jsx)("span", { className: "sk-toolbar-divider", "aria-hidden": "true" }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-icon-btn${background !== 'plain' ? ' is-active' : ''}`, title: background === 'grid' ? 'Grid background — G' : background === 'dots' ? 'Dotted background — G' : 'Plain background — G', "aria-keyshortcuts": "G", onClick: () => store.getState().setBackground(background === 'grid' ? 'dots' : background === 'dots' ? 'plain' : 'grid'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "grid" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Background style" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-icon-btn${snapEnabled ? ' is-active' : ''}`, title: "Snap to guides \u2014 \u21E7S", "aria-pressed": snapEnabled, "aria-keyshortcuts": "Shift+S", onClick: () => store.getState().toggleSnap(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "magnet" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Snap to guides" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-icon-btn${dialogs.minimap ? ' is-active' : ''}`, title: "Minimap \u2014 M", "aria-pressed": dialogs.minimap, "aria-keyshortcuts": "M", onClick: () => store.getState().setDialog('minimap', !dialogs.minimap), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "map" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Minimap" })] }), (0, jsx_runtime_1.jsx)("span", { className: "sk-toolbar-divider", "aria-hidden": "true" }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Export \u2014 \u2318E", "aria-keyshortcuts": "Meta+E", onClick: () => store.getState().setDialog('export', true), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "download" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Export" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Keyboard shortcuts \u2014 ?", "aria-keyshortcuts": "?", onClick: () => store.getState().setDialog('shortcuts', true), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "help" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Keyboard shortcuts" })] })] }));
};
exports.ActionBar = ActionBar;
const ZoomBar = () => {
    const zoom = (0, store_1.useSketch)((s) => s.viewport.zoom);
    const store = store_1.useSketch;
    const step = (factor) => {
        const size = (0, view_1.canvasSize)();
        store.getState().zoomTo(zoom * factor, [size.width / 2, size.height / 2]);
    };
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-island sk-zoombar", role: "toolbar", "aria-label": "Zoom", children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Zoom out \u2014 \u2318\u2212", onClick: () => step(1 / 1.2), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "zoom-out", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Zoom out" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-zoom-value", title: "Reset zoom to 100% \u2014 \u23180", onClick: () => {
                    const size = (0, view_1.canvasSize)();
                    store.getState().zoomTo(1, [size.width / 2, size.height / 2]);
                }, children: [Math.round(zoom * 100), "%"] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Zoom in \u2014 \u2318+", onClick: () => step(1.2), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "zoom-in", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Zoom in" })] }), (0, jsx_runtime_1.jsx)("span", { className: "sk-toolbar-divider", "aria-hidden": "true" }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Zoom to fit \u2014 \u21E71 (selection: \u21E72)", onClick: () => store.getState().zoomToFit((0, view_1.canvasSize)(), false), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "fit", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Zoom to fit" })] })] }));
};
exports.ZoomBar = ZoomBar;
const StatusToast = () => {
    const status = (0, store_1.useSketch)((s) => s.status);
    const setStatus = (0, store_1.useSketch)((s) => s.setStatus);
    (0, react_1.useEffect)(() => {
        if (!status)
            return;
        const timer = setTimeout(() => setStatus(null), 3200);
        return () => clearTimeout(timer);
    }, [status, setStatus]);
    if (!status)
        return null;
    return ((0, jsx_runtime_1.jsx)("div", { className: "sk-toast", role: "status", "aria-live": "polite", children: status }));
};
exports.StatusToast = StatusToast;

  };
  modules["src/ui/Toolbar.tsx"] = function (module, exports, require) {
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Toolbar = exports.TOOLS = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// The tool island. Twelve tools, each reachable by a letter and the
// first ten by a number, with the shortcut shown on the button so nobody
// has to open the help sheet twice.
const react_1 = __importDefault(require("react"));
const icons_1 = require("./icons");
const store_1 = require("../state/store");
exports.TOOLS = [
    { tool: 'select', icon: 'select', label: 'Select', key: 'V', digit: '1' },
    { tool: 'hand', icon: 'hand', label: 'Hand (pan)', key: 'H', digit: '2' },
    { tool: 'rect', icon: 'rect', label: 'Rectangle', key: 'R', digit: '3' },
    { tool: 'diamond', icon: 'diamond', label: 'Diamond', key: 'D', digit: '4' },
    { tool: 'ellipse', icon: 'ellipse', label: 'Ellipse', key: 'O', digit: '5' },
    { tool: 'arrow', icon: 'arrow', label: 'Arrow', key: 'A', digit: '6' },
    { tool: 'line', icon: 'line', label: 'Line', key: 'L', digit: '7' },
    { tool: 'draw', icon: 'draw', label: 'Draw', key: 'P', digit: '8' },
    { tool: 'text', icon: 'text', label: 'Text', key: 'T', digit: '9' },
    { tool: 'sticky', icon: 'sticky', label: 'Sticky note', key: 'N', digit: '0' },
    { tool: 'frame', icon: 'frame', label: 'Frame', key: 'F' },
    { tool: 'eraser', icon: 'eraser', label: 'Eraser', key: 'E' },
];
const Toolbar = () => {
    const tool = (0, store_1.useSketch)((s) => s.tool);
    const setTool = (0, store_1.useSketch)((s) => s.setTool);
    const setDialog = (0, store_1.useSketch)((s) => s.setDialog);
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-island sk-toolbar", role: "toolbar", "aria-label": "Tools", children: [exports.TOOLS.map((spec) => ((0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-tool${tool === spec.tool ? ' is-active' : ''}`, "aria-pressed": tool === spec.tool, "aria-keyshortcuts": spec.key, title: `${spec.label} — ${spec.key}`, onClick: () => setTool(spec.tool), onDoubleClick: () => setTool(spec.tool, true), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: spec.icon }), spec.digit ? (0, jsx_runtime_1.jsx)("span", { className: "sk-tool-digit", "aria-hidden": "true", children: spec.digit }) : null, (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: spec.label })] }, spec.tool))), (0, jsx_runtime_1.jsx)("span", { className: "sk-toolbar-divider", "aria-hidden": "true" }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-tool", title: "Shape library \u2014 /", "aria-keyshortcuts": "/", onClick: () => setDialog('slash', true), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "library" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Shape library" })] })] }));
};
exports.Toolbar = Toolbar;

  };
  modules["src/ui/Properties.tsx"] = function (module, exports, require) {
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Properties = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// The properties panel: everything about how the selection looks, and
// everything you can do to it as a group. It shows what the next shape
// will look like when nothing is selected and a drawing tool is active,
// so the choices you make before drawing are the ones you get.
const react_1 = __importDefault(require("react"));
const types_1 = require("../lib/types");
const palette_1 = require("../lib/palette");
const icons_1 = require("./icons");
const store_1 = require("../state/store");
function Seg({ label, value, options, onChange, }) {
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", id: `sk-lbl-${label.replace(/\s+/g, '-')}`, children: label }), (0, jsx_runtime_1.jsx)("div", { className: "sk-seg", role: "group", "aria-labelledby": `sk-lbl-${label.replace(/\s+/g, '-')}`, children: options.map((opt) => ((0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-seg-btn${opt.value === value ? ' is-active' : ''}`, "aria-pressed": opt.value === value, title: opt.label, onClick: () => onChange(opt.value), children: [opt.icon ? (0, jsx_runtime_1.jsx)(icons_1.Icon, { name: opt.icon, size: 16 }) : (0, jsx_runtime_1.jsx)("span", { className: "sk-seg-text", children: opt.text ?? opt.label }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: opt.label })] }, String(opt.value)))) })] }));
}
function Swatches({ label, value, swatches, onChange, }) {
    const [custom, setCustom] = react_1.default.useState('');
    const known = swatches.some((s) => s.value === value);
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: label }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-swatches", role: "group", "aria-label": label, children: [swatches.map((s) => ((0, jsx_runtime_1.jsx)("button", { type: "button", className: `sk-swatch${s.value === value ? ' is-active' : ''}${s.value === 'transparent' ? ' is-none' : ''}`, style: s.value === 'transparent' ? undefined : { background: s.value }, title: s.name, "aria-pressed": s.value === value, onClick: () => onChange(s.value), children: (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: s.name }) }, s.value))), (0, jsx_runtime_1.jsxs)("label", { className: "sk-swatch-custom", title: "Custom colour", children: [(0, jsx_runtime_1.jsxs)("span", { className: "sk-sr", children: ["Custom ", label.toLowerCase(), " colour, as a hex code"] }), (0, jsx_runtime_1.jsx)("input", { type: "text", inputMode: "text", spellCheck: false, placeholder: known ? '#hex' : value.replace('#', ''), value: custom, onChange: (e) => {
                                    setCustom(e.target.value);
                                    const hex = (0, palette_1.normalizeHex)(e.target.value);
                                    if (hex)
                                        onChange(hex);
                                } })] })] })] }));
}
const first = (list) => list[0];
const Properties = () => {
    const elements = (0, store_1.useSketch)((s) => s.elements);
    const selectedIds = (0, store_1.useSketch)((s) => s.selectedIds);
    const style = (0, store_1.useSketch)((s) => s.style);
    const palette = (0, store_1.useSketch)((s) => s.palette);
    const tool = (0, store_1.useSketch)((s) => s.tool);
    const setStyle = (0, store_1.useSketch)((s) => s.setStyle);
    const store = store_1.useSketch;
    const selected = react_1.default.useMemo(() => {
        const ids = new Set(selectedIds);
        return elements.filter((el) => ids.has(el.id));
    }, [elements, selectedIds]);
    const drawing = tool !== 'select' && tool !== 'hand' && tool !== 'eraser';
    if (!selected.length && !drawing)
        return null;
    const sample = first(selected) ?? style;
    const types = new Set(selected.map((el) => el.type));
    const anyLinear = selected.some(types_1.isLinear) || tool === 'arrow' || tool === 'line' || tool === 'draw';
    const anyShape = selected.some((el) => !(0, types_1.isLinear)(el) && el.type !== 'text') || ['rect', 'ellipse', 'diamond', 'sticky', 'frame'].includes(tool);
    const anyRect = types.has('rect') || tool === 'rect';
    const anyArrow = selected.some((el) => el.type === 'arrow') || tool === 'arrow';
    const hasText = selected.some((el) => el.text || el.type === 'text' || el.type === 'sticky') || tool === 'text' || tool === 'sticky';
    const multiple = selected.length > 1;
    const locked = selected.length > 0 && selected.every((el) => el.locked);
    return ((0, jsx_runtime_1.jsxs)("aside", { className: "sk-island sk-properties", "aria-label": "Properties", children: [(0, jsx_runtime_1.jsx)(Swatches, { label: "Stroke", value: sample.stroke, swatches: palette.strokes, onChange: (stroke) => setStyle({ stroke }) }), anyShape || anyLinear ? ((0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsx)(Swatches, { label: "Fill", value: sample.fill, swatches: palette.fills, onChange: (fill) => setStyle({ fill, fillStyle: fill === 'transparent' ? 'none' : sample.fillStyle === 'none' ? 'solid' : sample.fillStyle }) }), (0, jsx_runtime_1.jsx)(Seg, { label: "Fill style", value: sample.fillStyle, onChange: (fillStyle) => setStyle({ fillStyle }), options: [
                            { value: 'none', label: 'No fill', text: 'None' },
                            { value: 'solid', label: 'Solid fill', text: 'Solid' },
                            { value: 'hatch', label: 'Hatched fill', text: 'Hatch' },
                        ] })] })) : null, (0, jsx_runtime_1.jsx)(Seg, { label: "Stroke width", value: sample.strokeWidth, onChange: (strokeWidth) => setStyle({ strokeWidth }), options: [
                    { value: types_1.STROKE_WIDTHS[0], label: 'Thin', text: 'S' },
                    { value: types_1.STROKE_WIDTHS[1], label: 'Medium', text: 'M' },
                    { value: types_1.STROKE_WIDTHS[2], label: 'Bold', text: 'L' },
                ] }), (0, jsx_runtime_1.jsx)(Seg, { label: "Stroke style", value: sample.strokeStyle, onChange: (strokeStyle) => setStyle({ strokeStyle }), options: [
                    { value: 'solid', label: 'Solid line', text: '───' },
                    { value: 'dashed', label: 'Dashed line', text: '– –' },
                    { value: 'dotted', label: 'Dotted line', text: '· ·' },
                ] }), anyRect ? ((0, jsx_runtime_1.jsx)(Seg, { label: "Corners", value: sample.roundness > 0.02 ? 'round' : 'sharp', onChange: (v) => setStyle({ roundness: v === 'round' ? 0.16 : 0 }), options: [
                    { value: 'sharp', label: 'Sharp corners', text: 'Sharp' },
                    { value: 'round', label: 'Rounded corners', text: 'Round' },
                ] })) : null, anyLinear ? ((0, jsx_runtime_1.jsx)(Seg, { label: "Line shape", value: sample.edge, onChange: (edge) => setStyle({ edge }), options: [
                    { value: 'straight', label: 'Straight', text: 'Straight' },
                    { value: 'curved', label: 'Curved', text: 'Curved' },
                    { value: 'elbow', label: 'Elbow', text: 'Elbow' },
                ] })) : null, anyArrow ? ((0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: "Arrowheads" }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-arrowheads", children: [(0, jsx_runtime_1.jsx)(Seg, { label: "Start", value: sample.startArrow, onChange: (startArrow) => setStyle({ startArrow }), options: [
                                    { value: 'none', label: 'No head at the start', text: '—' },
                                    { value: 'arrow', label: 'Arrow at the start', text: '<' },
                                    { value: 'dot', label: 'Dot at the start', text: '•' },
                                    { value: 'bar', label: 'Bar at the start', text: '|' },
                                ] }), (0, jsx_runtime_1.jsx)(Seg, { label: "End", value: sample.endArrow, onChange: (endArrow) => setStyle({ endArrow }), options: [
                                    { value: 'none', label: 'No head at the end', text: '—' },
                                    { value: 'arrow', label: 'Arrow at the end', text: '>' },
                                    { value: 'dot', label: 'Dot at the end', text: '•' },
                                    { value: 'bar', label: 'Bar at the end', text: '|' },
                                ] })] })] })) : null, hasText ? ((0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsx)(Seg, { label: "Font size", value: sample.fontSize, onChange: (fontSize) => setStyle({ fontSize }), options: [
                            { value: types_1.FONT_SIZES[0], label: 'Small', text: 'S' },
                            { value: types_1.FONT_SIZES[1], label: 'Medium', text: 'M' },
                            { value: types_1.FONT_SIZES[2], label: 'Large', text: 'L' },
                            { value: types_1.FONT_SIZES[3], label: 'Extra large', text: 'XL' },
                        ] }), (0, jsx_runtime_1.jsx)(Seg, { label: "Font", value: sample.fontFamily, onChange: (fontFamily) => setStyle({ fontFamily }), options: [
                            { value: 'sans', label: 'Sans serif', text: 'Aa' },
                            { value: 'serif', label: 'Serif', text: 'Aa' },
                            { value: 'mono', label: 'Monospace', text: 'Aa' },
                        ] }), (0, jsx_runtime_1.jsx)(Seg, { label: "Align text", value: sample.textAlign, onChange: (textAlign) => setStyle({ textAlign }), options: [
                            { value: 'left', label: 'Align left', icon: 'align-left' },
                            { value: 'center', label: 'Align centre', icon: 'align-center-x' },
                            { value: 'right', label: 'Align right', icon: 'align-right' },
                        ] }), (0, jsx_runtime_1.jsx)(Swatches, { label: "Text colour", value: sample.textColor, swatches: palette.strokes, onChange: (textColor) => setStyle({ textColor }) })] })) : null, (0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("label", { className: "sk-field-label", htmlFor: "sk-opacity", children: "Opacity" }), (0, jsx_runtime_1.jsx)("input", { id: "sk-opacity", className: "sk-range", type: "range", min: 10, max: 100, step: 5, value: Math.round(sample.opacity * 100), onChange: (e) => setStyle({ opacity: Number(e.target.value) / 100 }) })] }), selected.length ? ((0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: "Layers" }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-seg", role: "group", "aria-label": "Layers", children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Send to back \u2014 \u2318\u21E7[", onClick: () => store.getState().order('back'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "to-back", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Send to back" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Send backward \u2014 \u2318[", onClick: () => store.getState().order('backward'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "backward", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Send backward" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Bring forward \u2014 \u2318]", onClick: () => store.getState().order('forward'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "forward", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Bring forward" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Bring to front \u2014 \u2318\u21E7]", onClick: () => store.getState().order('front'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "to-front", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Bring to front" })] })] })] }), multiple ? ((0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: "Align" }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-seg sk-seg-wrap", role: "group", "aria-label": "Align and distribute", children: [[
                                        ['left', 'align-left', 'Align left'],
                                        ['center-x', 'align-center-x', 'Align centres horizontally'],
                                        ['right', 'align-right', 'Align right'],
                                        ['top', 'align-top', 'Align top'],
                                        ['center-y', 'align-center-y', 'Align centres vertically'],
                                        ['bottom', 'align-bottom', 'Align bottom'],
                                    ].map(([mode, icon, label]) => ((0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: label, onClick: () => store.getState().align(mode), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: icon, size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: label })] }, mode))), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Distribute horizontally", disabled: selected.length < 3, onClick: () => store.getState().distribute('horizontal'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "distribute-x", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Distribute horizontally" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Distribute vertically", disabled: selected.length < 3, onClick: () => store.getState().distribute('vertical'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "distribute-y", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Distribute vertically" })] })] })] })) : null, (0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: "Actions" }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-seg sk-seg-wrap", role: "group", "aria-label": "Actions", children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Duplicate \u2014 \u2318D", onClick: () => store.getState().duplicateSelected(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "copy", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Duplicate" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Flip horizontally \u2014 \u21E7H", onClick: () => store.getState().flip('horizontal'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "flip-x", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Flip horizontally" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Flip vertically \u2014 \u21E7V", onClick: () => store.getState().flip('vertical'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "flip-y", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Flip vertically" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: multiple ? 'Group — ⌘G' : 'Group needs two or more elements', disabled: !multiple, onClick: () => store.getState().group(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "group", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Group" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn", title: "Ungroup \u2014 \u2318\u21E7G", onClick: () => store.getState().ungroup(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "ungroup", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Ungroup" })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-seg-btn${locked ? ' is-active' : ''}`, title: locked ? 'Unlock — ⌘⇧L' : 'Lock — ⌘⇧L', "aria-pressed": locked, onClick: () => store.getState().toggleLock(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: locked ? 'lock' : 'unlock', size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: locked ? 'Unlock' : 'Lock' })] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-seg-btn sk-danger", title: "Delete \u2014 Del", onClick: () => store.getState().deleteSelected(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "trash", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Delete" })] })] })] })] })) : null] }));
};
exports.Properties = Properties;

  };
  modules["src/ui/FileSwitcher.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.FileSwitcher = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// Every board you have, newest first: open one, rename it, duplicate it,
// delete it, start a new one, or bring a .sketch.json in from elsewhere.
const react_1 = __importStar(require("react"));
const Modal_1 = require("./Modal");
const icons_1 = require("./icons");
const store_1 = require("../state/store");
const files_1 = require("../state/files");
const when = (mtime) => {
    if (!mtime)
        return '';
    const date = new Date(mtime);
    const days = Math.floor((Date.now() - mtime) / 86400000);
    if (days <= 0)
        return `today, ${date.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' })}`;
    if (days === 1)
        return 'yesterday';
    if (days < 30)
        return `${days} days ago`;
    return date.toLocaleDateString([], { year: 'numeric', month: 'short', day: 'numeric' });
};
const FileSwitcher = () => {
    const files = (0, store_1.useSketch)((s) => s.files);
    const path = (0, store_1.useSketch)((s) => s.path);
    const recent = (0, store_1.useSketch)((s) => s.recent);
    const store = store_1.useSketch;
    const [query, setQuery] = (0, react_1.useState)('');
    const [confirming, setConfirming] = (0, react_1.useState)(null);
    const fileInput = (0, react_1.useRef)(null);
    (0, react_1.useEffect)(() => {
        void store.getState().refreshFiles();
    }, [store]);
    const shown = (0, react_1.useMemo)(() => {
        const q = query.trim().toLowerCase();
        const list = q ? files.filter((f) => f.name.toLowerCase().includes(q)) : files;
        const recentFirst = [...list].sort((a, b) => {
            const ra = recent.indexOf(a.path);
            const rb = recent.indexOf(b.path);
            if (ra === rb)
                return b.mtime - a.mtime;
            if (ra === -1)
                return 1;
            if (rb === -1)
                return -1;
            return ra - rb;
        });
        return recentFirst;
    }, [files, query, recent]);
    const close = () => store.getState().setDialog('files', false);
    return ((0, jsx_runtime_1.jsxs)(Modal_1.Modal, { title: "Boards", description: `Saved in your Sketches folder as ${files_1.FILE_EXTENSION} files.`, onClose: close, footer: (0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-btn", onClick: () => {
                        void store.getState().newDocument('Untitled');
                        close();
                    }, children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "plus", size: 16 }), " New board"] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-btn", onClick: () => fileInput.current?.click(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "upload", size: 16 }), " Import a file"] }), (0, jsx_runtime_1.jsx)("input", { ref: fileInput, type: "file", accept: ".json,application/json", className: "sk-sr", onChange: async (e) => {
                        const file = e.target.files?.[0];
                        e.target.value = '';
                        if (!file)
                            return;
                        const text = await file.text();
                        await store.getState().importDocument(text, file.name.replace(/\.sketch\.json$|\.json$/, ''));
                        close();
                    } })] }), children: [(0, jsx_runtime_1.jsxs)("label", { className: "sk-search", children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "search", size: 16 }), (0, jsx_runtime_1.jsx)("input", { type: "search", value: query, placeholder: "Search boards", "aria-label": "Search boards", onChange: (e) => setQuery(e.target.value), onKeyDown: (e) => e.stopPropagation() })] }), shown.length === 0 ? ((0, jsx_runtime_1.jsx)("p", { className: "sk-empty", children: files.length ? 'No board matches that.' : 'No boards yet - the one you are on will appear here once it is saved.' })) : ((0, jsx_runtime_1.jsx)("ul", { className: "sk-filelist", children: shown.map((file) => ((0, jsx_runtime_1.jsxs)("li", { className: file.path === path ? 'is-current' : undefined, children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-file-open", onClick: () => {
                                void store.getState().openDocument(file.path);
                                close();
                            }, children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "file", size: 16 }), (0, jsx_runtime_1.jsx)("span", { className: "sk-file-name", children: file.name }), (0, jsx_runtime_1.jsx)("span", { className: "sk-file-meta", children: when(file.mtime) })] }), (0, jsx_runtime_1.jsxs)("span", { className: "sk-file-actions", children: [file.path === path ? ((0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: "Duplicate this board", onClick: () => {
                                        void store.getState().duplicateDocument();
                                        close();
                                    }, children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "copy", size: 16 }), (0, jsx_runtime_1.jsxs)("span", { className: "sk-sr", children: ["Duplicate ", file.name] })] })) : null, confirming === file.path ? ((0, jsx_runtime_1.jsx)("button", { type: "button", className: "sk-btn sk-btn-danger sk-btn-small", onClick: () => {
                                        void store.getState().deleteDocument(file.path);
                                        setConfirming(null);
                                    }, children: "Delete for good" })) : ((0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", title: `Delete ${file.name}`, onClick: () => setConfirming(file.path), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "trash", size: 16 }), (0, jsx_runtime_1.jsxs)("span", { className: "sk-sr", children: ["Delete ", file.name] })] }))] })] }, file.path))) }))] }));
};
exports.FileSwitcher = FileSwitcher;

  };
  modules["src/ui/ExportDialog.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExportDialog = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// Export: PNG or SVG, the whole board, the selection or one frame, with
// or without the paper colour, at 1x or 2x. A live preview shows exactly
// what will land in the file, because the preview is drawn by the same
// two functions that write it.
const react_1 = __importStar(require("react"));
const Modal_1 = require("./Modal");
const icons_1 = require("./icons");
const geometry_1 = require("../lib/geometry");
const svg_1 = require("../lib/svg");
const align_1 = require("../lib/align");
const document_1 = require("../lib/document");
const png_1 = require("../canvas/png");
const store_1 = require("../state/store");
const view_1 = require("../state/view");
const PAD = 24;
const ExportDialog = () => {
    const elements = (0, store_1.useSketch)((s) => s.elements);
    const selectedIds = (0, store_1.useSketch)((s) => s.selectedIds);
    const palette = (0, store_1.useSketch)((s) => s.palette);
    const name = (0, store_1.useSketch)((s) => s.name);
    const store = store_1.useSketch;
    const frames = (0, react_1.useMemo)(() => elements.filter((el) => el.type === 'frame'), [elements]);
    const [scope, setScope] = (0, react_1.useState)(selectedIds.length ? { kind: 'selection' } : { kind: 'board' });
    const [format, setFormat] = (0, react_1.useState)('png');
    const [scale, setScale] = (0, react_1.useState)(2);
    const [withBackground, setWithBackground] = (0, react_1.useState)(true);
    const [preview, setPreview] = (0, react_1.useState)(null);
    const [note, setNote] = (0, react_1.useState)(null);
    const chosen = (0, react_1.useMemo)(() => {
        if (scope.kind === 'selection') {
            const ids = new Set(selectedIds);
            const picked = elements.filter((el) => ids.has(el.id));
            return { elements: picked, bounds: (0, geometry_1.expandBounds)(picked.length ? (0, geometry_1.boundsOfElements)(picked) : { x: 0, y: 0, w: 1, h: 1 }, PAD) };
        }
        if (scope.kind === 'frame') {
            const frame = elements.find((el) => el.id === scope.id);
            if (frame) {
                const inside = (0, align_1.elementsInFrame)(elements, frame);
                return { elements: inside, bounds: (0, geometry_1.rotatedBounds)(frame) };
            }
        }
        const drawable = elements;
        return { elements: drawable, bounds: (0, geometry_1.expandBounds)(drawable.length ? (0, geometry_1.boundsOfElements)(drawable) : { x: 0, y: 0, w: 1, h: 1 }, PAD) };
    }, [elements, scope, selectedIds]);
    const background = withBackground ? palette.background : null;
    const fileStem = (0, document_1.sanitizeName)(name) + (scope.kind === 'frame' ? ` - ${elements.find((el) => el.id === scope.id)?.name || 'frame'}` : '');
    const svgText = (0, react_1.useMemo)(() => (0, svg_1.elementsToSvg)(chosen.elements, {
        bounds: chosen.bounds,
        background,
        title: name,
        frameStroke: palette.frameStroke,
        measure: view_1.viewRef.current?.measure ?? undefined,
    }), [background, chosen, name, palette.frameStroke]);
    (0, react_1.useEffect)(() => {
        let url = null;
        let cancelled = false;
        const run = async () => {
            try {
                const previewScale = Math.min(1.5, 520 / Math.max(chosen.bounds.w, 1));
                const blob = await (0, png_1.renderPngBlob)(chosen.elements, chosen.bounds, { scale: previewScale, background: background ?? null, palette });
                if (cancelled)
                    return;
                url = URL.createObjectURL(blob);
                setPreview(url);
            }
            catch {
                setPreview(null);
            }
        };
        void run();
        return () => {
            cancelled = true;
            if (url)
                URL.revokeObjectURL(url);
        };
    }, [background, chosen, palette]);
    const close = () => store.getState().setDialog('export', false);
    const doDownload = async () => {
        if (format === 'svg') {
            (0, png_1.downloadBlob)((0, png_1.svgBlob)(svgText), `${fileStem}.svg`);
            return;
        }
        const blob = await (0, png_1.renderPngBlob)(chosen.elements, chosen.bounds, { scale, background, palette });
        (0, png_1.downloadBlob)(blob, `${fileStem}.png`);
    };
    const doCopy = async () => {
        if (format === 'svg') {
            const ok = await (0, png_1.copyTextToClipboard)(svgText);
            setNote(ok ? 'SVG copied to the clipboard.' : 'This browser would not let Sketch write to the clipboard.');
            return;
        }
        const blob = await (0, png_1.renderPngBlob)(chosen.elements, chosen.bounds, { scale, background, palette });
        const ok = await (0, png_1.copyBlobToClipboard)(blob);
        setNote(ok ? 'PNG copied to the clipboard.' : 'This browser will not copy images - download it instead.');
    };
    const empty = chosen.elements.length === 0;
    return ((0, jsx_runtime_1.jsx)(Modal_1.Modal, { title: "Export", description: `${Math.round(chosen.bounds.w)} x ${Math.round(chosen.bounds.h)} points${format === 'png' ? ` - ${Math.round(chosen.bounds.w * scale)} x ${Math.round(chosen.bounds.h * scale)} pixels` : ''}`, wide: true, onClose: close, footer: (0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-modal-note", role: "status", "aria-live": "polite", children: note }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-btn", disabled: empty, onClick: () => void doCopy(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "copy", size: 16 }), " Copy"] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-btn sk-btn-primary", disabled: empty, onClick: () => void doDownload(), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "download", size: 16 }), " Download ", format.toUpperCase()] })] }), children: (0, jsx_runtime_1.jsxs)("div", { className: "sk-export", children: [(0, jsx_runtime_1.jsx)("div", { className: "sk-export-preview", children: preview && !empty ? ((0, jsx_runtime_1.jsx)("img", { src: preview, alt: `Preview of the ${format.toUpperCase()} export` })) : ((0, jsx_runtime_1.jsx)("p", { className: "sk-empty", children: "Nothing to export yet." })) }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-export-options", children: [(0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: "What" }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-seg sk-seg-wrap", role: "group", "aria-label": "What to export", children: [(0, jsx_runtime_1.jsx)("button", { type: "button", className: `sk-seg-btn${scope.kind === 'board' ? ' is-active' : ''}`, onClick: () => setScope({ kind: 'board' }), children: "Whole board" }), (0, jsx_runtime_1.jsx)("button", { type: "button", className: `sk-seg-btn${scope.kind === 'selection' ? ' is-active' : ''}`, disabled: !selectedIds.length, onClick: () => setScope({ kind: 'selection' }), children: "Selection" }), frames.map((frame) => ((0, jsx_runtime_1.jsx)("button", { type: "button", className: `sk-seg-btn${scope.kind === 'frame' && scope.id === frame.id ? ' is-active' : ''}`, onClick: () => setScope({ kind: 'frame', id: frame.id }), children: frame.name || 'Frame' }, frame.id)))] })] }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: "Format" }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-seg", role: "group", "aria-label": "Format", children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-seg-btn${format === 'png' ? ' is-active' : ''}`, onClick: () => setFormat('png'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "image", size: 16 }), " PNG"] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: `sk-seg-btn${format === 'svg' ? ' is-active' : ''}`, onClick: () => setFormat('svg'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "code", size: 16 }), " SVG"] })] })] }), format === 'png' ? ((0, jsx_runtime_1.jsxs)("div", { className: "sk-field", children: [(0, jsx_runtime_1.jsx)("span", { className: "sk-field-label", children: "Size" }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-seg", role: "group", "aria-label": "Pixel scale", children: [(0, jsx_runtime_1.jsx)("button", { type: "button", className: `sk-seg-btn${scale === 1 ? ' is-active' : ''}`, onClick: () => setScale(1), children: "1x" }), (0, jsx_runtime_1.jsx)("button", { type: "button", className: `sk-seg-btn${scale === 2 ? ' is-active' : ''}`, onClick: () => setScale(2), children: "2x" })] })] })) : null, (0, jsx_runtime_1.jsxs)("label", { className: "sk-check", children: [(0, jsx_runtime_1.jsx)("input", { type: "checkbox", checked: withBackground, onChange: (e) => setWithBackground(e.target.checked) }), (0, jsx_runtime_1.jsx)("span", { children: "Include the background colour" })] }), (0, jsx_runtime_1.jsx)("p", { className: "sk-hint", children: "SVG keeps every shape editable and every word searchable. PNG is the one to paste into a chat." })] })] }) }));
};
exports.ExportDialog = ExportDialog;

  };
  modules["src/ui/Shortcuts.tsx"] = function (module, exports, require) {
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Shortcuts = exports.SHORTCUT_GROUPS = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// The ? sheet. Every shortcut the app answers to, grouped the way people
// look for them, with the platform's own modifier symbols.
const react_1 = __importDefault(require("react"));
const Modal_1 = require("./Modal");
const store_1 = require("../state/store");
const isApple = typeof navigator !== 'undefined' && /Mac|iPhone|iPad/.test(navigator.platform || navigator.userAgent);
const MOD = isApple ? '⌘' : 'Ctrl';
const ALT = isApple ? '⌥' : 'Alt';
exports.SHORTCUT_GROUPS = [
    {
        title: 'Tools',
        items: [
            ['V / 1', 'Select'],
            ['H / 2', 'Hand (pan)'],
            ['R / 3', 'Rectangle'],
            ['D / 4', 'Diamond'],
            ['O / 5', 'Ellipse'],
            ['A / 6', 'Arrow'],
            ['L / 7', 'Line'],
            ['P / 8', 'Draw'],
            ['T / 9', 'Text'],
            ['N / 0', 'Sticky note'],
            ['F', 'Frame'],
            ['E', 'Eraser'],
            ['/', 'Shape library'],
            ['Double-click a tool', 'Keep it after drawing'],
        ],
    },
    {
        title: 'Canvas',
        items: [
            ['Space + drag', 'Pan'],
            ['Middle or right drag', 'Pan'],
            ['Two fingers', 'Pan and pinch to zoom'],
            [`${MOD} + wheel`, 'Zoom to the cursor'],
            [`${MOD} + / ${MOD} −`, 'Zoom in / out'],
            [`${MOD}0`, 'Zoom to 100%'],
            ['⇧1', 'Zoom to fit'],
            ['⇧2', 'Zoom to selection'],
            ['G', 'Grid, dots or plain'],
            ['⇧S', 'Snapping on or off'],
            ['M', 'Minimap'],
        ],
    },
    {
        title: 'Editing',
        items: [
            [`${MOD}Z / ${MOD}⇧Z`, 'Undo / redo'],
            [`${MOD}C / ${MOD}V / ${MOD}X`, 'Copy / paste / cut'],
            [`${MOD}D`, 'Duplicate'],
            [`${ALT} + drag`, 'Duplicate as you drag'],
            ['Delete', 'Delete selection'],
            [`${MOD}A`, 'Select all'],
            ['Arrows', 'Nudge 1px (⇧ for 10px)'],
            ['Double-click', 'Edit text in place'],
            ['Enter', 'Edit the selected element'],
            ['Escape', 'Deselect or stop editing'],
        ],
    },
    {
        title: 'Arranging',
        items: [
            [`${MOD}G / ${MOD}⇧G`, 'Group / ungroup'],
            [`${MOD}] / ${MOD}[`, 'Bring forward / send backward'],
            [`${MOD}⇧] / ${MOD}⇧[`, 'Bring to front / send to back'],
            [`${MOD}⇧L`, 'Lock or unlock'],
            ['⇧H / ⇧V', 'Flip horizontally / vertically'],
            ['Shift + drag', 'Keep the aspect ratio, or one axis'],
            ['Shift while rotating', 'Snap to 15°'],
        ],
    },
    {
        title: 'Files',
        items: [
            [`${MOD}O`, 'All boards'],
            [`${MOD}N`, 'New board'],
            [`${MOD}E`, 'Export'],
            [`${MOD}S`, 'Save now (autosave is always on)'],
            ['?', 'This list'],
        ],
    },
];
const Shortcuts = () => {
    const setDialog = (0, store_1.useSketch)((s) => s.setDialog);
    return ((0, jsx_runtime_1.jsx)(Modal_1.Modal, { title: "Keyboard shortcuts", description: "Everything here works without the mouse.", wide: true, onClose: () => setDialog('shortcuts', false), children: (0, jsx_runtime_1.jsx)("div", { className: "sk-shortcut-grid", children: exports.SHORTCUT_GROUPS.map((group) => ((0, jsx_runtime_1.jsxs)("section", { children: [(0, jsx_runtime_1.jsx)("h3", { children: group.title }), (0, jsx_runtime_1.jsx)("dl", { children: group.items.map(([keys, what]) => ((0, jsx_runtime_1.jsxs)("div", { children: [(0, jsx_runtime_1.jsx)("dt", { children: (0, jsx_runtime_1.jsx)("kbd", { children: keys }) }), (0, jsx_runtime_1.jsx)("dd", { children: what })] }, keys + what))) })] }, group.title))) }) }));
};
exports.Shortcuts = Shortcuts;

  };
  modules["src/ui/SlashPalette.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.SlashPalette = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// The slash palette: type / anywhere on the board and the ready-made
// shapes appear, filtered as you type, chosen with the arrow keys. It is
// the fastest way to put a flowchart together without touching the mouse.
const react_1 = __importStar(require("react"));
const Modal_1 = require("./Modal");
const icons_1 = require("./icons");
const library_1 = require("../lib/library");
const store_1 = require("../state/store");
const view_1 = require("../state/view");
const ICONS = {
    start: 'ellipse',
    process: 'rect',
    decision: 'diamond',
    database: 'files',
    cloud: 'cloud',
    person: 'sparkle',
};
const SlashPalette = () => {
    const store = store_1.useSketch;
    const [query, setQuery] = (0, react_1.useState)('');
    const [index, setIndex] = (0, react_1.useState)(0);
    const results = (0, react_1.useMemo)(() => (query ? (0, library_1.searchLibrary)(query) : library_1.LIBRARY), [query]);
    const close = () => store.getState().setDialog('slash', false);
    const insert = (id) => {
        store.getState().insertLibrary(id, (0, view_1.sceneCenter)());
        close();
    };
    return ((0, jsx_runtime_1.jsxs)(Modal_1.Modal, { title: "Shape library", description: "Ready-made pieces, dropped into the middle of the view.", onClose: close, children: [(0, jsx_runtime_1.jsxs)("label", { className: "sk-search", children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "search", size: 16 }), (0, jsx_runtime_1.jsx)("input", { type: "search", autoFocus: true, value: query, placeholder: "Search shapes", "aria-label": "Search shapes", onChange: (e) => {
                            setQuery(e.target.value);
                            setIndex(0);
                        }, onKeyDown: (e) => {
                            e.stopPropagation();
                            if (e.key === 'ArrowDown') {
                                e.preventDefault();
                                setIndex((i) => Math.min(i + 1, results.length - 1));
                            }
                            else if (e.key === 'ArrowUp') {
                                e.preventDefault();
                                setIndex((i) => Math.max(i - 1, 0));
                            }
                            else if (e.key === 'Enter' && results[index]) {
                                e.preventDefault();
                                insert(results[index].id);
                            }
                        } })] }), results.length === 0 ? ((0, jsx_runtime_1.jsxs)("p", { className: "sk-empty", children: ["Nothing matches \"", query, "\"."] })) : ((0, jsx_runtime_1.jsx)("ul", { className: "sk-palette", role: "listbox", "aria-label": "Shapes", children: results.map((item, i) => ((0, jsx_runtime_1.jsx)("li", { children: (0, jsx_runtime_1.jsxs)("button", { type: "button", role: "option", "aria-selected": i === index, className: `sk-palette-item${i === index ? ' is-active' : ''}`, onMouseEnter: () => setIndex(i), onClick: () => insert(item.id), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: ICONS[item.id] ?? 'rect' }), (0, jsx_runtime_1.jsx)("span", { className: "sk-palette-name", children: item.name }), (0, jsx_runtime_1.jsx)("span", { className: "sk-palette-keys", children: item.keywords.slice(0, 3).join(', ') })] }) }, item.id))) }))] }));
};
exports.SlashPalette = SlashPalette;

  };
  modules["src/ui/Minimap.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Minimap = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// A small map of the whole board with the current view drawn on it.
// Click or drag it to go somewhere. It redraws only when something
// actually changed, and it draws elements as plain boxes - at this size
// a faithful render would be mud.
const react_1 = __importStar(require("react"));
const geometry_1 = require("../lib/geometry");
const types_1 = require("../lib/types");
const store_1 = require("../state/store");
const view_1 = require("../state/view");
const WIDTH = 188;
const HEIGHT = 124;
const Minimap = () => {
    const ref = (0, react_1.useRef)(null);
    const dragging = (0, react_1.useRef)(false);
    const draw = (0, react_1.useCallback)(() => {
        const canvas = ref.current;
        const ctx = canvas?.getContext('2d');
        if (!canvas || !ctx)
            return;
        const { elements, viewport, palette } = store_1.useSketch.getState();
        const view = (0, view_1.canvasSize)();
        const dpr = Math.min(window.devicePixelRatio || 1, 2);
        canvas.width = WIDTH * dpr;
        canvas.height = HEIGHT * dpr;
        ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
        ctx.clearRect(0, 0, WIDTH, HEIGHT);
        const viewBounds = { x: viewport.scrollX, y: viewport.scrollY, w: view.width / viewport.zoom, h: view.height / viewport.zoom };
        const content = elements.length ? (0, geometry_1.boundsOfElements)(elements) : viewBounds;
        const world = (0, geometry_1.expandBounds)({
            x: Math.min(content.x, viewBounds.x),
            y: Math.min(content.y, viewBounds.y),
            w: Math.max(content.x + content.w, viewBounds.x + viewBounds.w) - Math.min(content.x, viewBounds.x),
            h: Math.max(content.y + content.h, viewBounds.y + viewBounds.h) - Math.min(content.y, viewBounds.y),
        }, 40);
        const scale = Math.min(WIDTH / Math.max(world.w, 1), HEIGHT / Math.max(world.h, 1));
        const ox = (WIDTH - world.w * scale) / 2;
        const oy = (HEIGHT - world.h * scale) / 2;
        const px = (x) => ox + (x - world.x) * scale;
        const py = (y) => oy + (y - world.y) * scale;
        for (const el of elements) {
            const b = (0, geometry_1.rotatedBounds)(el);
            ctx.fillStyle = el.type === 'frame' ? 'transparent' : el.fill !== 'transparent' && el.fillStyle !== 'none' ? el.fill : el.stroke;
            ctx.globalAlpha = (0, types_1.isLinear)(el) ? 0.75 : 0.85;
            const w = Math.max(1.5, b.w * scale);
            const h = Math.max(1.5, b.h * scale);
            if ((0, types_1.isLinear)(el)) {
                ctx.strokeStyle = el.stroke;
                ctx.lineWidth = 1;
                ctx.strokeRect(px(b.x), py(b.y), w, h);
            }
            else {
                ctx.fillRect(px(b.x), py(b.y), w, h);
            }
        }
        ctx.globalAlpha = 1;
        ctx.strokeStyle = palette.selection;
        ctx.lineWidth = 1.5;
        ctx.strokeRect(px(viewBounds.x) + 0.5, py(viewBounds.y) + 0.5, viewBounds.w * scale, viewBounds.h * scale);
        return { world, scale, ox, oy };
    }, []);
    (0, react_1.useEffect)(() => {
        let frame = 0;
        let dirty = true;
        const loop = () => {
            frame = requestAnimationFrame(loop);
            if (!dirty)
                return;
            dirty = false;
            draw();
        };
        frame = requestAnimationFrame(loop);
        const unsubscribe = store_1.useSketch.subscribe(() => {
            dirty = true;
        });
        return () => {
            cancelAnimationFrame(frame);
            unsubscribe();
        };
    }, [draw]);
    const goTo = (0, react_1.useCallback)((clientX, clientY) => {
        const canvas = ref.current;
        const info = draw();
        if (!canvas || !info)
            return;
        const rect = canvas.getBoundingClientRect();
        const x = info.world.x + (clientX - rect.left - info.ox) / info.scale;
        const y = info.world.y + (clientY - rect.top - info.oy) / info.scale;
        const state = store_1.useSketch.getState();
        const view = (0, view_1.canvasSize)();
        state.setViewport({
            zoom: state.viewport.zoom,
            scrollX: x - view.width / 2 / state.viewport.zoom,
            scrollY: y - view.height / 2 / state.viewport.zoom,
        });
    }, [draw]);
    return ((0, jsx_runtime_1.jsx)("div", { className: "sk-island sk-minimap", children: (0, jsx_runtime_1.jsx)("canvas", { ref: ref, style: { width: WIDTH, height: HEIGHT }, "aria-label": "Minimap - click to move the view", role: "img", onPointerDown: (e) => {
                dragging.current = true;
                e.target.setPointerCapture(e.pointerId);
                goTo(e.clientX, e.clientY);
            }, onPointerMove: (e) => dragging.current && goTo(e.clientX, e.clientY), onPointerUp: (e) => {
                dragging.current = false;
                e.target.releasePointerCapture?.(e.pointerId);
            } }) }));
};
exports.Minimap = Minimap;

  };
  modules["src/ui/Hints.tsx"] = function (module, exports, require) {
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CorruptNotice = exports.EmptyHint = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// Two things that appear only when they should: the hint on a board with
// nothing on it, and the notice that replaces the canvas when a file on
// disk cannot be read. Neither is ever a blank white window.
const react_1 = __importDefault(require("react"));
const icons_1 = require("./icons");
const store_1 = require("../state/store");
const EmptyHint = () => {
    const count = (0, store_1.useSketch)((s) => s.elements.length);
    const ready = (0, store_1.useSketch)((s) => s.ready);
    const setTool = (0, store_1.useSketch)((s) => s.setTool);
    const setDialog = (0, store_1.useSketch)((s) => s.setDialog);
    if (!ready || count > 0)
        return null;
    return ((0, jsx_runtime_1.jsxs)("div", { className: "sk-firstrun", "aria-live": "polite", children: [(0, jsx_runtime_1.jsx)("h2", { children: "An empty board" }), (0, jsx_runtime_1.jsxs)("ul", { children: [(0, jsx_runtime_1.jsxs)("li", { children: [(0, jsx_runtime_1.jsx)("kbd", { children: "R" }), " ", (0, jsx_runtime_1.jsx)("kbd", { children: "O" }), " ", (0, jsx_runtime_1.jsx)("kbd", { children: "D" }), " draw a rectangle, an ellipse, a diamond"] }), (0, jsx_runtime_1.jsxs)("li", { children: [(0, jsx_runtime_1.jsx)("kbd", { children: "A" }), " drag from one shape to another - the arrow stays attached"] }), (0, jsx_runtime_1.jsxs)("li", { children: ["Double-click anywhere to type, or ", (0, jsx_runtime_1.jsx)("kbd", { children: "/" }), " for ready-made shapes"] }), (0, jsx_runtime_1.jsxs)("li", { children: [(0, jsx_runtime_1.jsx)("kbd", { children: "?" }), " shows every shortcut"] })] }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-firstrun-actions", children: [(0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-btn sk-btn-primary", onClick: () => setTool('rect'), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "rect", size: 16 }), " Draw a rectangle"] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-btn", onClick: () => setDialog('slash', true), children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "library", size: 16 }), " Shape library"] })] })] }));
};
exports.EmptyHint = EmptyHint;
const CorruptNotice = () => {
    const corrupt = (0, store_1.useSketch)((s) => s.corrupt);
    const store = store_1.useSketch;
    if (!corrupt)
        return null;
    return ((0, jsx_runtime_1.jsx)("div", { className: "sk-corrupt", role: "alert", children: (0, jsx_runtime_1.jsxs)("div", { className: "sk-corrupt-card", children: [(0, jsx_runtime_1.jsxs)("h2", { children: [corrupt.name, " could not be opened"] }), (0, jsx_runtime_1.jsx)("p", { children: corrupt.error }), corrupt.raw ? ((0, jsx_runtime_1.jsxs)("pre", { className: "sk-corrupt-raw", children: [corrupt.raw.slice(0, 400), corrupt.raw.length > 400 ? '…' : ''] })) : null, (0, jsx_runtime_1.jsx)("p", { className: "sk-hint", children: "The file on disk has not been touched, and nothing will be written over it." }), (0, jsx_runtime_1.jsxs)("div", { className: "sk-firstrun-actions", children: [(0, jsx_runtime_1.jsx)("button", { type: "button", className: "sk-btn sk-btn-primary", onClick: () => void store.getState().openCorruptAsCopy(), children: "Start a fresh copy" }), (0, jsx_runtime_1.jsx)("button", { type: "button", className: "sk-btn", onClick: () => store.getState().setDialog('files', true), children: "Open another board" })] })] }) }));
};
exports.CorruptNotice = CorruptNotice;

  };
  modules["src/state/store.ts"] = function (module, exports, require) {
"use strict";
// The app's single source of truth. One zustand store holds the board,
// the selection, the viewport, the undo stack and the file it came from;
// every action that changes elements goes through `commit`, which is the
// only place that records history, re-runs arrow bindings and schedules
// the autosave. The canvas subscribes imperatively and redraws in a
// requestAnimationFrame; React components subscribe with selectors, so a
// drag repaints the canvas without re-rendering the toolbar.
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.documentPathOf = exports.rotatedBoundsOf = exports.canRedoNow = exports.canUndoNow = exports.elementById = exports.selectionBoundsOf = exports.useSketch = exports.defaultStyle = void 0;
exports.cloneElements = cloneElements;
exports.elementFromStyle = elementFromStyle;
const zustand_1 = require("zustand");
const sdk_1 = __importDefault(require("@kumin/sdk"));
const document_1 = require("../lib/document");
const history_1 = require("../lib/history");
const palette_1 = require("../lib/palette");
const geometry_1 = require("../lib/geometry");
const binding_1 = require("../lib/binding");
const align_1 = require("../lib/align");
const library_1 = require("../lib/library");
const files_1 = require("./files");
const RECENT_KEY = 'recent';
const STYLE_KEY = 'style';
const PREFS_KEY = 'prefs';
const defaultStyle = (palette) => ({
    stroke: palette.defaultStroke,
    fill: 'transparent',
    fillStyle: 'none',
    strokeWidth: 3,
    strokeStyle: 'solid',
    opacity: 1,
    roundness: 0.12,
    fontSize: 20,
    fontFamily: 'sans',
    textAlign: 'center',
    textColor: palette.defaultText,
    edge: 'straight',
    startArrow: 'none',
    endArrow: 'arrow',
});
exports.defaultStyle = defaultStyle;
const snapshotOf = (s) => ({ elements: s.elements, selectedIds: s.selectedIds });
/** The clipboard lives outside the store: it survives switching boards,
 *  and copying should never be an undo step. */
let clipboard = [];
let saveTimer = null;
let savingPromise = null;
const stylePatchFor = (style, type) => ({
    stroke: style.stroke,
    fill: style.fill,
    fillStyle: style.fillStyle,
    strokeWidth: style.strokeWidth,
    strokeStyle: style.strokeStyle,
    opacity: style.opacity,
    roundness: style.roundness,
    fontSize: style.fontSize,
    fontFamily: style.fontFamily,
    textAlign: type === 'text' ? 'left' : style.textAlign,
    textColor: style.textColor,
    edge: style.edge,
    startArrow: type === 'arrow' ? style.startArrow : 'none',
    endArrow: type === 'arrow' ? style.endArrow : 'none',
});
exports.useSketch = (0, zustand_1.create)((set, get) => ({
    ready: false,
    isDark: false,
    palette: (0, palette_1.paletteFor)(false),
    name: 'Untitled',
    path: null,
    background: 'grid',
    createdAt: new Date().toISOString(),
    elements: [],
    selectedIds: [],
    tool: 'select',
    keepTool: false,
    viewport: { scrollX: -400, scrollY: -300, zoom: 1 },
    history: (0, history_1.createHistory)({ elements: [], selectedIds: [] }),
    style: (0, exports.defaultStyle)((0, palette_1.paletteFor)(false)),
    editingId: null,
    editRequest: null,
    saveState: 'saved',
    saveError: null,
    files: [],
    recent: [],
    corrupt: null,
    dialogs: { files: false, export: false, shortcuts: false, slash: false, minimap: true },
    snapEnabled: true,
    status: null,
    setDark(isDark) {
        const state = get();
        if (state.isDark === isDark && state.ready)
            return;
        const palette = (0, palette_1.paletteFor)(isDark);
        // A board drawn in one theme stays legible in the other: the curated
        // palette entries map across, custom colours are left alone.
        const elements = state.elements.map((el) => {
            const stroke = (0, palette_1.translateColor)(el.stroke, isDark);
            const fill = (0, palette_1.translateColor)(el.fill, isDark);
            const textColor = (0, palette_1.translateColor)(el.textColor, isDark);
            return stroke === el.stroke && fill === el.fill && textColor === el.textColor ? el : { ...el, stroke, fill, textColor };
        });
        set({
            isDark,
            palette,
            elements,
            style: {
                ...state.style,
                stroke: (0, palette_1.translateColor)(state.style.stroke, isDark),
                fill: (0, palette_1.translateColor)(state.style.fill, isDark),
                textColor: (0, palette_1.translateColor)(state.style.textColor, isDark),
            },
        });
    },
    setTool(tool, keep = false) {
        set({ tool, keepTool: keep, history: (0, history_1.seal)(get().history) });
        if (tool !== 'select' && get().editingId)
            set({ editingId: null });
    },
    setViewport(viewport) {
        set({ viewport });
    },
    zoomTo(zoom, at, size) {
        const { viewport } = get();
        const point = at ?? [size ? size.width / 2 : 400, size ? size.height / 2 : 300];
        set({ viewport: (0, geometry_1.zoomAt)(viewport, zoom, point) });
    },
    zoomToFit(size, onlySelection = false) {
        const { elements, selectedIds } = get();
        const target = onlySelection && selectedIds.length ? elements.filter((el) => selectedIds.includes(el.id)) : elements;
        if (!target.length) {
            set({ viewport: { scrollX: -size.width / 2, scrollY: -size.height / 2, zoom: 1 } });
            return;
        }
        set({ viewport: (0, geometry_1.fitBounds)((0, geometry_1.boundsOfElements)(target), size.width, size.height) });
    },
    setSelection(ids) {
        const { elements } = get();
        const expanded = Array.from((0, align_1.expandSelection)(elements, new Set(ids)));
        set({ selectedIds: expanded });
    },
    selectAll() {
        set({ selectedIds: get().elements.filter((el) => !el.locked).map((el) => el.id) });
    },
    commit(elements, opts = {}) {
        const state = get();
        const pruned = (0, binding_1.pruneBindings)(elements);
        const next = (0, binding_1.refreshBindings)(pruned);
        const selectedIds = opts.selectedIds ?? state.selectedIds.filter((id) => next.some((el) => el.id === id));
        const history = (0, history_1.record)(state.history, { elements: next, selectedIds }, { label: opts.label ?? 'Edit', coalesceKey: opts.coalesce ?? null });
        set({ elements: next, selectedIds, history });
        if (!opts.quiet)
            scheduleSave(get, set);
    },
    patchSelected(patch, opts = {}) {
        const { elements, selectedIds } = get();
        if (!selectedIds.length)
            return;
        const ids = new Set(selectedIds);
        get().commit(elements.map((el) => (ids.has(el.id) && !el.locked ? { ...el, ...patch } : el)), { label: 'Style', ...opts });
    },
    setStyle(patch) {
        const style = { ...get().style, ...patch };
        set({ style });
        sdk_1.default.storage.set(STYLE_KEY, style);
        const { selectedIds } = get();
        if (!selectedIds.length)
            return;
        // Every ElementStyle key is also an element field, so the patch
        // applies to the selection unchanged.
        get().patchSelected(patch, { label: 'Style', coalesce: 'style' });
    },
    addElements(elements, opts = {}) {
        const state = get();
        const next = [...state.elements, ...elements];
        state.commit(next, { label: opts.label ?? 'Add', selectedIds: opts.select === false ? state.selectedIds : elements.map((el) => el.id) });
    },
    deleteSelected() {
        const { elements, selectedIds } = get();
        if (!selectedIds.length)
            return;
        const ids = new Set(selectedIds);
        const kept = elements.filter((el) => !ids.has(el.id) || el.locked);
        if (kept.length === elements.length)
            return;
        get().commit(kept, { label: 'Delete', selectedIds: [] });
    },
    duplicateSelected() {
        const { elements, selectedIds } = get();
        if (!selectedIds.length)
            return;
        const ids = new Set(selectedIds);
        const copies = cloneElements(elements.filter((el) => ids.has(el.id)), 16, 16);
        get().commit([...elements, ...copies], { label: 'Duplicate', selectedIds: copies.map((el) => el.id) });
    },
    copySelection(cut = false) {
        const { elements, selectedIds } = get();
        const ids = new Set(selectedIds);
        clipboard = elements.filter((el) => ids.has(el.id)).map((el) => ({ ...el }));
        if (!clipboard.length)
            return;
        get().setStatus(`${clipboard.length} element${clipboard.length === 1 ? '' : 's'} ${cut ? 'cut' : 'copied'}`);
        if (cut)
            get().deleteSelected();
    },
    paste(at) {
        if (!clipboard.length)
            return;
        const { elements } = get();
        const box = (0, geometry_1.boundsOfElements)(clipboard);
        const dx = at ? at[0] - (box.x + box.w / 2) : 24;
        const dy = at ? at[1] - (box.y + box.h / 2) : 24;
        const copies = cloneElements(clipboard, dx, dy);
        get().commit([...elements, ...copies], { label: 'Paste', selectedIds: copies.map((el) => el.id) });
    },
    group() {
        const { elements, selectedIds } = get();
        if (selectedIds.length < 2)
            return;
        const groupId = (0, document_1.newId)('g');
        const ids = new Set(selectedIds);
        get().commit(elements.map((el) => (ids.has(el.id) ? { ...el, groupId } : el)), { label: 'Group' });
    },
    ungroup() {
        const { elements, selectedIds } = get();
        const ids = new Set(selectedIds);
        let changed = false;
        const next = elements.map((el) => {
            if (!ids.has(el.id) || !el.groupId)
                return el;
            changed = true;
            return { ...el, groupId: null };
        });
        if (changed)
            get().commit(next, { label: 'Ungroup' });
    },
    order(mode) {
        const { elements, selectedIds } = get();
        if (!selectedIds.length)
            return;
        get().commit((0, align_1.reorder)(elements, new Set(selectedIds), mode), { label: 'Reorder' });
    },
    align(mode) {
        const { elements, selectedIds } = get();
        if (selectedIds.length < 2)
            return;
        get().commit((0, align_1.alignElements)(elements, new Set(selectedIds), mode), { label: 'Align' });
    },
    distribute(axis) {
        const { elements, selectedIds } = get();
        if (selectedIds.length < 3)
            return;
        get().commit((0, align_1.distributeElements)(elements, new Set(selectedIds), axis), { label: 'Distribute' });
    },
    flip(axis) {
        const { elements, selectedIds } = get();
        if (!selectedIds.length)
            return;
        get().commit((0, align_1.flipElements)(elements, new Set(selectedIds), axis), { label: 'Flip' });
    },
    toggleLock() {
        const { elements, selectedIds } = get();
        if (!selectedIds.length)
            return;
        const ids = new Set(selectedIds);
        const anyUnlocked = elements.some((el) => ids.has(el.id) && !el.locked);
        get().commit(elements.map((el) => (ids.has(el.id) ? { ...el, locked: anyUnlocked } : el)), { label: anyUnlocked ? 'Lock' : 'Unlock' });
    },
    nudge(dx, dy) {
        const { elements, selectedIds } = get();
        if (!selectedIds.length)
            return;
        const ids = new Set(selectedIds);
        get().commit(elements.map((el) => (ids.has(el.id) && !el.locked ? { ...el, x: el.x + dx, y: el.y + dy } : el)), { label: 'Move', coalesce: 'nudge' });
    },
    undo() {
        const history = (0, history_1.undo)(get().history);
        const { elements, selectedIds } = history.present.snapshot;
        set({ history, elements, selectedIds, editingId: null });
        scheduleSave(get, set);
    },
    redo() {
        const history = (0, history_1.redo)(get().history);
        const { elements, selectedIds } = history.present.snapshot;
        set({ history, elements, selectedIds, editingId: null });
        scheduleSave(get, set);
    },
    seal() {
        set({ history: (0, history_1.seal)(get().history) });
    },
    setEditing(id) {
        set({ editingId: id, editRequest: null, history: (0, history_1.seal)(get().history) });
    },
    requestEdit(id) {
        set({ editRequest: id });
    },
    setBackground(background) {
        set({ background });
        scheduleSave(get, set);
    },
    toggleSnap() {
        const snapEnabled = !get().snapEnabled;
        set({ snapEnabled });
        sdk_1.default.storage.set(PREFS_KEY, { snapEnabled, minimap: get().dialogs.minimap });
    },
    setDialog(key, open) {
        set({ dialogs: { ...get().dialogs, [key]: open } });
        if (key === 'minimap')
            sdk_1.default.storage.set(PREFS_KEY, { snapEnabled: get().snapEnabled, minimap: open });
    },
    setStatus(message) {
        set({ status: message });
    },
    insertLibrary(itemId, center) {
        const { style } = get();
        const elements = (0, library_1.insertLibraryItem)(itemId, center, {
            stroke: style.stroke,
            fill: style.fill === 'transparent' ? (get().isDark ? '#23374f' : '#d6e6fb') : style.fill,
            fillStyle: 'solid',
            textColor: style.textColor,
            fontSize: style.fontSize,
        });
        if (elements.length)
            get().addElements(elements, { label: 'Insert shape' });
    },
    /* ------------------------------------------------------------ files */
    async boot() {
        const prefs = sdk_1.default.storage.get(PREFS_KEY);
        const savedStyle = sdk_1.default.storage.get(STYLE_KEY);
        const recent = sdk_1.default.storage.get(RECENT_KEY) ?? [];
        set({
            recent,
            snapEnabled: prefs?.snapEnabled ?? true,
            dialogs: { ...get().dialogs, minimap: prefs?.minimap ?? true },
            ...(savedStyle ? { style: { ...(0, exports.defaultStyle)(get().palette), ...savedStyle } } : {}),
        });
        await (0, files_1.ensureFolder)();
        const files = await (0, files_1.listDocuments)();
        set({ files });
        const target = recent.find((p) => files.some((f) => f.path === p)) ?? files[0]?.path;
        if (target) {
            await get().openDocument(target);
        }
        else {
            await get().newDocument('Untitled');
        }
        set({ ready: true });
    },
    async refreshFiles() {
        set({ files: await (0, files_1.listDocuments)() });
    },
    async newDocument(name) {
        await get().flush();
        const base = (0, document_1.sanitizeName)(name ?? 'Untitled');
        const unique = await (0, files_1.freeName)(base);
        const doc = (0, document_1.createDocument)(unique);
        const path = (0, files_1.pathFor)(unique);
        loadIntoStore(set, get, doc, path);
        await (0, files_1.writeDocumentFile)(path, doc);
        rememberRecent(get, set, path);
        await get().refreshFiles();
        set({ saveState: 'saved' });
    },
    async openDocument(path) {
        await get().flush();
        const result = await (0, files_1.readDocumentFile)(path);
        if (!result.ok) {
            set({
                corrupt: { path, name: path.split('/').pop() ?? path, error: result.error, raw: result.raw },
                ready: true,
            });
            return;
        }
        loadIntoStore(set, get, result.doc, path);
        rememberRecent(get, set, path);
        set({
            saveState: 'saved',
            corrupt: null,
            status: result.dropped ? `Opened - ${result.dropped} unreadable element${result.dropped === 1 ? '' : 's'} were dropped` : null,
        });
        await get().refreshFiles();
    },
    async renameDocument(name) {
        const state = get();
        const clean = (0, document_1.sanitizeName)(name);
        if (!clean || clean === state.name)
            return;
        const unique = await (0, files_1.freeName)(clean);
        const from = state.path;
        const to = from ? await (0, files_1.renameDocumentFile)(from, unique) : (0, files_1.pathFor)(unique);
        set({ name: unique, path: to });
        await (0, files_1.writeDocumentFile)(to, currentDocument(get()));
        rememberRecent(get, set, to);
        await get().refreshFiles();
    },
    async duplicateDocument() {
        await get().flush();
        const state = get();
        const unique = await (0, files_1.freeName)(`${state.name} copy`);
        const path = (0, files_1.pathFor)(unique);
        const doc = { ...currentDocument(state), name: unique };
        await (0, files_1.writeDocumentFile)(path, doc);
        loadIntoStore(set, get, doc, path);
        rememberRecent(get, set, path);
        await get().refreshFiles();
        set({ saveState: 'saved', status: `Duplicated as "${unique}"` });
    },
    async deleteDocument(path) {
        await (0, files_1.deleteDocumentFile)(path);
        const recent = get().recent.filter((p) => p !== path);
        sdk_1.default.storage.set(RECENT_KEY, recent);
        set({ recent });
        await get().refreshFiles();
        if (get().path === path) {
            const next = get().files[0]?.path;
            if (next)
                await get().openDocument(next);
            else
                await get().newDocument('Untitled');
        }
    },
    async importDocument(text, name) {
        const parsed = (0, document_1.parseDocument)(text, name);
        if (!parsed.ok) {
            set({ status: parsed.error });
            return;
        }
        const unique = await (0, files_1.freeName)(parsed.doc.name || name);
        const path = (0, files_1.pathFor)(unique);
        const doc = { ...parsed.doc, name: unique };
        await (0, files_1.writeDocumentFile)(path, doc);
        loadIntoStore(set, get, doc, path);
        rememberRecent(get, set, path);
        await get().refreshFiles();
        set({ saveState: 'saved', status: `Imported "${unique}"` });
    },
    async openCorruptAsCopy() {
        const corrupt = get().corrupt;
        if (!corrupt)
            return;
        const unique = await (0, files_1.freeName)('Recovered board');
        const doc = (0, document_1.createDocument)(unique);
        const path = (0, files_1.pathFor)(unique);
        await (0, files_1.writeDocumentFile)(path, doc);
        loadIntoStore(set, get, doc, path);
        set({ corrupt: null, saveState: 'saved', status: `The unreadable file was left untouched; "${unique}" is a fresh copy` });
        await get().refreshFiles();
    },
    async save() {
        const state = get();
        if (!state.path)
            return;
        set({ saveState: 'saving' });
        try {
            await (0, files_1.writeDocumentFile)(state.path, currentDocument(get()));
            set({ saveState: get().saveState === 'saving' ? 'saved' : get().saveState, saveError: null });
        }
        catch (err) {
            set({ saveState: 'error', saveError: err instanceof Error ? err.message : String(err) });
        }
    },
    async flush() {
        if (saveTimer) {
            clearTimeout(saveTimer);
            saveTimer = null;
            savingPromise = get().save();
        }
        if (savingPromise)
            await savingPromise;
    },
}));
/* ---------------------------------------------------------- helpers */
function currentDocument(state) {
    return {
        type: 'sketch',
        version: 1,
        name: state.name,
        createdAt: state.createdAt,
        updatedAt: new Date().toISOString(),
        background: state.background,
        view: state.viewport,
        elements: state.elements,
    };
}
function loadIntoStore(set, get, doc, path) {
    const elements = (0, binding_1.refreshBindings)(doc.elements);
    set({
        name: doc.name,
        path,
        background: doc.background,
        createdAt: doc.createdAt,
        elements,
        selectedIds: [],
        editingId: null,
        viewport: doc.view,
        history: (0, history_1.createHistory)({ elements, selectedIds: [] }),
        corrupt: null,
    });
}
function rememberRecent(get, set, path) {
    const recent = [path, ...get().recent.filter((p) => p !== path)].slice(0, 8);
    sdk_1.default.storage.set(RECENT_KEY, recent);
    set({ recent });
}
const SAVE_DELAY = 600;
function scheduleSave(get, set) {
    if (!get().path)
        return;
    set({ saveState: 'dirty' });
    if (saveTimer)
        clearTimeout(saveTimer);
    saveTimer = setTimeout(() => {
        saveTimer = null;
        savingPromise = get().save();
    }, SAVE_DELAY);
}
/** Fresh ids for a copy, with group ids and arrow bindings rewritten to
 *  point at the copies - so pasting a bound diagram pastes a diagram, not
 *  a pile of arrows still attached to the originals. */
function cloneElements(elements, dx, dy) {
    const idMap = new Map();
    const groupMap = new Map();
    for (const el of elements) {
        idMap.set(el.id, (0, document_1.newId)());
        if (el.groupId && !groupMap.has(el.groupId))
            groupMap.set(el.groupId, (0, document_1.newId)('g'));
    }
    return elements.map((el) => ({
        ...el,
        id: idMap.get(el.id),
        x: el.x + dx,
        y: el.y + dy,
        groupId: el.groupId ? groupMap.get(el.groupId) : null,
        startBinding: el.startBinding && idMap.has(el.startBinding.elementId) ? { ...el.startBinding, elementId: idMap.get(el.startBinding.elementId) } : null,
        endBinding: el.endBinding && idMap.has(el.endBinding.elementId) ? { ...el.endBinding, elementId: idMap.get(el.endBinding.elementId) } : null,
    }));
}
/** A new element carrying the current style - what every draw tool makes. */
function elementFromStyle(type, props, style) {
    const base = stylePatchFor(style, type);
    if (type === 'sticky') {
        return (0, document_1.createElement)(type, { ...base, fill: props.fill ?? base.fill, fillStyle: 'solid', stroke: 'transparent', textAlign: 'center', ...props });
    }
    if (type === 'text') {
        return (0, document_1.createElement)(type, { ...base, stroke: 'transparent', fill: 'transparent', fillStyle: 'none', textAlign: 'left', ...props });
    }
    if (type === 'frame') {
        return (0, document_1.createElement)(type, { ...base, fill: 'transparent', fillStyle: 'none', fontSize: 16, textAlign: 'left', ...props });
    }
    return (0, document_1.createElement)(type, { ...base, ...props });
}
const selectionBoundsOf = (elements, ids) => {
    const set = new Set(ids);
    return (0, geometry_1.boundsOfElements)(elements.filter((el) => set.has(el.id)));
};
exports.selectionBoundsOf = selectionBoundsOf;
const elementById = (elements, id) => elements.find((el) => el.id === id);
exports.elementById = elementById;
const canUndoNow = (s) => (0, history_1.canUndo)(s.history);
exports.canUndoNow = canUndoNow;
const canRedoNow = (s) => (0, history_1.canRedo)(s.history);
exports.canRedoNow = canRedoNow;
exports.rotatedBoundsOf = geometry_1.rotatedBounds;
exports.documentPathOf = document_1.documentPath;

  };
  modules["src/state/view.ts"] = function (module, exports, require) {
"use strict";
// A single mutable handle onto the live canvas: its size in CSS pixels
// and the scene point at its centre. The zoom controls, the shape
// library and the agent tools all need those two facts and none of them
// should re-render when the window is resized, so they live in a ref
// rather than in the store.
Object.defineProperty(exports, "__esModule", { value: true });
exports.sceneCenter = exports.canvasSize = exports.viewRef = void 0;
exports.viewRef = { current: null };
const canvasSize = () => exports.viewRef.current?.size() ?? { width: 1000, height: 700 };
exports.canvasSize = canvasSize;
const sceneCenter = () => exports.viewRef.current?.centerScene() ?? [0, 0];
exports.sceneCenter = sceneCenter;

  };
  modules["src/lib/layout.ts"] = function (module, exports, require) {
"use strict";
// Layered ("Sugiyama") diagram layout: a graph description in, a tidy
// flowchart out. Four phases, each its own exported function so each can
// be tested on its own:
//
//   1. layerNodes       cycles broken by DFS, then longest-path layering
//   2. orderLayers      barycentre sweeps, keeping the best crossing count
//   3. assignPositions  median straightening with overlap resolution
//   4. buildDiagram     boxes and bound arrows as real Sketch elements
//
// Pure: no DOM, no canvas. Text is sized with the estimator in text.ts,
// which is what lets `sketch_create_diagram` lay a board out with nothing
// on screen at all.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_DIAGRAM_STYLE = exports.shapeToType = void 0;
exports.sizeNode = sizeNode;
exports.layerNodes = layerNodes;
exports.countCrossings = countCrossings;
exports.orderLayers = orderLayers;
exports.assignPositions = assignPositions;
exports.layoutGraph = layoutGraph;
exports.buildDiagram = buildDiagram;
const document_1 = require("./document");
const binding_1 = require("./binding");
const text_1 = require("./text");
const SHAPE_ALIASES = {
    rect: 'rect',
    rectangle: 'rect',
    box: 'rect',
    process: 'rect',
    ellipse: 'ellipse',
    oval: 'ellipse',
    circle: 'ellipse',
    start: 'ellipse',
    end: 'ellipse',
    terminator: 'ellipse',
    diamond: 'diamond',
    decision: 'diamond',
    sticky: 'sticky',
    note: 'sticky',
};
const shapeToType = (shape) => SHAPE_ALIASES[(shape ?? 'rect').toLowerCase()] ?? 'rect';
exports.shapeToType = shapeToType;
const DEFAULTS = {
    direction: 'TB',
    nodeGap: 48,
    layerGap: 92,
    fontSize: 20,
    maxNodeWidth: 240,
    minNodeWidth: 120,
    origin: [0, 0],
};
/* --------------------------------------------------------- 0. sizing */
function sizeNode(node, opts = {}) {
    const o = { ...DEFAULTS, ...opts };
    const type = (0, exports.shapeToType)(node.shape);
    const label = node.label ?? '';
    // A diamond wastes its corners, so the same text needs a wider box.
    const slack = type === 'diamond' ? 1.7 : 1;
    const padding = type === 'diamond' ? 28 : 18;
    const contentMax = Math.max(40, o.maxNodeWidth - padding * 2);
    const measure = (0, text_1.estimateMeasure)(o.fontSize);
    const single = (0, text_1.estimateTextWidth)(label, o.fontSize);
    const lines = single <= contentMax ? [label] : (0, text_1.wrapText)(label, contentMax, measure);
    let textWidth = 0;
    for (const line of lines)
        textWidth = Math.max(textWidth, measure(line));
    const w = Math.max(o.minNodeWidth, Math.round((textWidth + padding * 2) * slack));
    const h = Math.max(type === 'diamond' ? 88 : 60, Math.round(lines.length * (0, text_1.lineHeightFor)(o.fontSize) * slack + padding * 2));
    return { ...node, type, w, h, lines };
}
/** Longest-path layering after breaking cycles with a DFS: every edge
 *  points from a lower layer to a higher one. */
function layerNodes(nodes, edges) {
    const ids = nodes.map((n) => n.id);
    const known = new Set(ids);
    const clean = edges.filter((e) => known.has(e.from) && known.has(e.to) && e.from !== e.to);
    const out = new Map();
    for (const id of ids)
        out.set(id, []);
    for (const e of clean)
        out.get(e.from).push(e);
    // Break cycles: any edge back to a node still on the DFS stack.
    const state = new Map();
    const reversed = [];
    const reversedKeys = new Set();
    const key = (e) => `${e.from} ${e.to}`;
    const visit = (id) => {
        state.set(id, 1);
        for (const e of out.get(id) ?? []) {
            const s = state.get(e.to) ?? 0;
            if (s === 1) {
                reversed.push(e);
                reversedKeys.add(key(e));
            }
            else if (s === 0) {
                visit(e.to);
            }
        }
        state.set(id, 2);
    };
    for (const id of ids)
        if ((state.get(id) ?? 0) === 0)
            visit(id);
    const acyclic = clean.map((e) => (reversedKeys.has(key(e)) ? { ...e, from: e.to, to: e.from } : e));
    // Longest path: layer(v) = 1 + max(layer(u)) over incoming edges.
    const incoming = new Map();
    for (const id of ids)
        incoming.set(id, []);
    for (const e of acyclic)
        incoming.get(e.to).push(e.from);
    const layerOf = new Map();
    const resolving = new Set();
    const layerFor = (id) => {
        const cached = layerOf.get(id);
        if (cached !== undefined)
            return cached;
        if (resolving.has(id))
            return 0;
        resolving.add(id);
        let best = 0;
        for (const from of incoming.get(id) ?? [])
            best = Math.max(best, layerFor(from) + 1);
        resolving.delete(id);
        layerOf.set(id, best);
        return best;
    };
    for (const id of ids)
        layerFor(id);
    const depth = Math.max(0, ...Array.from(layerOf.values())) + 1;
    const layers = Array.from({ length: depth }, () => []);
    for (const id of ids)
        layers[layerOf.get(id)].push(id);
    return { layerOf, layers, reversed, acyclic };
}
/* ------------------------------------------------------- 2. ordering */
/** Number of edge crossings between consecutive layers, counted pairwise.
 *  Small graphs only - which is what a hand-written diagram spec is. */
function countCrossings(layers, edges, layerOf) {
    let total = 0;
    for (let i = 0; i < layers.length - 1; i++) {
        const upper = new Map(layers[i].map((id, idx) => [id, idx]));
        const lower = new Map(layers[i + 1].map((id, idx) => [id, idx]));
        const pairs = [];
        for (const e of edges) {
            if (layerOf.get(e.from) === i && layerOf.get(e.to) === i + 1) {
                const a = upper.get(e.from);
                const b = lower.get(e.to);
                if (a !== undefined && b !== undefined)
                    pairs.push([a, b]);
            }
        }
        for (let a = 0; a < pairs.length; a++) {
            for (let b = a + 1; b < pairs.length; b++) {
                if ((pairs[a][0] - pairs[b][0]) * (pairs[a][1] - pairs[b][1]) < 0)
                    total++;
            }
        }
    }
    return total;
}
/** Barycentre sweeps, down then up, keeping whichever ordering crossed
 *  least. Deterministic: ties keep the previous order. */
function orderLayers(layers, edges, layerOf, sweeps = 8) {
    let best = layers.map((l) => l.slice());
    let bestScore = countCrossings(best, edges, layerOf);
    let current = best.map((l) => l.slice());
    const neighbours = (id, dir) => edges.filter((e) => (dir === 'up' ? e.to === id : e.from === id)).map((e) => (dir === 'up' ? e.from : e.to));
    for (let sweep = 0; sweep < sweeps; sweep++) {
        const downward = sweep % 2 === 0;
        const range = downward ? current.map((_, i) => i).slice(1) : current.map((_, i) => i).slice(0, -1).reverse();
        for (const i of range) {
            const refIndex = new Map((downward ? current[i - 1] : current[i + 1]).map((id, idx) => [id, idx]));
            const keyed = current[i].map((id, idx) => {
                const ns = neighbours(id, downward ? 'up' : 'down').map((n) => refIndex.get(n)).filter((v) => v !== undefined);
                const bary = ns.length ? ns.reduce((a, b) => a + b, 0) / ns.length : idx;
                return { id, bary, idx };
            });
            keyed.sort((a, b) => (a.bary === b.bary ? a.idx - b.idx : a.bary - b.bary));
            current[i] = keyed.map((k) => k.id);
        }
        const score = countCrossings(current, edges, layerOf);
        if (score < bestScore) {
            bestScore = score;
            best = current.map((l) => l.slice());
        }
        current = best.map((l) => l.slice());
        if (bestScore === 0)
            break;
    }
    return best;
}
/* ------------------------------------------------------ 3. positions */
/** Cross-axis coordinate for each node: nodes are packed inside their
 *  layer, then pulled toward the median of their neighbours and
 *  de-overlapped, which is what makes a chain of boxes line up. */
function assignPositions(layers, sized, edges, opts = {}) {
    const o = { ...DEFAULTS, ...opts };
    const across = (n) => (o.direction === 'TB' ? n.w : n.h);
    const pos = new Map();
    for (const layer of layers) {
        let cursor = 0;
        for (const id of layer) {
            const n = sized.get(id);
            pos.set(id, cursor + across(n) / 2);
            cursor += across(n) + o.nodeGap;
        }
        const width = cursor - o.nodeGap;
        for (const id of layer)
            pos.set(id, pos.get(id) - width / 2);
    }
    const neighboursOf = (id, dir) => edges.filter((e) => (dir === 'up' ? e.to === id : e.from === id)).map((e) => (dir === 'up' ? e.from : e.to));
    for (let pass = 0; pass < 6; pass++) {
        const downward = pass % 2 === 0;
        const order = downward ? layers.map((_, i) => i) : layers.map((_, i) => i).reverse();
        for (const i of order) {
            for (const id of layers[i]) {
                const ns = neighboursOf(id, downward ? 'up' : 'down')
                    .map((n) => pos.get(n))
                    .filter((v) => v !== undefined)
                    .sort((a, b) => a - b);
                if (!ns.length)
                    continue;
                const mid = ns.length % 2 ? ns[(ns.length - 1) / 2] : (ns[ns.length / 2 - 1] + ns[ns.length / 2]) / 2;
                pos.set(id, mid);
            }
            // Resolve overlaps left-to-right then right-to-left so the layer
            // keeps its order and stays centred on the medians it was given.
            const layer = layers[i];
            for (let k = 1; k < layer.length; k++) {
                const prev = sized.get(layer[k - 1]);
                const cur = sized.get(layer[k]);
                const minGap = across(prev) / 2 + across(cur) / 2 + o.nodeGap;
                if (pos.get(layer[k]) - pos.get(layer[k - 1]) < minGap)
                    pos.set(layer[k], pos.get(layer[k - 1]) + minGap);
            }
            for (let k = layer.length - 2; k >= 0; k--) {
                const next = sized.get(layer[k + 1]);
                const cur = sized.get(layer[k]);
                const minGap = across(next) / 2 + across(cur) / 2 + o.nodeGap;
                if (pos.get(layer[k + 1]) - pos.get(layer[k]) < minGap)
                    pos.set(layer[k], pos.get(layer[k + 1]) - minGap);
            }
        }
    }
    return pos;
}
/* ---------------------------------------------------------- 4. layout */
function layoutGraph(spec, opts = {}) {
    const direction = opts.direction ?? spec.direction ?? 'TB';
    const o = { ...DEFAULTS, ...opts, direction };
    const seen = new Set();
    const nodes = spec.nodes.filter((n) => n && typeof n.id === 'string' && n.id && !seen.has(n.id) && seen.add(n.id));
    const sized = new Map(nodes.map((n) => [n.id, sizeNode(n, o)]));
    const known = new Set(nodes.map((n) => n.id));
    const edges = (spec.edges ?? []).filter((e) => e && known.has(e.from) && known.has(e.to));
    const { layers, layerOf, acyclic } = layerNodes(nodes, edges);
    const ordered = orderLayers(layers, acyclic, layerOf);
    const cross = assignPositions(ordered, sized, acyclic, o);
    // Down-axis: stack layers, each centred on the tallest node in it.
    const down = new Map();
    let cursor = 0;
    for (const layer of ordered) {
        let extent = 0;
        for (const id of layer)
            extent = Math.max(extent, direction === 'TB' ? sized.get(id).h : sized.get(id).w);
        for (const id of layer)
            down.set(id, cursor + extent / 2);
        cursor += extent + o.layerGap;
    }
    const placed = [];
    for (const [layerIndex, layer] of ordered.entries()) {
        for (const [order, id] of layer.entries()) {
            const n = sized.get(id);
            const c = cross.get(id) ?? 0;
            const d = down.get(id) ?? 0;
            const cx = direction === 'TB' ? c : d;
            const cy = direction === 'TB' ? d : c;
            placed.push({ ...n, layer: layerIndex, order, x: cx - n.w / 2, y: cy - n.h / 2 });
        }
    }
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    for (const n of placed) {
        minX = Math.min(minX, n.x);
        minY = Math.min(minY, n.y);
        maxX = Math.max(maxX, n.x + n.w);
        maxY = Math.max(maxY, n.y + n.h);
    }
    if (!placed.length) {
        minX = 0;
        minY = 0;
        maxX = 0;
        maxY = 0;
    }
    const [ox, oy] = o.origin;
    for (const n of placed) {
        n.x = Math.round(n.x - minX + ox);
        n.y = Math.round(n.y - minY + oy);
    }
    return {
        nodes: placed,
        edges,
        direction,
        width: maxX - minX,
        height: maxY - minY,
        crossings: countCrossings(ordered, acyclic, layerOf),
    };
}
exports.DEFAULT_DIAGRAM_STYLE = {
    stroke: '#1e1e1e',
    fill: '#d6e6fb',
    fillStyle: 'solid',
    textColor: '#1e1e1e',
    stickyFill: '#fde9a9',
    stickyText: '#413a1c',
    fontSize: 20,
};
/** Lays the graph out and turns it into elements: one shape per node, one
 *  arrow per edge with BOTH ends bound, so dragging a box afterwards
 *  keeps the diagram connected. */
function buildDiagram(spec, style = {}, opts = {}) {
    const s = { ...exports.DEFAULT_DIAGRAM_STYLE, ...style };
    const result = layoutGraph(spec, { fontSize: s.fontSize, ...opts });
    const elements = [];
    const byGraphId = new Map();
    for (const n of result.nodes) {
        const isSticky = n.type === 'sticky';
        const el = (0, document_1.createElement)(n.type, {
            x: n.x,
            y: n.y,
            w: n.w,
            h: n.h,
            text: n.label,
            fontSize: s.fontSize,
            textAlign: 'center',
            stroke: isSticky ? 'transparent' : s.stroke,
            fill: isSticky ? s.stickyFill : s.fill,
            fillStyle: 'solid',
            textColor: isSticky ? s.stickyText : s.textColor,
        });
        elements.push(el);
        byGraphId.set(n.id, el);
    }
    for (const e of result.edges) {
        const from = byGraphId.get(e.from);
        const to = byGraphId.get(e.to);
        if (!from || !to)
            continue;
        const start = (0, binding_1.boundaryPoint)(from, [to.x + to.w / 2, to.y + to.h / 2], 6);
        const end = (0, binding_1.boundaryPoint)(to, [from.x + from.w / 2, from.y + from.h / 2], 6);
        elements.push((0, document_1.createElement)('arrow', {
            x: start[0],
            y: start[1],
            points: [
                [0, 0],
                [end[0] - start[0], end[1] - start[1]],
            ],
            stroke: s.stroke,
            endArrow: 'arrow',
            text: e.label ?? '',
            textColor: s.textColor,
            fontSize: Math.max(12, Math.round(s.fontSize * 0.7)),
            startBinding: { elementId: from.id, gap: 6 },
            endBinding: { elementId: to.id, gap: 6 },
        }));
    }
    return elements;
}

  };
  modules["src/lib/document.ts"] = function (module, exports, require) {
"use strict";
// The file format: what a .sketch.json holds, how it is written, how it
// is read back, and what happens when the bytes on disk are not what this
// version expects. Parsing NEVER throws - it returns a result the UI can
// render as a readable problem, because a whiteboard that shows a blank
// white window when a file is a byte short is worse than useless.
Object.defineProperty(exports, "__esModule", { value: true });
exports.isSketchFile = exports.documentPath = exports.fileNameFor = exports.FOLDER_NAME = exports.FILE_EXTENSION = exports.SCHEMA_VERSION = void 0;
exports.newId = newId;
exports.createElement = createElement;
exports.createDocument = createDocument;
exports.coerceElement = coerceElement;
exports.serializeDocument = serializeDocument;
exports.parseDocument = parseDocument;
exports.sanitizeName = sanitizeName;
exports.nameFromFileName = nameFromFileName;
exports.uniqueName = uniqueName;
exports.describeDocument = describeDocument;
const types_1 = require("./types");
const geometry_1 = require("./geometry");
exports.SCHEMA_VERSION = 1;
exports.FILE_EXTENSION = '.sketch.json';
exports.FOLDER_NAME = 'Sketches';
/* -------------------------------------------------------------- ids */
let idCounter = 0;
/** Short, sortable-enough, collision-free within a document. Not a UUID
 *  on purpose: these end up in every serialised element, and 12 chars
 *  keeps a 2,000-element file readable in a diff. */
function newId(prefix = 'e') {
    idCounter = (idCounter + 1) % 0xffff;
    const time = Date.now().toString(36).slice(-6);
    const rand = Math.floor(Math.random() * 0xffffff).toString(36).padStart(4, '0').slice(-4);
    return `${prefix}${time}${rand}${idCounter.toString(36)}`;
}
/* -------------------------------------------------------- factories */
function createElement(type, props = {}) {
    const el = { ...types_1.DEFAULT_ELEMENT, id: newId(), type, ...props };
    return types_1.LINEAR_TYPES.has(type) && el.points.length ? (0, geometry_1.normalizeLinear)(el) : el;
}
function createDocument(name = 'Untitled', elements = []) {
    const now = new Date().toISOString();
    return {
        type: 'sketch',
        version: exports.SCHEMA_VERSION,
        name,
        createdAt: now,
        updatedAt: now,
        background: 'grid',
        view: { scrollX: -400, scrollY: -300, zoom: 1 },
        elements,
    };
}
/* ------------------------------------------------------ coercion */
const num = (v, fallback) => (typeof v === 'number' && Number.isFinite(v) ? v : fallback);
const str = (v, fallback) => (typeof v === 'string' ? v : fallback);
const bool = (v, fallback) => (typeof v === 'boolean' ? v : fallback);
const oneOf = (v, allowed, fallback) => typeof v === 'string' && allowed.includes(v) ? v : fallback;
const ELEMENT_TYPES = ['rect', 'ellipse', 'diamond', 'line', 'arrow', 'draw', 'text', 'sticky', 'frame'];
const FILL_STYLES = ['none', 'solid', 'hatch'];
const STROKE_STYLES = ['solid', 'dashed', 'dotted'];
const TEXT_ALIGNS = ['left', 'center', 'right'];
const ARROW_HEADS = ['none', 'arrow', 'dot', 'bar'];
const EDGES = ['straight', 'curved', 'elbow'];
const FONTS = ['sans', 'serif', 'mono'];
function coercePoints(v) {
    if (!Array.isArray(v))
        return [];
    const out = [];
    for (const p of v) {
        if (Array.isArray(p) && typeof p[0] === 'number' && typeof p[1] === 'number' && Number.isFinite(p[0]) && Number.isFinite(p[1])) {
            out.push([p[0], p[1]]);
        }
    }
    return out;
}
function coerceBinding(v) {
    if (!v || typeof v !== 'object')
        return null;
    const raw = v;
    if (typeof raw.elementId !== 'string' || !raw.elementId)
        return null;
    return { elementId: raw.elementId, gap: num(raw.gap, 6) };
}
/** Turns anything into a valid element, defaulting every field it cannot
 *  read. Returns null only when there is nothing recognisable at all. */
function coerceElement(raw) {
    if (!raw || typeof raw !== 'object' || Array.isArray(raw))
        return null;
    const r = raw;
    const type = oneOf(r.type, ELEMENT_TYPES, 'rect');
    const el = {
        ...types_1.DEFAULT_ELEMENT,
        id: str(r.id, '') || newId(),
        type,
        x: num(r.x, 0),
        y: num(r.y, 0),
        w: Math.max(0, num(r.w, 0)),
        h: Math.max(0, num(r.h, 0)),
        angle: num(r.angle, 0),
        stroke: str(r.stroke, types_1.DEFAULT_ELEMENT.stroke),
        fill: str(r.fill, types_1.DEFAULT_ELEMENT.fill),
        fillStyle: oneOf(r.fillStyle, FILL_STYLES, types_1.DEFAULT_ELEMENT.fillStyle),
        strokeWidth: Math.max(0.25, num(r.strokeWidth, types_1.DEFAULT_ELEMENT.strokeWidth)),
        strokeStyle: oneOf(r.strokeStyle, STROKE_STYLES, types_1.DEFAULT_ELEMENT.strokeStyle),
        opacity: Math.min(1, Math.max(0.05, num(r.opacity, 1))),
        roundness: Math.min(0.5, Math.max(0, num(r.roundness, types_1.DEFAULT_ELEMENT.roundness))),
        locked: bool(r.locked, false),
        groupId: typeof r.groupId === 'string' && r.groupId ? r.groupId : null,
        text: str(r.text, ''),
        fontSize: Math.max(6, num(r.fontSize, types_1.DEFAULT_ELEMENT.fontSize)),
        fontFamily: oneOf(r.fontFamily, FONTS, 'sans'),
        textAlign: oneOf(r.textAlign, TEXT_ALIGNS, 'center'),
        textColor: str(r.textColor, types_1.DEFAULT_ELEMENT.textColor),
        points: coercePoints(r.points),
        edge: oneOf(r.edge, EDGES, 'straight'),
        startArrow: oneOf(r.startArrow, ARROW_HEADS, 'none'),
        endArrow: oneOf(r.endArrow, ARROW_HEADS, 'none'),
        startBinding: coerceBinding(r.startBinding),
        endBinding: coerceBinding(r.endBinding),
        name: str(r.name, ''),
    };
    if (types_1.LINEAR_TYPES.has(el.type)) {
        if (el.points.length < 2 && el.type !== 'draw')
            return null;
        if (!el.points.length)
            return null;
        return (0, geometry_1.normalizeLinear)(el);
    }
    return el;
}
function coerceView(v) {
    const r = (v && typeof v === 'object' ? v : {});
    return {
        scrollX: num(r.scrollX, 0),
        scrollY: num(r.scrollY, 0),
        zoom: Math.min(30, Math.max(0.05, num(r.zoom, 1))),
    };
}
/* ---------------------------------------------------- serialisation */
function serializeDocument(doc) {
    return `${JSON.stringify({ ...doc, type: 'sketch', version: exports.SCHEMA_VERSION }, null, 2)}\n`;
}
/** Reads a .sketch.json. Tolerates a bare element array (what an early
 *  export wrote) and anything with recognisable elements; only genuinely
 *  unreadable bytes fail. */
function parseDocument(text, fallbackName = 'Untitled') {
    let data;
    try {
        data = JSON.parse(text);
    }
    catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        return { ok: false, error: `This file is not valid JSON (${message}).`, raw: text };
    }
    let migrated = false;
    let rawElements;
    let base;
    if (Array.isArray(data)) {
        // Version 0: a bare array of elements.
        rawElements = data;
        base = {};
        migrated = true;
    }
    else if (data && typeof data === 'object') {
        base = data;
        if (base.type !== undefined && base.type !== 'sketch') {
            return { ok: false, error: `This is a "${String(base.type)}" file, not a Sketch board.`, raw: text };
        }
        if (!Array.isArray(base.elements)) {
            return { ok: false, error: 'This Sketch file has no "elements" list, so there is nothing to draw.', raw: text };
        }
        rawElements = base.elements;
        if (num(base.version, 0) !== exports.SCHEMA_VERSION)
            migrated = true;
    }
    else {
        return { ok: false, error: 'This file does not contain a Sketch board.', raw: text };
    }
    const elements = [];
    const seen = new Set();
    let dropped = 0;
    for (const raw of rawElements) {
        const el = coerceElement(raw);
        if (!el) {
            dropped++;
            continue;
        }
        if (seen.has(el.id))
            el.id = newId();
        seen.add(el.id);
        elements.push(el);
    }
    const created = str(base.createdAt, '') || new Date().toISOString();
    const doc = {
        type: 'sketch',
        version: exports.SCHEMA_VERSION,
        name: str(base.name, '') || fallbackName,
        createdAt: created,
        updatedAt: str(base.updatedAt, '') || created,
        background: oneOf(base.background, ['grid', 'dots', 'plain'], 'grid'),
        view: coerceView(base.view),
        elements,
    };
    return { ok: true, doc, dropped, migrated };
}
/* ------------------------------------------------------------ names */
const ILLEGAL_CHARS = new Set(('/:*?"<>|' + String.fromCharCode(92)).split(''));
/** A safe, human-looking file stem: path separators, the Windows-reserved
 *  punctuation and control characters become spaces, but commas, hyphens
 *  and spaces survive - a board called "Q3 plan, v2" keeps its name.
 *  Never empty, never a dotfile. */
function sanitizeName(name) {
    let swapped = '';
    for (const ch of name) {
        const code = ch.codePointAt(0) ?? 0;
        swapped += ILLEGAL_CHARS.has(ch) || code < 32 || code === 127 ? ' ' : ch;
    }
    const cleaned = swapped.replace(/\s+/g, ' ').trim().replace(/^\.+/, '').slice(0, 80).trim();
    return cleaned || 'Untitled';
}
const fileNameFor = (name) => `${sanitizeName(name)}${exports.FILE_EXTENSION}`;
exports.fileNameFor = fileNameFor;
const documentPath = (folder, name) => `${folder}/${(0, exports.fileNameFor)(name)}`;
exports.documentPath = documentPath;
function nameFromFileName(fileName) {
    return fileName.endsWith(exports.FILE_EXTENSION) ? fileName.slice(0, -exports.FILE_EXTENSION.length) : fileName.replace(/\.json$/, '');
}
const isSketchFile = (fileName) => fileName.endsWith(exports.FILE_EXTENSION);
exports.isSketchFile = isSketchFile;
/** "Board", "Board 2", "Board 3"... against names already taken. */
function uniqueName(existing, base) {
    const taken = new Set(Array.from(existing, (n) => n.toLowerCase()));
    const clean = sanitizeName(base);
    if (!taken.has(clean.toLowerCase()))
        return clean;
    for (let i = 2; i < 10000; i++) {
        const candidate = `${clean} ${i}`;
        if (!taken.has(candidate.toLowerCase()))
            return candidate;
    }
    return `${clean} ${Date.now()}`;
}
/* -------------------------------------------------------- describing */
const TYPE_LABEL = {
    rect: 'rectangle',
    ellipse: 'ellipse',
    diamond: 'diamond',
    line: 'line',
    arrow: 'arrow',
    draw: 'freehand stroke',
    text: 'text',
    sticky: 'sticky note',
    frame: 'frame',
};
/** A plain-text outline of a board: what is on it and what points at what.
 *  This is what `sketch_describe` returns, so an assistant can talk about
 *  a diagram it cannot see. */
function describeDocument(doc) {
    const lines = [];
    const byId = new Map(doc.elements.map((el) => [el.id, el]));
    const labelFor = (el) => {
        const t = (el.text || el.name).replace(/\s+/g, ' ').trim();
        return t ? `"${t}"` : `${TYPE_LABEL[el.type]} ${el.id}`;
    };
    const shapes = doc.elements.filter((el) => !types_1.LINEAR_TYPES.has(el.type));
    const connectors = doc.elements.filter((el) => el.type === 'arrow' || el.type === 'line');
    const strokes = doc.elements.filter((el) => el.type === 'draw');
    lines.push(`Board "${doc.name}" - ${doc.elements.length} element${doc.elements.length === 1 ? '' : 's'}.`);
    const frames = shapes.filter((el) => el.type === 'frame');
    if (frames.length)
        lines.push(`Frames: ${frames.map((f) => `"${f.name || 'Frame'}"`).join(', ')}.`);
    if (shapes.length) {
        lines.push('', 'Shapes:');
        for (const el of shapes) {
            if (el.type === 'frame')
                continue;
            const pos = `at (${Math.round(el.x)}, ${Math.round(el.y)}), ${Math.round(el.w)}x${Math.round(el.h)}`;
            lines.push(`- ${TYPE_LABEL[el.type]} ${labelFor(el)} ${pos}`);
        }
    }
    if (connectors.length) {
        lines.push('', 'Connections:');
        for (const el of connectors) {
            const from = el.startBinding ? byId.get(el.startBinding.elementId) : undefined;
            const to = el.endBinding ? byId.get(el.endBinding.elementId) : undefined;
            const label = el.text ? ` labelled "${el.text}"` : '';
            if (from && to)
                lines.push(`- ${labelFor(from)} -> ${labelFor(to)}${label}`);
            else if (to)
                lines.push(`- (unattached) -> ${labelFor(to)}${label}`);
            else if (from)
                lines.push(`- ${labelFor(from)} -> (unattached)${label}`);
            else
                lines.push(`- free ${TYPE_LABEL[el.type]}${label}`);
        }
    }
    if (strokes.length)
        lines.push('', `${strokes.length} freehand stroke${strokes.length === 1 ? '' : 's'}.`);
    if (doc.elements.length === 0)
        lines.push('', 'The board is empty.');
    return lines.join('\n');
}

  };
  modules["src/lib/svg.ts"] = function (module, exports, require) {
"use strict";
// SVG export. Not a screenshot in an <image> tag and not a pile of paths:
// a rectangle leaves as <rect>, an ellipse as <ellipse>, a diamond as
// <polygon>, text as real <text> with <tspan> lines, and only freehand
// and curved connectors become <path> - so the file opens in Figma,
// Illustrator or Inkscape as editable objects, and its text is
// selectable, searchable and translatable.
//
// Pure: builds a string. The text measurer is injected, so the exporter
// runs with no DOM at all (which is how `sketch_export_svg` answers an
// agent on a board that is not even open).
Object.defineProperty(exports, "__esModule", { value: true });
exports.frameBounds = exports.documentToSvg = void 0;
exports.escapeXml = escapeXml;
exports.elementsToSvg = elementsToSvg;
exports.exportBounds = exportBounds;
const types_1 = require("./types");
const geometry_1 = require("./geometry");
const shapes_1 = require("./shapes");
const textLayout_1 = require("./textLayout");
const text_1 = require("./text");
const defaultMeasure = (text, fontSize, font) => (0, text_1.estimateTextWidth)(text, fontSize, font);
const num = (n) => {
    const r = Math.round(n * 100) / 100;
    return Object.is(r, -0) ? '0' : String(r);
};
function escapeXml(value) {
    return value
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&apos;');
}
const attrs = (map) => Object.entries(map)
    .filter(([, v]) => v !== undefined && v !== '')
    .map(([k, v]) => `${k}="${typeof v === 'number' ? num(v) : escapeXml(String(v))}"`)
    .join(' ');
const degrees = (radians) => (radians * 180) / Math.PI;
function fillFor(el, defs) {
    if (el.fillStyle === 'none' || el.fill === 'transparent' || !el.fill)
        return 'none';
    if (el.fillStyle === 'hatch') {
        let id = defs.hatch.get(el.fill);
        if (!id) {
            id = `hatch-${defs.hatch.size + 1}`;
            defs.hatch.set(el.fill, id);
        }
        return `url(#${id})`;
    }
    return el.fill;
}
function strokeAttrs(el) {
    const dash = (0, shapes_1.dashPattern)(el.strokeStyle, el.strokeWidth);
    return {
        stroke: el.stroke === 'transparent' ? undefined : el.stroke,
        'stroke-width': el.stroke === 'transparent' ? undefined : el.strokeWidth,
        'stroke-linecap': 'round',
        'stroke-linejoin': 'round',
        'stroke-dasharray': dash.length ? dash.map(num).join(' ') : undefined,
    };
}
/* ---------------------------------------------------------- elements */
function textSvg(el, measure) {
    const layout = (0, textLayout_1.elementTextLayout)(el, measure);
    if (!layout)
        return '';
    const anchor = layout.align === 'left' ? 'start' : layout.align === 'right' ? 'end' : 'middle';
    const colour = el.type === 'frame' ? el.stroke : el.textColor;
    const tspans = layout.lines
        .map((line, i) => `<tspan ${attrs({ x: layout.anchorX, y: layout.firstBaselineY + i * layout.lineHeight })}>${escapeXml(line) || ' '}</tspan>`)
        .join('');
    return `<text ${attrs({
        'font-family': types_1.FONT_STACKS[el.fontFamily],
        'font-size': el.fontSize,
        fill: colour,
        'text-anchor': anchor,
        'xml:space': 'preserve',
    })}>${tspans}</text>`;
}
function headSvg(el, which) {
    const head = (0, shapes_1.headsFor)(el)[which];
    if (!head)
        return '';
    if (head.kind === 'dot') {
        return `<circle ${attrs({ cx: head.center[0], cy: head.center[1], r: head.radius, fill: el.stroke })} />`;
    }
    const commands = [
        ['M', head.points[0][0], head.points[0][1]],
        ...head.points.slice(1).map((p) => ['L', p[0], p[1]]),
    ];
    const d = (0, shapes_1.commandsToPathData)(commands);
    return `<path ${attrs({
        d,
        fill: 'none',
        stroke: el.stroke,
        'stroke-width': el.strokeWidth,
        'stroke-linecap': 'round',
        'stroke-linejoin': 'round',
    })} />`;
}
function elementSvg(el, defs, opts, measure) {
    const parts = [];
    const fill = fillFor(el, defs);
    const stroke = strokeAttrs(el);
    if ((0, types_1.isLinear)(el)) {
        const d = (0, shapes_1.commandsToPathData)((0, shapes_1.linearCommands)(el));
        if (d)
            parts.push(`<path ${attrs({ d, fill: 'none', ...stroke })} />`);
        parts.push(headSvg(el, 'start'), headSvg(el, 'end'));
    }
    else if (el.type === 'rect' || el.type === 'sticky') {
        const r = el.type === 'sticky' ? 3 : (0, shapes_1.cornerRadius)(el);
        parts.push(`<rect ${attrs({
            x: el.x,
            y: el.y,
            width: el.w,
            height: el.h,
            rx: r || undefined,
            ry: r || undefined,
            fill: el.type === 'sticky' ? el.fill : fill,
            ...stroke,
        })} />`);
    }
    else if (el.type === 'ellipse') {
        parts.push(`<ellipse ${attrs({ cx: el.x + el.w / 2, cy: el.y + el.h / 2, rx: el.w / 2, ry: el.h / 2, fill, ...stroke })} />`);
    }
    else if (el.type === 'diamond') {
        const points = (0, geometry_1.diamondPoints)(el).map(([x, y]) => `${num(x)},${num(y)}`).join(' ');
        parts.push(`<polygon ${attrs({ points, fill, ...stroke })} />`);
    }
    else if (el.type === 'frame') {
        parts.push(`<rect ${attrs({
            x: el.x,
            y: el.y,
            width: el.w,
            height: el.h,
            fill: 'none',
            stroke: opts.frameStroke ?? el.stroke,
            'stroke-width': 1.5,
        })} />`);
    }
    const text = textSvg(el, measure);
    if (text)
        parts.push(text);
    const body = parts.filter(Boolean).join('');
    if (!body)
        return '';
    const wrapper = {};
    if (el.angle) {
        wrapper.transform = `rotate(${num(degrees(el.angle))} ${num(el.x + el.w / 2)} ${num(el.y + el.h / 2)})`;
    }
    if (el.opacity < 1)
        wrapper.opacity = el.opacity;
    const wrapperAttrs = attrs(wrapper);
    return wrapperAttrs ? `<g ${wrapperAttrs}>${body}</g>` : body;
}
/* ------------------------------------------------------------ export */
function elementsToSvg(elements, opts = {}) {
    const measure = opts.measure ?? defaultMeasure;
    const padding = opts.padding ?? 24;
    const box = opts.bounds ?? (0, geometry_1.expandBounds)(elements.length ? (0, geometry_1.boundsOfElements)(elements) : { x: 0, y: 0, w: 1, h: 1 }, padding);
    const width = Math.max(1, Math.round(box.w));
    const height = Math.max(1, Math.round(box.h));
    const scale = opts.scale ?? 1;
    const defs = { hatch: new Map() };
    // Frames first so their outlines never sit on top of their contents.
    const ordered = [...elements.filter((el) => el.type === 'frame'), ...elements.filter((el) => el.type !== 'frame')];
    const body = ordered.map((el) => elementSvg(el, defs, opts, measure)).filter(Boolean).join('\n  ');
    const patterns = Array.from(defs.hatch.entries())
        .map(([colour, id]) => `<pattern id="${id}" width="7" height="7" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">` +
        `<line ${attrs({ x1: 0, y1: 0, x2: 0, y2: 7, stroke: colour, 'stroke-width': 1.6 })} /></pattern>`)
        .join('');
    const background = opts.background && opts.background !== 'transparent'
        ? `\n  <rect ${attrs({ x: box.x, y: box.y, width, height, fill: opts.background })} />`
        : '';
    return [
        '<?xml version="1.0" encoding="UTF-8"?>',
        `<svg xmlns="http://www.w3.org/2000/svg" width="${num(width * scale)}" height="${num(height * scale)}" ` +
            `viewBox="${num(box.x)} ${num(box.y)} ${num(width)} ${num(height)}">`,
        opts.title ? `  <title>${escapeXml(opts.title)}</title>` : '',
        patterns ? `  <defs>${patterns}</defs>` : '',
        background.trim() ? background.trim().replace(/^/, '  ') : '',
        body ? `  ${body}` : '',
        '</svg>',
        '',
    ]
        .filter((line) => line !== '')
        .join('\n');
}
/** The whole board, framed on its own contents. */
const documentToSvg = (doc, opts = {}) => elementsToSvg(doc.elements, { title: doc.name, ...opts });
exports.documentToSvg = documentToSvg;
/** The box an export covers: the selection's, a frame's, or everything. */
function exportBounds(elements, padding = 24) {
    if (!elements.length)
        return { x: 0, y: 0, w: 1, h: 1 };
    return (0, geometry_1.expandBounds)((0, geometry_1.boundsOfElements)(elements), padding);
}
const frameBounds = (frame) => (0, geometry_1.rotatedBounds)(frame);
exports.frameBounds = frameBounds;

  };
  modules["src/lib/types.ts"] = function (module, exports, require) {
"use strict";
// The retained element model. One flat shape covers every element type so
// serialisation, history snapshots and generic transforms (move, resize,
// align, z-order) never need a discriminated-union switch; the fields a
// given type does not use are simply left at their defaults.
//
// Pure: no React, no DOM, no SDK. Everything under src/lib is importable
// from a plain `node --experimental-transform-types` test.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_ELEMENT = exports.STROKE_WIDTHS = exports.FONT_SIZES = exports.FONT_STACKS = exports.isContainer = exports.isBindable = exports.isLinear = exports.CONTAINER_TYPES = exports.BINDABLE_TYPES = exports.LINEAR_TYPES = void 0;
exports.LINEAR_TYPES = new Set(['line', 'arrow', 'draw']);
exports.BINDABLE_TYPES = new Set(['rect', 'ellipse', 'diamond', 'sticky', 'text', 'frame']);
exports.CONTAINER_TYPES = new Set(['rect', 'ellipse', 'diamond', 'sticky']);
const isLinear = (el) => exports.LINEAR_TYPES.has(el.type);
exports.isLinear = isLinear;
const isBindable = (el) => exports.BINDABLE_TYPES.has(el.type);
exports.isBindable = isBindable;
/** Double-clicking one of these edits text centred inside the shape. */
const isContainer = (el) => exports.CONTAINER_TYPES.has(el.type);
exports.isContainer = isContainer;
exports.FONT_STACKS = {
    sans: "'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif",
    serif: "'Iowan Old Style', Georgia, 'Times New Roman', serif",
    mono: "ui-monospace, 'SF Mono', 'JetBrains Mono', Menlo, monospace",
};
exports.FONT_SIZES = [14, 20, 28, 40];
exports.STROKE_WIDTHS = [1.5, 3, 6];
exports.DEFAULT_ELEMENT = {
    x: 0,
    y: 0,
    w: 0,
    h: 0,
    angle: 0,
    stroke: '#1e1e1e',
    fill: 'transparent',
    fillStyle: 'none',
    strokeWidth: 3,
    strokeStyle: 'solid',
    opacity: 1,
    roundness: 0.12,
    locked: false,
    groupId: null,
    text: '',
    fontSize: 20,
    fontFamily: 'sans',
    textAlign: 'center',
    textColor: '#1e1e1e',
    points: [],
    edge: 'straight',
    startArrow: 'none',
    endArrow: 'none',
    startBinding: null,
    endBinding: null,
    name: '',
};

  };
  modules["src/lib/palette.ts"] = function (module, exports, require) {
"use strict";
// The curated colours, one set per theme, plus the canvas chrome colours
// the renderer needs. Two palettes rather than one because a stroke that
// reads as "ink" on paper reads as mud on a dark board: the dark set is
// lighter, less saturated and tuned to sit on #16171b.
Object.defineProperty(exports, "__esModule", { value: true });
exports.readableTextOn = exports.isHexColor = exports.paletteFor = exports.DARK = exports.LIGHT = void 0;
exports.normalizeHex = normalizeHex;
exports.translateColor = translateColor;
exports.luminance = luminance;
exports.LIGHT = {
    strokes: [
        { name: 'Ink', value: '#1e1e1e' },
        { name: 'Slate', value: '#5b6472' },
        { name: 'Red', value: '#c2372b' },
        { name: 'Orange', value: '#c2610b' },
        { name: 'Green', value: '#2e7d4f' },
        { name: 'Teal', value: '#0f7a8a' },
        { name: 'Blue', value: '#1c62c9' },
        { name: 'Violet', value: '#6a41c4' },
    ],
    fills: [
        { name: 'None', value: 'transparent' },
        { name: 'Paper', value: '#f3f3f1' },
        { name: 'Rose', value: '#fbdcd8' },
        { name: 'Amber', value: '#fbe6c4' },
        { name: 'Mint', value: '#d4eedd' },
        { name: 'Sky', value: '#d6e6fb' },
        { name: 'Lilac', value: '#e3daf8' },
        { name: 'Stone', value: '#e2e3e6' },
    ],
    background: '#fbfbfa',
    surface: '#ffffff',
    surfaceRaised: '#ffffff',
    border: '#e3e3e0',
    text: '#1e1e1e',
    textDim: '#6c6f76',
    accent: '#3b5bdb',
    accentSoft: '#e7ecfd',
    selection: '#3b5bdb',
    grid: '#ececea',
    gridStrong: '#dedede',
    defaultStroke: '#1e1e1e',
    defaultText: '#1e1e1e',
    stickyFill: '#fde9a9',
    stickyText: '#413a1c',
    frameStroke: '#b8bbc2',
};
exports.DARK = {
    strokes: [
        { name: 'Chalk', value: '#e6e7ea' },
        { name: 'Slate', value: '#9aa3b2' },
        { name: 'Red', value: '#f08a7d' },
        { name: 'Orange', value: '#efb063' },
        { name: 'Green', value: '#77d39a' },
        { name: 'Teal', value: '#63cbd9' },
        { name: 'Blue', value: '#7fa9f5' },
        { name: 'Violet', value: '#b49af0' },
    ],
    fills: [
        { name: 'None', value: 'transparent' },
        { name: 'Charcoal', value: '#2a2c33' },
        { name: 'Rose', value: '#4a2b2a' },
        { name: 'Amber', value: '#4a3a22' },
        { name: 'Mint', value: '#22412f' },
        { name: 'Sky', value: '#23374f' },
        { name: 'Lilac', value: '#3a3151' },
        { name: 'Stone', value: '#33363d' },
    ],
    background: '#16171b',
    surface: '#1e2026',
    surfaceRaised: '#24272e',
    border: '#31343c',
    text: '#e6e7ea',
    textDim: '#9096a1',
    accent: '#7fa9f5',
    accentSoft: '#23304a',
    selection: '#7fa9f5',
    grid: '#1e2026',
    gridStrong: '#272a31',
    defaultStroke: '#e6e7ea',
    defaultText: '#e6e7ea',
    stickyFill: '#6b5a1f',
    stickyText: '#fdf3d3',
    frameStroke: '#454952',
};
const paletteFor = (isDark) => (isDark ? exports.DARK : exports.LIGHT);
exports.paletteFor = paletteFor;
const HEX = /^#(?:[0-9a-f]{3}|[0-9a-f]{6}|[0-9a-f]{8})$/i;
const isHexColor = (value) => HEX.test(value.trim());
exports.isHexColor = isHexColor;
function normalizeHex(value) {
    const v = value.trim();
    const withHash = v.startsWith('#') ? v : `#${v}`;
    return HEX.test(withHash) ? withHash.toLowerCase() : null;
}
/** Same stroke colour expressed for the other theme, so a document made
 *  in light mode is still legible in dark mode and back. Only the exact
 *  palette entries are mapped; a custom colour is left alone. */
function translateColor(value, toDark) {
    const from = toDark ? exports.LIGHT : exports.DARK;
    const to = toDark ? exports.DARK : exports.LIGHT;
    const i = from.strokes.findIndex((s) => s.value === value);
    if (i >= 0)
        return to.strokes[i].value;
    const j = from.fills.findIndex((s) => s.value === value);
    if (j >= 0)
        return to.fills[j].value;
    return value;
}
/** Relative luminance, for picking readable text over a fill. */
function luminance(hex) {
    const h = hex.replace('#', '');
    const full = h.length === 3 ? h.split('').map((c) => c + c).join('') : h.slice(0, 6);
    const n = parseInt(full, 16);
    if (Number.isNaN(n))
        return 0.5;
    const channel = (c) => {
        const s = c / 255;
        return s <= 0.04045 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4;
    };
    return 0.2126 * channel((n >> 16) & 255) + 0.7152 * channel((n >> 8) & 255) + 0.0722 * (n & 255);
}
const readableTextOn = (background, light = '#ffffff', dark = '#1e1e1e') => background === 'transparent' ? dark : luminance(background) > 0.5 ? dark : light;
exports.readableTextOn = readableTextOn;

  };
  modules["src/state/files.ts"] = function (module, exports, require) {
"use strict";
// Everything that touches the VFS. A board is one JSON file in
// <home>/Sketches; this module is the only place that knows that, so the
// rest of the app deals in documents and names, and the agent tools and
// the UI share exactly one implementation of "save this board".
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FILE_EXTENSION = exports.pathFor = exports.sketchesDir = void 0;
exports.homeDir = homeDir;
exports.ensureFolder = ensureFolder;
exports.listDocuments = listDocuments;
exports.readDocumentFile = readDocumentFile;
exports.writeDocumentFile = writeDocumentFile;
exports.deleteDocumentFile = deleteDocumentFile;
exports.renameDocumentFile = renameDocumentFile;
exports.freeName = freeName;
exports.fileExists = fileExists;
const sdk_1 = __importDefault(require("@kumin/sdk"));
const document_1 = require("../lib/document");
Object.defineProperty(exports, "FILE_EXTENSION", { enumerable: true, get: function () { return document_1.FILE_EXTENSION; } });
const vfs = () => sdk_1.default.getVfs();
/** `~/Apps/sketch` -> `~`. The OS hands an app its own directory; the
 *  person's home is its grandparent, and that is where their work lives. */
function homeDir() {
    const dir = sdk_1.default.app.dir || '';
    const stripped = dir.replace(/\/Apps\/[^/]+\/?$/, '');
    return stripped || '/home/user';
}
const sketchesDir = () => `${homeDir()}/${document_1.FOLDER_NAME}`;
exports.sketchesDir = sketchesDir;
const pathFor = (name) => `${(0, exports.sketchesDir)()}/${(0, document_1.fileNameFor)(name)}`;
exports.pathFor = pathFor;
async function ensureFolder() {
    await vfs().mkdir((0, exports.sketchesDir)(), { recursive: true });
}
async function listDocuments() {
    try {
        const entries = await vfs().readdir((0, exports.sketchesDir)());
        return entries
            .filter((e) => e.kind !== 'dir' && (0, document_1.isSketchFile)(e.name))
            .map((e) => ({ name: (0, document_1.nameFromFileName)(e.name), path: e.path, size: e.size, mtime: e.mtime }))
            .sort((a, b) => b.mtime - a.mtime);
    }
    catch {
        return [];
    }
}
async function readDocumentFile(path) {
    const fallback = (0, document_1.nameFromFileName)(path.split('/').pop() ?? 'Untitled');
    try {
        const text = await vfs().readText(path);
        return (0, document_1.parseDocument)(text, fallback);
    }
    catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        return { ok: false, error: `This board could not be read (${message}).`, raw: '' };
    }
}
async function writeDocumentFile(path, doc) {
    await ensureFolder();
    await vfs().writeFile(path, (0, document_1.serializeDocument)({ ...doc, updatedAt: new Date().toISOString() }));
}
async function deleteDocumentFile(path) {
    await vfs().rm(path, { force: true });
}
async function renameDocumentFile(from, name) {
    const to = (0, exports.pathFor)(name);
    if (to !== from)
        await vfs().rename(from, to);
    return to;
}
/** A name nothing else in the folder is using. */
async function freeName(base) {
    const existing = await listDocuments();
    return (0, document_1.uniqueName)(existing.map((f) => f.name), base);
}
async function fileExists(path) {
    try {
        return await vfs().exists(path);
    }
    catch {
        return false;
    }
}

  };
  modules["src/lib/geometry.ts"] = function (module, exports, require) {
"use strict";
// Every piece of maths the canvas, the hit tester and the SVG exporter
// share: rotation, bounds, point-in-shape tests, segment distance,
// resize/flip transforms and the curve/elbow routing used to draw a line.
//
// Pure - no DOM, no React. src/canvas/render.ts and src/lib/svg.ts both
// build their paths from the functions here, so a curve looks identical
// on screen and in an exported file by construction.
Object.defineProperty(exports, "__esModule", { value: true });
exports.visibleBounds = exports.canvasToScene = exports.sceneToCanvas = exports.RESIZE_HANDLES = exports.translateElement = exports.normalizeBounds = exports.boundsContain = exports.boundsIntersect = exports.pointInBounds = exports.expandBounds = exports.boundsCenter = exports.boundsOfElements = exports.elementBounds = exports.elementCenter = exports.roundTo = exports.clamp = exports.TAU = void 0;
exports.rotatePoint = rotatePoint;
exports.rotatedBounds = rotatedBounds;
exports.boundsOfPoints = boundsOfPoints;
exports.unionBounds = unionBounds;
exports.distanceToSegment = distanceToSegment;
exports.distanceToPolyline = distanceToPolyline;
exports.distanceToRectOutline = distanceToRectOutline;
exports.distanceToDiamondOutline = distanceToDiamondOutline;
exports.diamondPoints = diamondPoints;
exports.ellipseValue = ellipseValue;
exports.distanceToEllipseOutline = distanceToEllipseOutline;
exports.pointInDiamond = pointInDiamond;
exports.toLocal = toLocal;
exports.toWorld = toWorld;
exports.linearPoints = linearPoints;
exports.hitTest = hitTest;
exports.hitTestElements = hitTestElements;
exports.elementsInBounds = elementsInBounds;
exports.curveSegments = curveSegments;
exports.elbowRoute = elbowRoute;
exports.linearPathPoints = linearPathPoints;
exports.pointAlongPolyline = pointAlongPolyline;
exports.polylineLength = polylineLength;
exports.simplifyPoints = simplifyPoints;
exports.normalizeLinear = normalizeLinear;
exports.transformElement = transformElement;
exports.handlePositions = handlePositions;
exports.rotateHandlePosition = rotateHandlePosition;
exports.resizedLocalBounds = resizedLocalBounds;
exports.resizeElement = resizeElement;
exports.rotateElementTo = rotateElementTo;
exports.rotateAbout = rotateAbout;
exports.zoomAt = zoomAt;
exports.fitBounds = fitBounds;
const types_1 = require("./types");
exports.TAU = Math.PI * 2;
const clamp = (v, lo, hi) => (v < lo ? lo : v > hi ? hi : v);
exports.clamp = clamp;
const roundTo = (v, step) => (step > 0 ? Math.round(v / step) * step : v);
exports.roundTo = roundTo;
function rotatePoint(p, cx, cy, angle) {
    if (angle === 0)
        return [p[0], p[1]];
    const c = Math.cos(angle);
    const s = Math.sin(angle);
    const dx = p[0] - cx;
    const dy = p[1] - cy;
    return [cx + dx * c - dy * s, cy + dx * s + dy * c];
}
const elementCenter = (el) => [el.x + el.w / 2, el.y + el.h / 2];
exports.elementCenter = elementCenter;
const elementBounds = (el) => ({ x: el.x, y: el.y, w: el.w, h: el.h });
exports.elementBounds = elementBounds;
/** The axis-aligned box that contains the element after rotation. */
function rotatedBounds(el) {
    if (!el.angle)
        return (0, exports.elementBounds)(el);
    const [cx, cy] = (0, exports.elementCenter)(el);
    const corners = [
        [el.x, el.y],
        [el.x + el.w, el.y],
        [el.x + el.w, el.y + el.h],
        [el.x, el.y + el.h],
    ].map((p) => rotatePoint(p, cx, cy, el.angle));
    return boundsOfPoints(corners);
}
function boundsOfPoints(points) {
    if (!points.length)
        return { x: 0, y: 0, w: 0, h: 0 };
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    for (const [x, y] of points) {
        if (x < minX)
            minX = x;
        if (y < minY)
            minY = y;
        if (x > maxX)
            maxX = x;
        if (y > maxY)
            maxY = y;
    }
    return { x: minX, y: minY, w: maxX - minX, h: maxY - minY };
}
function unionBounds(list) {
    if (!list.length)
        return { x: 0, y: 0, w: 0, h: 0 };
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    for (const b of list) {
        if (b.x < minX)
            minX = b.x;
        if (b.y < minY)
            minY = b.y;
        if (b.x + b.w > maxX)
            maxX = b.x + b.w;
        if (b.y + b.h > maxY)
            maxY = b.y + b.h;
    }
    return { x: minX, y: minY, w: maxX - minX, h: maxY - minY };
}
const boundsOfElements = (els) => unionBounds(els.map(rotatedBounds));
exports.boundsOfElements = boundsOfElements;
const boundsCenter = (b) => [b.x + b.w / 2, b.y + b.h / 2];
exports.boundsCenter = boundsCenter;
const expandBounds = (b, by) => ({ x: b.x - by, y: b.y - by, w: b.w + by * 2, h: b.h + by * 2 });
exports.expandBounds = expandBounds;
const pointInBounds = (p, b) => p[0] >= b.x && p[0] <= b.x + b.w && p[1] >= b.y && p[1] <= b.y + b.h;
exports.pointInBounds = pointInBounds;
const boundsIntersect = (a, b) => a.x <= b.x + b.w && a.x + a.w >= b.x && a.y <= b.y + b.h && a.y + a.h >= b.y;
exports.boundsIntersect = boundsIntersect;
const boundsContain = (outer, inner) => inner.x >= outer.x && inner.y >= outer.y && inner.x + inner.w <= outer.x + outer.w && inner.y + inner.h <= outer.y + outer.h;
exports.boundsContain = boundsContain;
const normalizeBounds = (b) => ({
    x: b.w < 0 ? b.x + b.w : b.x,
    y: b.h < 0 ? b.y + b.h : b.y,
    w: Math.abs(b.w),
    h: Math.abs(b.h),
});
exports.normalizeBounds = normalizeBounds;
/* --------------------------------------------------------- distances */
function distanceToSegment(p, a, b) {
    const vx = b[0] - a[0];
    const vy = b[1] - a[1];
    const wx = p[0] - a[0];
    const wy = p[1] - a[1];
    const len2 = vx * vx + vy * vy;
    const t = len2 === 0 ? 0 : (0, exports.clamp)((wx * vx + wy * vy) / len2, 0, 1);
    const dx = a[0] + t * vx - p[0];
    const dy = a[1] + t * vy - p[1];
    return Math.hypot(dx, dy);
}
function distanceToPolyline(p, points) {
    if (points.length === 0)
        return Infinity;
    if (points.length === 1)
        return Math.hypot(p[0] - points[0][0], p[1] - points[0][1]);
    let best = Infinity;
    for (let i = 1; i < points.length; i++) {
        const d = distanceToSegment(p, points[i - 1], points[i]);
        if (d < best)
            best = d;
    }
    return best;
}
/** Perimeter distance to an axis-aligned rectangle's outline (0 when on it). */
function distanceToRectOutline(p, b) {
    const corners = [
        [b.x, b.y],
        [b.x + b.w, b.y],
        [b.x + b.w, b.y + b.h],
        [b.x, b.y + b.h],
    ];
    return distanceToPolyline(p, [...corners, corners[0]]);
}
function distanceToDiamondOutline(p, b) {
    const pts = diamondPoints(b);
    return distanceToPolyline(p, [...pts, pts[0]]);
}
function diamondPoints(b) {
    return [
        [b.x + b.w / 2, b.y],
        [b.x + b.w, b.y + b.h / 2],
        [b.x + b.w / 2, b.y + b.h],
        [b.x, b.y + b.h / 2],
    ];
}
/** Signed-ish measure: <= 1 inside an axis-aligned ellipse inscribed in b. */
function ellipseValue(p, b) {
    const rx = b.w / 2 || 1e-6;
    const ry = b.h / 2 || 1e-6;
    const dx = (p[0] - (b.x + rx)) / rx;
    const dy = (p[1] - (b.y + ry)) / ry;
    return dx * dx + dy * dy;
}
function distanceToEllipseOutline(p, b) {
    // Sampled outline: exact ellipse distance needs an iterative solve, and
    // 64 samples is well under a pixel of error at the tolerances hit
    // testing uses.
    const rx = b.w / 2;
    const ry = b.h / 2;
    const cx = b.x + rx;
    const cy = b.y + ry;
    const pts = [];
    for (let i = 0; i <= 64; i++) {
        const t = (i / 64) * exports.TAU;
        pts.push([cx + rx * Math.cos(t), cy + ry * Math.sin(t)]);
    }
    return distanceToPolyline(p, pts);
}
function pointInDiamond(p, b) {
    const rx = b.w / 2 || 1e-6;
    const ry = b.h / 2 || 1e-6;
    const dx = Math.abs(p[0] - (b.x + rx)) / rx;
    const dy = Math.abs(p[1] - (b.y + ry)) / ry;
    return dx + dy <= 1;
}
/* -------------------------------------------------------- hit testing */
/** Scene point expressed in the element's own unrotated frame. */
function toLocal(el, p) {
    const [cx, cy] = (0, exports.elementCenter)(el);
    return rotatePoint(p, cx, cy, -el.angle);
}
function toWorld(el, p) {
    const [cx, cy] = (0, exports.elementCenter)(el);
    return rotatePoint(p, cx, cy, el.angle);
}
/** Absolute scene positions of a line/arrow/freehand element's vertices. */
function linearPoints(el) {
    const [cx, cy] = (0, exports.elementCenter)(el);
    return el.points.map((p) => rotatePoint([el.x + p[0], el.y + p[1]], cx, cy, el.angle));
}
const isFilled = (el) => el.fillStyle !== 'none' && el.fill !== 'transparent';
/** Does `p` (scene coords) select this element, allowing `tolerance` slack? */
function hitTest(el, p, tolerance = 8) {
    const slack = tolerance + el.strokeWidth / 2;
    if ((0, types_1.isLinear)(el)) {
        return distanceToPolyline(p, linearPathPoints(el)) <= slack;
    }
    const local = toLocal(el, p);
    const b = (0, exports.elementBounds)(el);
    if (el.type === 'text' || el.type === 'sticky')
        return (0, exports.pointInBounds)(local, (0, exports.expandBounds)(b, slack / 2));
    if (el.type === 'frame') {
        // A frame is grabbed by its outline or its title strip, never by its
        // empty middle - otherwise it would swallow every click inside it.
        if (local[1] >= b.y - 26 && local[1] <= b.y && local[0] >= b.x && local[0] <= b.x + b.w)
            return true;
        return distanceToRectOutline(local, b) <= slack;
    }
    if (el.type === 'ellipse') {
        if (isFilled(el))
            return ellipseValue(local, b) <= 1;
        return distanceToEllipseOutline(local, b) <= slack;
    }
    if (el.type === 'diamond') {
        if (isFilled(el))
            return pointInDiamond(local, b);
        return distanceToDiamondOutline(local, b) <= slack;
    }
    // rect
    if (isFilled(el) || el.text)
        return (0, exports.pointInBounds)(local, (0, exports.expandBounds)(b, slack / 2));
    return distanceToRectOutline(local, b) <= slack;
}
/** Topmost element under the point; `elements` is bottom-to-top. */
function hitTestElements(elements, p, tolerance = 8) {
    for (let i = elements.length - 1; i >= 0; i--) {
        const el = elements[i];
        if (el.locked)
            continue;
        if (hitTest(el, p, tolerance))
            return el;
    }
    return null;
}
/** Marquee selection: an element counts when its rotated box is fully
 *  inside for shapes, or when any vertex is inside for linear elements. */
function elementsInBounds(elements, b) {
    const box = (0, exports.normalizeBounds)(b);
    return elements.filter((el) => {
        if (el.locked)
            return false;
        if ((0, types_1.isLinear)(el))
            return linearPoints(el).some((p) => (0, exports.pointInBounds)(p, box));
        return (0, exports.boundsContain)(box, rotatedBounds(el));
    });
}
/* ------------------------------------------------------- line routing */
/** Catmull-Rom through the points, expressed as cubic Bezier control
 *  points - one entry per segment: [c1, c2, end]. */
function curveSegments(points, tension = 0.5) {
    const out = [];
    if (points.length < 2)
        return out;
    for (let i = 0; i < points.length - 1; i++) {
        const p0 = points[i - 1] ?? points[i];
        const p1 = points[i];
        const p2 = points[i + 1];
        const p3 = points[i + 2] ?? p2;
        const c1 = [p1[0] + ((p2[0] - p0[0]) / 6) * tension * 2, p1[1] + ((p2[1] - p0[1]) / 6) * tension * 2];
        const c2 = [p2[0] - ((p3[0] - p1[0]) / 6) * tension * 2, p2[1] - ((p3[1] - p1[1]) / 6) * tension * 2];
        out.push([c1, c2, p2]);
    }
    return out;
}
/** An orthogonal route between two points, leaving each end along the
 *  dominant axis - what "elbow" arrows draw. */
function elbowRoute(a, b) {
    const dx = b[0] - a[0];
    const dy = b[1] - a[1];
    if (Math.abs(dx) < 1 || Math.abs(dy) < 1)
        return [a, b];
    if (Math.abs(dx) >= Math.abs(dy)) {
        const midX = a[0] + dx / 2;
        return [a, [midX, a[1]], [midX, b[1]], b];
    }
    const midY = a[1] + dy / 2;
    return [a, [a[0], midY], [b[0], midY], b];
}
/** The points actually drawn for a linear element, after elbow routing.
 *  Curved elements keep their vertices here (the curve is applied when
 *  the path is built) so hit testing stays close to what is on screen. */
function linearPathPoints(el) {
    const pts = linearPoints(el);
    if (el.edge === 'elbow' && pts.length === 2)
        return elbowRoute(pts[0], pts[1]);
    return pts;
}
/** Point at parameter t (0..1) along a polyline - used for arrow labels. */
function pointAlongPolyline(points, t) {
    if (points.length === 0)
        return [0, 0];
    if (points.length === 1)
        return points[0];
    let total = 0;
    const segs = [];
    for (let i = 1; i < points.length; i++) {
        const d = Math.hypot(points[i][0] - points[i - 1][0], points[i][1] - points[i - 1][1]);
        segs.push(d);
        total += d;
    }
    if (total === 0)
        return points[0];
    let target = (0, exports.clamp)(t, 0, 1) * total;
    for (let i = 0; i < segs.length; i++) {
        if (target <= segs[i] || i === segs.length - 1) {
            const f = segs[i] === 0 ? 0 : target / segs[i];
            return [points[i][0] + (points[i + 1][0] - points[i][0]) * f, points[i][1] + (points[i + 1][1] - points[i][1]) * f];
        }
        target -= segs[i];
    }
    return points[points.length - 1];
}
function polylineLength(points) {
    let total = 0;
    for (let i = 1; i < points.length; i++)
        total += Math.hypot(points[i][0] - points[i - 1][0], points[i][1] - points[i - 1][1]);
    return total;
}
/** Douglas-Peucker: keeps a freehand stroke light without visibly
 *  changing it (pen input arrives far denser than it needs to be). */
function simplifyPoints(points, epsilon = 0.6) {
    if (points.length < 3)
        return points.slice();
    let maxDist = 0;
    let index = 0;
    const first = points[0];
    const last = points[points.length - 1];
    for (let i = 1; i < points.length - 1; i++) {
        const d = distanceToSegment(points[i], first, last);
        if (d > maxDist) {
            maxDist = d;
            index = i;
        }
    }
    if (maxDist <= epsilon)
        return [first, last];
    const left = simplifyPoints(points.slice(0, index + 1), epsilon);
    const right = simplifyPoints(points.slice(index), epsilon);
    return [...left.slice(0, -1), ...right];
}
/* ------------------------------------------------------- transforms */
/** Re-anchors a linear element so its points start at (0,0) and w/h match
 *  their bounding box - the invariant every other function relies on. */
function normalizeLinear(el) {
    if (!(0, types_1.isLinear)(el) || el.points.length === 0)
        return el;
    const b = boundsOfPoints(el.points);
    const points = el.points.map((p) => [p[0] - b.x, p[1] - b.y]);
    return { ...el, x: el.x + b.x, y: el.y + b.y, w: b.w, h: b.h, points };
}
const translateElement = (el, dx, dy) => ({ ...el, x: el.x + dx, y: el.y + dy });
exports.translateElement = translateElement;
/** Maps an element from one bounding box to another, mirroring when a
 *  target dimension is negative. Drives resize, flip and scale-to-fit. */
function transformElement(el, from, to) {
    const sx = from.w === 0 ? 1 : to.w / from.w;
    const sy = from.h === 0 ? 1 : to.h / from.h;
    const mapX = (x) => to.x + (x - from.x) * sx;
    const mapY = (y) => to.y + (y - from.y) * sy;
    const next = {
        ...el,
        x: mapX(el.x),
        y: mapY(el.y),
        w: el.w * sx,
        h: el.h * sy,
        points: el.points.map((p) => [p[0] * sx, p[1] * sy]),
        angle: sx * sy < 0 ? -el.angle : el.angle,
    };
    const fixed = {
        ...next,
        x: next.w < 0 ? next.x + next.w : next.x,
        y: next.h < 0 ? next.y + next.h : next.y,
        w: Math.abs(next.w),
        h: Math.abs(next.h),
    };
    if ((0, types_1.isLinear)(fixed)) {
        const shifted = {
            ...fixed,
            x: next.x,
            y: next.y,
            points: next.points,
        };
        return normalizeLinear(shifted);
    }
    return fixed;
}
exports.RESIZE_HANDLES = ['nw', 'n', 'ne', 'e', 'se', 's', 'sw', 'w'];
/** Handle positions (scene coords) around a selection box. */
function handlePositions(b, angle = 0) {
    const [cx, cy] = (0, exports.boundsCenter)(b);
    const raw = {
        nw: [b.x, b.y],
        n: [b.x + b.w / 2, b.y],
        ne: [b.x + b.w, b.y],
        e: [b.x + b.w, b.y + b.h / 2],
        se: [b.x + b.w, b.y + b.h],
        s: [b.x + b.w / 2, b.y + b.h],
        sw: [b.x, b.y + b.h],
        w: [b.x, b.y + b.h / 2],
    };
    return exports.RESIZE_HANDLES.map((handle) => ({ handle, point: rotatePoint(raw[handle], cx, cy, angle) }));
}
function rotateHandlePosition(b, angle = 0, offset = 28) {
    const [cx, cy] = (0, exports.boundsCenter)(b);
    return rotatePoint([b.x + b.w / 2, b.y - offset], cx, cy, angle);
}
/** New local bounds for a handle drag. `local` is the pointer expressed in
 *  the element's unrotated frame. */
function resizedLocalBounds(b, handle, local, keepAspect = false, minSize = 1) {
    let { x, y, w, h } = b;
    let x1 = x + w;
    let y1 = y + h;
    if (handle.includes('w'))
        x = local[0];
    if (handle.includes('e'))
        x1 = local[0];
    if (handle.includes('n'))
        y = local[1];
    if (handle.includes('s'))
        y1 = local[1];
    let nw = x1 - x;
    let nh = y1 - y;
    if (keepAspect && b.w !== 0 && b.h !== 0 && handle.length === 2) {
        const ratio = b.h / b.w;
        const signW = nw < 0 ? -1 : 1;
        const signH = nh < 0 ? -1 : 1;
        const mag = Math.max(Math.abs(nw), Math.abs(nh) / ratio);
        nw = mag * signW;
        nh = mag * ratio * signH;
        if (handle.includes('w'))
            x = x1 - nw;
        if (handle.includes('n'))
            y = y1 - nh;
    }
    if (Math.abs(nw) < minSize)
        nw = nw < 0 ? -minSize : minSize;
    if (Math.abs(nh) < minSize)
        nh = nh < 0 ? -minSize : minSize;
    return { x, y, w: nw, h: nh };
}
/** Resizes one element by a handle, keeping the opposite corner pinned in
 *  world space even when the element is rotated. */
function resizeElement(el, handle, pointerScene, keepAspect = false) {
    const from = (0, exports.elementBounds)(el);
    const local = toLocal(el, pointerScene);
    const to = resizedLocalBounds(from, handle, local, keepAspect, (0, types_1.isLinear)(el) ? 0.01 : 2);
    const moved = transformElement(el, from, to);
    if (!el.angle)
        return moved;
    // The anchor is the corner/edge the drag does not touch; both frames
    // rotate about their own centre, so pin it by comparing world positions.
    const anchorLocal = [
        handle.includes('w') ? from.x + from.w : handle.includes('e') ? from.x : from.x + from.w / 2,
        handle.includes('n') ? from.y + from.h : handle.includes('s') ? from.y : from.y + from.h / 2,
    ];
    const [oldCx, oldCy] = (0, exports.elementCenter)(el);
    const worldAnchor = rotatePoint(anchorLocal, oldCx, oldCy, el.angle);
    const normTo = (0, exports.normalizeBounds)(to);
    const anchorInNew = [
        handle.includes('w') ? normTo.x + normTo.w : handle.includes('e') ? normTo.x : normTo.x + normTo.w / 2,
        handle.includes('n') ? normTo.y + normTo.h : handle.includes('s') ? normTo.y : normTo.y + normTo.h / 2,
    ];
    const [newCx, newCy] = [moved.x + moved.w / 2, moved.y + moved.h / 2];
    const rotatedAnchor = rotatePoint(anchorInNew, newCx, newCy, moved.angle);
    return { ...moved, x: moved.x + (worldAnchor[0] - rotatedAnchor[0]), y: moved.y + (worldAnchor[1] - rotatedAnchor[1]) };
}
function rotateElementTo(el, pointerScene, snap = false) {
    const [cx, cy] = (0, exports.elementCenter)(el);
    let angle = Math.atan2(pointerScene[1] - cy, pointerScene[0] - cx) + Math.PI / 2;
    if (snap)
        angle = (0, exports.roundTo)(angle, Math.PI / 12);
    return { ...el, angle: ((angle % exports.TAU) + exports.TAU) % exports.TAU };
}
/** Rotates a group about a shared centre: each element spins on its own
 *  axis and orbits the centre, which is what a person expects. */
function rotateAbout(el, cx, cy, delta) {
    const [ex, ey] = (0, exports.elementCenter)(el);
    const [nx, ny] = rotatePoint([ex, ey], cx, cy, delta);
    return { ...el, x: nx - el.w / 2, y: ny - el.h / 2, angle: ((el.angle + delta) % exports.TAU + exports.TAU) % exports.TAU };
}
const sceneToCanvas = (p, v) => [(p[0] - v.scrollX) * v.zoom, (p[1] - v.scrollY) * v.zoom];
exports.sceneToCanvas = sceneToCanvas;
const canvasToScene = (p, v) => [p[0] / v.zoom + v.scrollX, p[1] / v.zoom + v.scrollY];
exports.canvasToScene = canvasToScene;
/** The scene rectangle currently visible in a canvas of this size. */
const visibleBounds = (v, width, height) => ({
    x: v.scrollX,
    y: v.scrollY,
    w: width / v.zoom,
    h: height / v.zoom,
});
exports.visibleBounds = visibleBounds;
/** Zoom keeping the scene point under the cursor pinned to the cursor. */
function zoomAt(v, nextZoom, canvasPoint) {
    const zoom = (0, exports.clamp)(nextZoom, 0.05, 30);
    const before = (0, exports.canvasToScene)(canvasPoint, v);
    const after = (0, exports.canvasToScene)(canvasPoint, { ...v, zoom });
    return { zoom, scrollX: v.scrollX + before[0] - after[0], scrollY: v.scrollY + before[1] - after[1] };
}
/** Viewport that frames `b` inside a width x height canvas with padding. */
function fitBounds(b, width, height, padding = 64, maxZoom = 1.6) {
    if (b.w <= 0 && b.h <= 0)
        return { scrollX: b.x - width / 2, scrollY: b.y - height / 2, zoom: 1 };
    const zoom = (0, exports.clamp)(Math.min((width - padding * 2) / Math.max(b.w, 1), (height - padding * 2) / Math.max(b.h, 1)), 0.05, maxZoom);
    return {
        zoom,
        scrollX: b.x + b.w / 2 - width / 2 / zoom,
        scrollY: b.y + b.h / 2 - height / 2 / zoom,
    };
}

  };
  modules["src/lib/snap.ts"] = function (module, exports, require) {
"use strict";
// Snapping while you drag: to the grid, and to the edges and centres of
// the other elements on the board. Returns the nudge to apply plus the
// guide lines to draw, so the renderer never recomputes what the drag
// already worked out.
//
// Pure: bounds in, bounds and guides out.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_SNAP_THRESHOLD = void 0;
exports.snapToElements = snapToElements;
exports.snapToGrid = snapToGrid;
exports.snapDrag = snapDrag;
exports.DEFAULT_SNAP_THRESHOLD = 6;
const edgesX = (b) => [b.x, b.x + b.w / 2, b.x + b.w];
const edgesY = (b) => [b.y, b.y + b.h / 2, b.y + b.h];
/** Aligns `moving` to whichever of `targets` it is nearly aligned with.
 *  `threshold` is in scene units (the caller divides by the zoom, so the
 *  pull feels the same at every magnification). */
function snapToElements(moving, targets, threshold = exports.DEFAULT_SNAP_THRESHOLD) {
    let bestX = null;
    let bestY = null;
    for (const t of targets) {
        for (const mx of edgesX(moving)) {
            for (const tx of edgesX(t)) {
                const delta = tx - mx;
                if (Math.abs(delta) <= threshold && (!bestX || Math.abs(delta) < Math.abs(bestX.delta))) {
                    bestX = {
                        delta,
                        line: { x: tx, from: Math.min(moving.y, t.y), to: Math.max(moving.y + moving.h, t.y + t.h) },
                    };
                }
            }
        }
        for (const my of edgesY(moving)) {
            for (const ty of edgesY(t)) {
                const delta = ty - my;
                if (Math.abs(delta) <= threshold && (!bestY || Math.abs(delta) < Math.abs(bestY.delta))) {
                    bestY = {
                        delta,
                        line: { y: ty, from: Math.min(moving.x, t.x), to: Math.max(moving.x + moving.w, t.x + t.w) },
                    };
                }
            }
        }
    }
    const lines = [];
    if (bestX)
        lines.push(bestX.line);
    if (bestY)
        lines.push(bestY.line);
    return { dx: bestX?.delta ?? 0, dy: bestY?.delta ?? 0, lines };
}
/** Grid snapping for the top-left corner. */
function snapToGrid(b, grid) {
    if (grid <= 0)
        return { dx: 0, dy: 0, lines: [] };
    return { dx: Math.round(b.x / grid) * grid - b.x, dy: Math.round(b.y / grid) * grid - b.y, lines: [] };
}
/** Element alignment first (it is what a person is usually after), the
 *  grid only on the axis alignment did not claim. */
function snapDrag(moving, targets, opts = {}) {
    const threshold = opts.threshold ?? exports.DEFAULT_SNAP_THRESHOLD;
    const aligned = snapToElements(moving, targets, threshold);
    if (!opts.grid)
        return aligned;
    const grid = snapToGrid({ ...moving, x: moving.x + aligned.dx, y: moving.y + aligned.dy }, opts.grid);
    return {
        dx: aligned.dx + (aligned.lines.some((l) => l.x !== undefined) ? 0 : grid.dx),
        dy: aligned.dy + (aligned.lines.some((l) => l.y !== undefined) ? 0 : grid.dy),
        lines: aligned.lines,
    };
}

  };
  modules["src/lib/binding.ts"] = function (module, exports, require) {
"use strict";
// Arrow-to-shape attachment. A bound endpoint stores only the shape's id
// and the gap it keeps; the actual coordinate is recomputed from the
// shape's current geometry every time anything moves, so an arrow follows
// its shape through a move, a resize, a rotation and an undo without any
// of those code paths knowing that bindings exist.
Object.defineProperty(exports, "__esModule", { value: true });
exports.absolutePoints = exports.BINDING_REACH = exports.MAX_BINDING_GAP = exports.DEFAULT_BINDING_GAP = void 0;
exports.boundaryPoint = boundaryPoint;
exports.fromAbsolutePoints = fromAbsolutePoints;
exports.bindableAt = bindableAt;
exports.makeBinding = makeBinding;
exports.updateBoundArrow = updateBoundArrow;
exports.refreshBindings = refreshBindings;
exports.pruneBindings = pruneBindings;
exports.boundArrowIds = boundArrowIds;
exports.labelAnchor = labelAnchor;
exports.labelBounds = labelBounds;
const types_1 = require("./types");
const geometry_1 = require("./geometry");
exports.DEFAULT_BINDING_GAP = 6;
exports.MAX_BINDING_GAP = 32;
/** How close a pointer has to be to a shape for a new arrow to snap to it. */
exports.BINDING_REACH = 18;
/** Where a ray aimed at `towards` leaves `shape`, pushed out by `gap`.
 *  Works in the shape's own rotated frame, so a tilted box still gets a
 *  tip that sits on its real edge. */
function boundaryPoint(shape, towards, gap = exports.DEFAULT_BINDING_GAP) {
    const [cx, cy] = (0, geometry_1.elementCenter)(shape);
    const local = (0, geometry_1.rotatePoint)(towards, cx, cy, -shape.angle);
    let dx = local[0] - cx;
    let dy = local[1] - cy;
    if (dx === 0 && dy === 0)
        dy = -1;
    const rx = Math.max(shape.w / 2, 0.5);
    const ry = Math.max(shape.h / 2, 0.5);
    let t;
    if (shape.type === 'ellipse') {
        t = 1 / Math.hypot(dx / rx, dy / ry);
    }
    else if (shape.type === 'diamond') {
        t = 1 / (Math.abs(dx) / rx + Math.abs(dy) / ry);
    }
    else {
        const tx = dx === 0 ? Infinity : rx / Math.abs(dx);
        const ty = dy === 0 ? Infinity : ry / Math.abs(dy);
        t = Math.min(tx, ty);
    }
    const len = Math.hypot(dx, dy) * t;
    const scale = len === 0 ? 0 : (len + gap) / len;
    const edgeLocal = [cx + dx * t * scale, cy + dy * t * scale];
    return (0, geometry_1.rotatePoint)(edgeLocal, cx, cy, shape.angle);
}
/** Absolute coordinates of a linear element's vertices (bindings ignore
 *  rotation: a bound arrow is always stored unrotated). */
const absolutePoints = (el) => el.points.map((p) => [el.x + p[0], el.y + p[1]]);
exports.absolutePoints = absolutePoints;
function fromAbsolutePoints(el, points) {
    if (!points.length)
        return el;
    const [ox, oy] = points[0];
    return (0, geometry_1.normalizeLinear)({ ...el, x: ox, y: oy, points: points.map((p) => [p[0] - ox, p[1] - oy]) });
}
/** The shape a new arrow endpoint at `p` should fasten to, if any. */
function bindableAt(elements, p, reach = exports.BINDING_REACH, exclude) {
    for (let i = elements.length - 1; i >= 0; i--) {
        const el = elements[i];
        if (el.id === exclude || el.locked || !(0, types_1.isBindable)(el) || el.type === 'frame')
            continue;
        if ((0, geometry_1.hitTest)(el, p, reach))
            return el;
    }
    return null;
}
function makeBinding(shape, tip) {
    const towards = tip;
    const edge = boundaryPoint(shape, towards, 0);
    const gap = Math.min(exports.MAX_BINDING_GAP, Math.max(0, Math.hypot(tip[0] - edge[0], tip[1] - edge[1])));
    return { elementId: shape.id, gap: Number.isFinite(gap) ? gap : exports.DEFAULT_BINDING_GAP };
}
/** Recomputes a single arrow's bound endpoints against the current scene. */
function updateBoundArrow(arrow, byId) {
    if (!(0, types_1.isLinear)(arrow) || (!arrow.startBinding && !arrow.endBinding))
        return arrow;
    const pts = (0, exports.absolutePoints)(arrow);
    if (pts.length < 2)
        return arrow;
    const start = arrow.startBinding ? byId.get(arrow.startBinding.elementId) : undefined;
    const end = arrow.endBinding ? byId.get(arrow.endBinding.elementId) : undefined;
    let next = pts.slice();
    if (start) {
        const aim = end && pts.length === 2 ? (0, geometry_1.elementCenter)(end) : next[1];
        next[0] = boundaryPoint(start, aim, arrow.startBinding.gap);
    }
    if (end) {
        const aim = start && pts.length === 2 ? (0, geometry_1.elementCenter)(start) : next[next.length - 2];
        next[next.length - 1] = boundaryPoint(end, aim, arrow.endBinding.gap);
    }
    if (next[0][0] === pts[0][0] && next[0][1] === pts[0][1] && next[next.length - 1][0] === pts[pts.length - 1][0] && next[next.length - 1][1] === pts[pts.length - 1][1]) {
        return arrow;
    }
    return fromAbsolutePoints(arrow, next);
}
/** Re-runs every binding in the scene. Cheap enough to call after any
 *  mutation: it touches only arrows that actually carry a binding, and
 *  returns the same array reference shape when nothing changed. */
function refreshBindings(elements) {
    const byId = new Map(elements.map((el) => [el.id, el]));
    let changed = false;
    const out = elements.map((el) => {
        const next = updateBoundArrow(el, byId);
        if (next !== el)
            changed = true;
        return next;
    });
    return changed ? out : elements;
}
/** Drops bindings that point at elements that no longer exist (after a
 *  delete, or after importing a partial selection). */
function pruneBindings(elements) {
    const ids = new Set(elements.map((el) => el.id));
    let changed = false;
    const out = elements.map((el) => {
        if (!el.startBinding && !el.endBinding)
            return el;
        const startOk = !el.startBinding || ids.has(el.startBinding.elementId);
        const endOk = !el.endBinding || ids.has(el.endBinding.elementId);
        if (startOk && endOk)
            return el;
        changed = true;
        return { ...el, startBinding: startOk ? el.startBinding : null, endBinding: endOk ? el.endBinding : null };
    });
    return changed ? out : elements;
}
/** Ids of arrows fastened to any of `ids` - the set that has to be
 *  refreshed when those elements move. */
function boundArrowIds(elements, ids) {
    const out = new Set();
    for (const el of elements) {
        if (!(0, types_1.isLinear)(el))
            continue;
        if ((el.startBinding && ids.has(el.startBinding.elementId)) || (el.endBinding && ids.has(el.endBinding.elementId)))
            out.add(el.id);
    }
    return out;
}
/** Where an arrow's label sits: the midpoint of the drawn route. */
function labelAnchor(arrow) {
    return (0, geometry_1.pointAlongPolyline)((0, geometry_1.linearPathPoints)(arrow), 0.5);
}
/** Box a label occupies, centred on the arrow's midpoint. */
function labelBounds(arrow, width, height) {
    const [x, y] = labelAnchor(arrow);
    return { x: x - width / 2, y: y - height / 2, w: width, h: height };
}

  };
  modules["src/lib/textLayout.ts"] = function (module, exports, require) {
"use strict";
// Where an element's text goes. One function answers it for every case -
// a free text element, a label centred inside a shape, the caption on an
// arrow's midpoint, a sticky note - and the canvas renderer, the SVG
// exporter and the in-place editing overlay all position themselves from
// its answer, so what you type, what you see and what you export line up
// to the pixel.
//
// Pure: the text measurer is injected (ctx.measureText on screen, the
// estimator from text.ts everywhere else).
Object.defineProperty(exports, "__esModule", { value: true });
exports.BASELINE_OFFSET = void 0;
exports.innerWidth = innerWidth;
exports.elementTextLayout = elementTextLayout;
exports.requiredHeight = requiredHeight;
const types_1 = require("./types");
const text_1 = require("./text");
const binding_1 = require("./binding");
/** The fraction of the font size between a line's vertical centre and its
 *  alphabetic baseline. Measured once against the three font stacks in
 *  types.ts; it is what keeps canvas text and SVG text on the same line. */
exports.BASELINE_OFFSET = 0.355;
const anchorFor = (align, x, width) => align === 'left' ? x : align === 'right' ? x + width : x + width / 2;
/** Wrap width for text living inside a shape: a diamond and an ellipse
 *  lose room to their curved sides, a rectangle only to its padding. */
function innerWidth(el) {
    if (el.type === 'diamond')
        return Math.max(16, el.w * 0.58);
    if (el.type === 'ellipse')
        return Math.max(16, el.w * 0.72);
    return Math.max(16, el.w - text_1.TEXT_PADDING * 2);
}
/** Null when the element carries no text at all. */
function elementTextLayout(el, measure) {
    const content = el.type === 'frame' ? el.name : el.text;
    if (!content)
        return null;
    const fontSize = el.fontSize;
    const lineHeight = (0, text_1.lineHeightFor)(fontSize);
    const m = (t) => measure(t, fontSize, el.fontFamily);
    if (el.type === 'frame') {
        // A frame's name sits on the strip above it, left aligned, never wrapped.
        const width = m(content);
        return {
            lines: [content],
            anchorX: el.x,
            firstBaselineY: el.y - 9 + fontSize * exports.BASELINE_OFFSET,
            lineHeight,
            align: 'left',
            x: el.x,
            y: el.y - 9 - lineHeight / 2,
            width,
            height: lineHeight,
            wrapWidth: width,
        };
    }
    if ((0, types_1.isLinear)(el)) {
        // An arrow's label rides the midpoint of the route it actually draws.
        const [cx, cy] = (0, binding_1.labelAnchor)(el);
        const lines = content.split('\n');
        let width = 0;
        for (const line of lines)
            width = Math.max(width, m(line));
        const height = lines.length * lineHeight;
        return {
            lines,
            anchorX: cx,
            firstBaselineY: cy - height / 2 + lineHeight / 2 + fontSize * exports.BASELINE_OFFSET,
            lineHeight,
            align: 'center',
            x: cx - width / 2,
            y: cy - height / 2,
            width,
            height,
            wrapWidth: width,
        };
    }
    if (el.type === 'text') {
        // A text element is its own box: it wraps to the width it was given
        // and grows downward from the top edge.
        const wrapWidth = el.w > 1 ? el.w : Infinity;
        const lines = wrapWidth === Infinity ? content.split('\n') : (0, text_1.wrapText)(content, wrapWidth, m);
        let width = 0;
        for (const line of lines)
            width = Math.max(width, m(line));
        const boxWidth = wrapWidth === Infinity ? width : el.w;
        return {
            lines,
            anchorX: anchorFor(el.textAlign, el.x, boxWidth),
            firstBaselineY: el.y + lineHeight / 2 + fontSize * exports.BASELINE_OFFSET,
            lineHeight,
            align: el.textAlign,
            x: el.x,
            y: el.y,
            width: boxWidth,
            height: lines.length * lineHeight,
            wrapWidth: boxWidth,
        };
    }
    // A container: wrapped, centred on the shape in both directions.
    const wrapWidth = (0, types_1.isContainer)(el) ? innerWidth(el) : Math.max(16, el.w - text_1.TEXT_PADDING * 2);
    const lines = (0, text_1.wrapText)(content, wrapWidth, m);
    const height = lines.length * lineHeight;
    const cx = el.x + el.w / 2;
    const cy = el.y + el.h / 2;
    return {
        lines,
        anchorX: anchorFor(el.textAlign, cx - wrapWidth / 2, wrapWidth),
        firstBaselineY: cy - height / 2 + lineHeight / 2 + fontSize * exports.BASELINE_OFFSET,
        lineHeight,
        align: el.textAlign,
        x: cx - wrapWidth / 2,
        y: cy - height / 2,
        width: wrapWidth,
        height,
        wrapWidth,
    };
}
/** Height a container needs to hold its text - used to grow a shape (and
 *  a text element) as someone types into it. */
function requiredHeight(el, measure) {
    const layout = elementTextLayout(el, measure);
    if (!layout)
        return el.h;
    if (el.type === 'text')
        return layout.height;
    return layout.height + text_1.TEXT_PADDING * 2 * (el.type === 'diamond' ? 2.2 : 1);
}

  };
  modules["src/lib/align.ts"] = function (module, exports, require) {
"use strict";
// Arranging a selection: align, distribute, flip and z-order. Each
// function takes the whole element list plus the selected ids and returns
// a new list in the same order (except the z-order ones, which reorder by
// definition), so the caller never has to splice anything itself.
Object.defineProperty(exports, "__esModule", { value: true });
exports.selectionBounds = void 0;
exports.alignElements = alignElements;
exports.distributeElements = distributeElements;
exports.flipElements = flipElements;
exports.reorder = reorder;
exports.elementsInFrame = elementsInFrame;
exports.expandSelection = expandSelection;
const geometry_1 = require("./geometry");
const pick = (elements, ids) => elements.filter((el) => ids.has(el.id) && !el.locked);
function replace(elements, moved) {
    if (moved.size === 0)
        return elements;
    return elements.map((el) => moved.get(el.id) ?? el);
}
function alignElements(elements, ids, mode) {
    const selected = pick(elements, ids);
    if (selected.length < 2)
        return elements;
    const target = (0, geometry_1.boundsOfElements)(selected);
    const moved = new Map();
    for (const el of selected) {
        const b = (0, geometry_1.rotatedBounds)(el);
        let dx = 0;
        let dy = 0;
        if (mode === 'left')
            dx = target.x - b.x;
        else if (mode === 'right')
            dx = target.x + target.w - (b.x + b.w);
        else if (mode === 'center-x')
            dx = target.x + target.w / 2 - (b.x + b.w / 2);
        else if (mode === 'top')
            dy = target.y - b.y;
        else if (mode === 'bottom')
            dy = target.y + target.h - (b.y + b.h);
        else if (mode === 'center-y')
            dy = target.y + target.h / 2 - (b.y + b.h / 2);
        if (dx || dy)
            moved.set(el.id, (0, geometry_1.translateElement)(el, dx, dy));
    }
    return replace(elements, moved);
}
/** Equal gaps between neighbours, outermost two left where they are. */
function distributeElements(elements, ids, axis) {
    const selected = pick(elements, ids);
    if (selected.length < 3)
        return elements;
    const horizontal = axis === 'horizontal';
    const withBounds = selected
        .map((el) => ({ el, b: (0, geometry_1.rotatedBounds)(el) }))
        .sort((a, b) => (horizontal ? a.b.x - b.b.x : a.b.y - b.b.y));
    const first = withBounds[0].b;
    const last = withBounds[withBounds.length - 1].b;
    const span = horizontal ? last.x + last.w - first.x : last.y + last.h - first.y;
    const used = withBounds.reduce((sum, e) => sum + (horizontal ? e.b.w : e.b.h), 0);
    const gap = (span - used) / (withBounds.length - 1);
    const moved = new Map();
    let cursor = horizontal ? first.x : first.y;
    for (const { el, b } of withBounds) {
        const current = horizontal ? b.x : b.y;
        const delta = cursor - current;
        if (delta)
            moved.set(el.id, (0, geometry_1.translateElement)(el, horizontal ? delta : 0, horizontal ? 0 : delta));
        cursor += (horizontal ? b.w : b.h) + gap;
    }
    return replace(elements, moved);
}
function flipElements(elements, ids, axis) {
    const selected = pick(elements, ids);
    if (!selected.length)
        return elements;
    const box = (0, geometry_1.boundsOfElements)(selected);
    const to = axis === 'horizontal'
        ? { x: box.x + box.w, y: box.y, w: -box.w, h: box.h }
        : { x: box.x, y: box.y + box.h, w: box.w, h: -box.h };
    const moved = new Map();
    for (const el of selected)
        moved.set(el.id, (0, geometry_1.transformElement)(el, box, to));
    return replace(elements, moved);
}
function reorder(elements, ids, mode) {
    if (!ids.size)
        return elements;
    const moving = elements.filter((el) => ids.has(el.id));
    if (!moving.length)
        return elements;
    const rest = elements.filter((el) => !ids.has(el.id));
    if (mode === 'front')
        return [...rest, ...moving];
    if (mode === 'back')
        return [...moving, ...rest];
    const out = elements.slice();
    if (mode === 'forward') {
        for (let i = out.length - 2; i >= 0; i--) {
            if (ids.has(out[i].id) && !ids.has(out[i + 1].id)) {
                const tmp = out[i];
                out[i] = out[i + 1];
                out[i + 1] = tmp;
            }
        }
    }
    else {
        for (let i = 1; i < out.length; i++) {
            if (ids.has(out[i].id) && !ids.has(out[i - 1].id)) {
                const tmp = out[i];
                out[i] = out[i - 1];
                out[i - 1] = tmp;
            }
        }
    }
    return out;
}
/* ----------------------------------------------------------- frames */
/** Elements whose box sits inside the frame - what a frame carries when
 *  it is dragged, and what "export this frame" contains. */
function elementsInFrame(elements, frame) {
    const fb = (0, geometry_1.rotatedBounds)(frame);
    return elements.filter((el) => {
        if (el.id === frame.id || el.type === 'frame')
            return false;
        const b = (0, geometry_1.rotatedBounds)(el);
        const cx = b.x + b.w / 2;
        const cy = b.y + b.h / 2;
        return cx >= fb.x && cx <= fb.x + fb.w && cy >= fb.y && cy <= fb.y + fb.h;
    });
}
/** Expands `ids` with everything the selection implies: whole groups, and
 *  the contents of any selected frame. */
function expandSelection(elements, ids) {
    const out = new Set(ids);
    const groups = new Set();
    for (const el of elements)
        if (ids.has(el.id) && el.groupId)
            groups.add(el.groupId);
    for (const el of elements)
        if (el.groupId && groups.has(el.groupId))
            out.add(el.id);
    for (const el of elements) {
        if (el.type === 'frame' && out.has(el.id))
            for (const child of elementsInFrame(elements, el))
                out.add(child.id);
    }
    return out;
}
const selectionBounds = (elements, ids) => (0, geometry_1.unionBounds)(elements.filter((el) => ids.has(el.id)).map(geometry_1.rotatedBounds));
exports.selectionBounds = selectionBounds;

  };
  modules["src/canvas/render.ts"] = function (module, exports, require) {
"use strict";
// The renderer: one full redraw of the visible scene into a 2D context.
// It owns no state - everything it needs arrives in a RenderScene - so
// the same function draws the live board, the minimap and an exported
// PNG, and a test could hand it a stub context.
//
// Speed comes from three things and no cleverness: culling to the visible
// rectangle, drawing in scene coordinates under one transform (so stroke
// widths need no per-element maths), and a font string cache (setting
// ctx.font is the single most expensive call in a text-heavy board).
Object.defineProperty(exports, "__esModule", { value: true });
exports.pathDataFor = exports.GRID_SIZE = void 0;
exports.setFont = setFont;
exports.measureWith = measureWith;
exports.drawElement = drawElement;
exports.drawElementText = drawElementText;
exports.renderScene = renderScene;
exports.renderExport = renderExport;
const types_1 = require("../lib/types");
const geometry_1 = require("../lib/geometry");
const shapes_1 = require("../lib/shapes");
const textLayout_1 = require("../lib/textLayout");
exports.GRID_SIZE = 20;
/* ------------------------------------------------------- measuring */
const fontCache = new WeakMap();
function setFont(ctx, fontSize, family) {
    const font = `${fontSize}px ${types_1.FONT_STACKS[family]}`;
    if (fontCache.get(ctx) === font)
        return;
    ctx.font = font;
    fontCache.set(ctx, font);
}
/** A measurer bound to a context - the exact widths the browser will use,
 *  which is what keeps the textarea overlay on top of the drawn text. */
function measureWith(ctx) {
    const cache = new Map();
    return (text, fontSize, family) => {
        const key = `${fontSize}|${family}|${text}`;
        const hit = cache.get(key);
        if (hit !== undefined)
            return hit;
        setFont(ctx, fontSize, family);
        const width = ctx.measureText(text).width;
        if (cache.size > 4000)
            cache.clear();
        cache.set(key, width);
        return width;
    };
}
/* ------------------------------------------------------------ paths */
function roundRectPath(ctx, x, y, w, h, r) {
    const radius = Math.max(0, Math.min(r, Math.min(w, h) / 2));
    ctx.beginPath();
    if (radius === 0) {
        ctx.rect(x, y, w, h);
        return;
    }
    ctx.moveTo(x + radius, y);
    ctx.arcTo(x + w, y, x + w, y + h, radius);
    ctx.arcTo(x + w, y + h, x, y + h, radius);
    ctx.arcTo(x, y + h, x, y, radius);
    ctx.arcTo(x, y, x + w, y, radius);
    ctx.closePath();
}
function elementPath(ctx, el) {
    if (el.type === 'ellipse') {
        ctx.beginPath();
        ctx.ellipse(el.x + el.w / 2, el.y + el.h / 2, Math.abs(el.w) / 2, Math.abs(el.h) / 2, 0, 0, Math.PI * 2);
        return;
    }
    if (el.type === 'diamond') {
        const pts = (0, geometry_1.diamondPoints)(el);
        ctx.beginPath();
        ctx.moveTo(pts[0][0], pts[0][1]);
        for (const p of pts.slice(1))
            ctx.lineTo(p[0], p[1]);
        ctx.closePath();
        return;
    }
    if ((0, types_1.isLinear)(el)) {
        ctx.beginPath();
        for (const c of (0, shapes_1.linearCommands)(el)) {
            if (c[0] === 'M')
                ctx.moveTo(c[1], c[2]);
            else if (c[0] === 'L')
                ctx.lineTo(c[1], c[2]);
            else if (c[0] === 'C')
                ctx.bezierCurveTo(c[1], c[2], c[3], c[4], c[5], c[6]);
        }
        return;
    }
    roundRectPath(ctx, el.x, el.y, el.w, el.h, el.type === 'sticky' ? 3 : (0, shapes_1.cornerRadius)(el));
}
function fillHatched(ctx, el, colour) {
    const b = (0, geometry_1.expandBounds)((0, geometry_1.rotatedBounds)(el), 8);
    ctx.save();
    ctx.clip();
    ctx.strokeStyle = colour;
    ctx.lineWidth = Math.max(0.8, el.strokeWidth * 0.45);
    ctx.setLineDash([]);
    ctx.beginPath();
    const step = 8;
    const start = b.x - b.h;
    for (let x = start; x <= b.x + b.w; x += step) {
        ctx.moveTo(x, b.y);
        ctx.lineTo(x + b.h, b.y + b.h);
    }
    ctx.stroke();
    ctx.restore();
}
/* -------------------------------------------------------- elements */
function drawElement(ctx, el, palette, measure) {
    ctx.save();
    ctx.globalAlpha = el.opacity;
    if (el.angle) {
        const cx = el.x + el.w / 2;
        const cy = el.y + el.h / 2;
        ctx.translate(cx, cy);
        ctx.rotate(el.angle);
        ctx.translate(-cx, -cy);
    }
    const filled = el.fillStyle !== 'none' && el.fill && el.fill !== 'transparent';
    const stroked = el.stroke && el.stroke !== 'transparent' && el.type !== 'text';
    if (el.type === 'frame') {
        ctx.strokeStyle = palette.frameStroke;
        ctx.lineWidth = 1.5;
        ctx.setLineDash([]);
        roundRectPath(ctx, el.x, el.y, el.w, el.h, 4);
        ctx.stroke();
    }
    else if (el.type !== 'text') {
        elementPath(ctx, el);
        if (filled) {
            if (el.fillStyle === 'hatch') {
                fillHatched(ctx, el, el.fill);
            }
            else {
                ctx.fillStyle = el.fill;
                if (el.type === 'sticky') {
                    ctx.shadowColor = 'rgba(0,0,0,0.18)';
                    ctx.shadowBlur = 8;
                    ctx.shadowOffsetY = 2;
                }
                ctx.fill();
                ctx.shadowColor = 'transparent';
                ctx.shadowBlur = 0;
                ctx.shadowOffsetY = 0;
            }
        }
        if (stroked) {
            ctx.strokeStyle = el.stroke;
            ctx.lineWidth = el.strokeWidth;
            ctx.lineCap = 'round';
            ctx.lineJoin = 'round';
            ctx.setLineDash((0, shapes_1.dashPattern)(el.strokeStyle, el.strokeWidth));
            ctx.stroke();
            ctx.setLineDash([]);
        }
    }
    if ((0, types_1.isLinear)(el) && stroked) {
        const { start, end } = (0, shapes_1.headsFor)(el);
        ctx.strokeStyle = el.stroke;
        ctx.fillStyle = el.stroke;
        ctx.lineWidth = el.strokeWidth;
        for (const head of [start, end]) {
            if (!head)
                continue;
            if (head.kind === 'dot') {
                ctx.beginPath();
                ctx.arc(head.center[0], head.center[1], head.radius, 0, Math.PI * 2);
                ctx.fill();
            }
            else {
                ctx.beginPath();
                ctx.moveTo(head.points[0][0], head.points[0][1]);
                for (const p of head.points.slice(1))
                    ctx.lineTo(p[0], p[1]);
                ctx.stroke();
            }
        }
    }
    drawElementText(ctx, el, palette, measure);
    ctx.restore();
}
function drawElementText(ctx, el, palette, measure) {
    const layout = (0, textLayout_1.elementTextLayout)(el, measure);
    if (!layout)
        return;
    setFont(ctx, el.fontSize, el.fontFamily);
    ctx.textAlign = layout.align === 'left' ? 'left' : layout.align === 'right' ? 'right' : 'center';
    ctx.textBaseline = 'alphabetic';
    if ((0, types_1.isLinear)(el)) {
        // A label on a connector gets the paper colour behind it so the line
        // does not run through the words.
        ctx.fillStyle = palette.background;
        roundRectPath(ctx, layout.x - 5, layout.y - 2, layout.width + 10, layout.height + 4, 4);
        ctx.fill();
    }
    ctx.fillStyle = el.type === 'frame' ? palette.textDim : el.textColor;
    for (let i = 0; i < layout.lines.length; i++) {
        ctx.fillText(layout.lines[i], layout.anchorX, layout.firstBaselineY + i * layout.lineHeight);
    }
}
/* ------------------------------------------------------ background */
function drawBackground(ctx, scene) {
    const { palette, viewport: v, width, height } = scene;
    if (scene.paintBackground !== false) {
        ctx.fillStyle = palette.background;
        ctx.fillRect(0, 0, width, height);
    }
    if (scene.background === 'plain' || scene.chrome === false)
        return;
    const size = (scene.gridSize ?? exports.GRID_SIZE) * v.zoom;
    if (size < 6)
        return;
    const offsetX = -((v.scrollX * v.zoom) % size);
    const offsetY = -((v.scrollY * v.zoom) % size);
    const firstIndexX = Math.floor((v.scrollX * v.zoom) / size) + 1;
    const firstIndexY = Math.floor((v.scrollY * v.zoom) / size) + 1;
    if (scene.background === 'dots') {
        ctx.fillStyle = palette.gridStrong;
        for (let x = offsetX, i = firstIndexX; x < width; x += size, i++) {
            for (let y = offsetY, j = firstIndexY; y < height; y += size, j++) {
                const major = i % 5 === 0 && j % 5 === 0;
                ctx.globalAlpha = major ? 1 : 0.55;
                ctx.fillRect(Math.round(x), Math.round(y), major ? 2 : 1.4, major ? 2 : 1.4);
            }
        }
        ctx.globalAlpha = 1;
        return;
    }
    ctx.lineWidth = 1;
    for (let pass = 0; pass < 2; pass++) {
        ctx.strokeStyle = pass === 0 ? palette.grid : palette.gridStrong;
        ctx.beginPath();
        for (let x = offsetX, i = firstIndexX; x < width; x += size, i++) {
            if ((i % 5 === 0) !== (pass === 1))
                continue;
            const px = Math.round(x) + 0.5;
            ctx.moveTo(px, 0);
            ctx.lineTo(px, height);
        }
        for (let y = offsetY, j = firstIndexY; y < height; y += size, j++) {
            if ((j % 5 === 0) !== (pass === 1))
                continue;
            const py = Math.round(y) + 0.5;
            ctx.moveTo(0, py);
            ctx.lineTo(width, py);
        }
        ctx.stroke();
    }
}
/* --------------------------------------------------------- overlays */
const HANDLE = 9;
function strokeScreenRect(ctx, b, colour, dash = []) {
    ctx.strokeStyle = colour;
    ctx.lineWidth = 1.5;
    ctx.setLineDash(dash);
    ctx.strokeRect(Math.round(b.x) + 0.5, Math.round(b.y) + 0.5, Math.round(b.w), Math.round(b.h));
    ctx.setLineDash([]);
}
function drawSelectionChrome(ctx, scene) {
    const { palette, viewport: v } = scene;
    const selected = scene.elements.filter((el) => scene.selectedIds.has(el.id));
    if (!selected.length)
        return;
    const single = selected.length === 1 ? selected[0] : null;
    if (single && !single.locked) {
        const b = { x: single.x, y: single.y, w: single.w, h: single.h };
        ctx.save();
        const [cx, cy] = (0, geometry_1.sceneToCanvas)([single.x + single.w / 2, single.y + single.h / 2], v);
        ctx.translate(cx, cy);
        ctx.rotate(single.angle);
        const w = single.w * v.zoom;
        const h = single.h * v.zoom;
        strokeScreenRect(ctx, { x: -w / 2 - 3, y: -h / 2 - 3, w: w + 6, h: h + 6 }, palette.selection);
        ctx.restore();
        if (!(0, types_1.isLinear)(single)) {
            ctx.fillStyle = palette.surface;
            ctx.strokeStyle = palette.selection;
            ctx.lineWidth = 1.5;
            for (const { point } of (0, geometry_1.handlePositions)(b, single.angle)) {
                const [hx, hy] = (0, geometry_1.sceneToCanvas)(point, v);
                ctx.beginPath();
                ctx.rect(hx - HANDLE / 2, hy - HANDLE / 2, HANDLE, HANDLE);
                ctx.fill();
                ctx.stroke();
            }
            const [rx, ry] = (0, geometry_1.sceneToCanvas)((0, geometry_1.rotateHandlePosition)(b, single.angle, 28 / v.zoom), v);
            ctx.beginPath();
            ctx.arc(rx, ry, HANDLE / 2 + 0.5, 0, Math.PI * 2);
            ctx.fill();
            ctx.stroke();
        }
        else {
            ctx.fillStyle = palette.surface;
            ctx.strokeStyle = palette.selection;
            ctx.lineWidth = 1.5;
            for (const p of (0, geometry_1.linearPathPoints)(single)) {
                const [hx, hy] = (0, geometry_1.sceneToCanvas)(p, v);
                ctx.beginPath();
                ctx.arc(hx, hy, HANDLE / 2, 0, Math.PI * 2);
                ctx.fill();
                ctx.stroke();
            }
        }
        return;
    }
    for (const el of selected) {
        const b = (0, geometry_1.rotatedBounds)(el);
        const [x, y] = (0, geometry_1.sceneToCanvas)([b.x, b.y], v);
        strokeScreenRect(ctx, { x: x - 2, y: y - 2, w: b.w * v.zoom + 4, h: b.h * v.zoom + 4 }, palette.selection, [4, 4]);
    }
    const all = selected.map(geometry_1.rotatedBounds);
    const minX = Math.min(...all.map((b) => b.x));
    const minY = Math.min(...all.map((b) => b.y));
    const maxX = Math.max(...all.map((b) => b.x + b.w));
    const maxY = Math.max(...all.map((b) => b.y + b.h));
    const [sx, sy] = (0, geometry_1.sceneToCanvas)([minX, minY], v);
    const box = { x: sx - 4, y: sy - 4, w: (maxX - minX) * v.zoom + 8, h: (maxY - minY) * v.zoom + 8 };
    strokeScreenRect(ctx, box, palette.selection);
    ctx.fillStyle = palette.surface;
    ctx.strokeStyle = palette.selection;
    ctx.lineWidth = 1.5;
    for (const { point } of (0, geometry_1.handlePositions)({ x: minX, y: minY, w: maxX - minX, h: maxY - minY }, 0)) {
        const [hx, hy] = (0, geometry_1.sceneToCanvas)(point, v);
        ctx.beginPath();
        ctx.rect(hx - HANDLE / 2, hy - HANDLE / 2, HANDLE, HANDLE);
        ctx.fill();
        ctx.stroke();
    }
}
function drawLockBadges(ctx, scene) {
    const { palette, viewport: v } = scene;
    for (const el of scene.elements) {
        if (!el.locked || !scene.selectedIds.has(el.id))
            continue;
        const b = (0, geometry_1.rotatedBounds)(el);
        const [x, y] = (0, geometry_1.sceneToCanvas)([b.x, b.y], v);
        strokeScreenRect(ctx, { x: x - 2, y: y - 2, w: b.w * v.zoom + 4, h: b.h * v.zoom + 4 }, palette.textDim, [2, 3]);
    }
}
/* ---------------------------------------------------------- scene */
function renderScene(ctx, scene) {
    const { viewport: v, palette } = scene;
    const measure = measureWith(ctx);
    ctx.save();
    drawBackground(ctx, scene);
    const view = (0, geometry_1.expandBounds)((0, geometry_1.visibleBounds)(v, scene.width, scene.height), 64 / v.zoom);
    ctx.save();
    ctx.translate(-v.scrollX * v.zoom, -v.scrollY * v.zoom);
    ctx.scale(v.zoom, v.zoom);
    const frames = [];
    const rest = [];
    for (const el of scene.elements) {
        if (el.id === scene.editingId)
            continue;
        const b = (0, geometry_1.rotatedBounds)(el);
        const padded = el.type === 'frame' ? (0, geometry_1.expandBounds)(b, 40) : (0, geometry_1.expandBounds)(b, el.strokeWidth + 4);
        if (!(0, geometry_1.boundsIntersect)(padded, view))
            continue;
        (el.type === 'frame' ? frames : rest).push(el);
    }
    for (const el of frames)
        drawElement(ctx, el, palette, measure);
    for (const el of rest)
        drawElement(ctx, el, palette, measure);
    if (scene.bindHighlightId) {
        const target = scene.elements.find((el) => el.id === scene.bindHighlightId);
        if (target) {
            ctx.save();
            ctx.strokeStyle = palette.accent;
            ctx.lineWidth = 2 / v.zoom;
            ctx.setLineDash([6 / v.zoom, 4 / v.zoom]);
            const b = (0, geometry_1.expandBounds)((0, geometry_1.rotatedBounds)(target), 6 / v.zoom);
            ctx.strokeRect(b.x, b.y, b.w, b.h);
            ctx.restore();
        }
    }
    ctx.restore();
    if (scene.chrome !== false) {
        for (const line of scene.snapLines ?? []) {
            ctx.save();
            ctx.strokeStyle = palette.accent;
            ctx.lineWidth = 1;
            ctx.setLineDash([4, 4]);
            ctx.beginPath();
            if (line.x !== undefined) {
                const [x, y1] = (0, geometry_1.sceneToCanvas)([line.x, line.from], v);
                const [, y2] = (0, geometry_1.sceneToCanvas)([line.x, line.to], v);
                ctx.moveTo(Math.round(x) + 0.5, y1);
                ctx.lineTo(Math.round(x) + 0.5, y2);
            }
            else if (line.y !== undefined) {
                const [x1, y] = (0, geometry_1.sceneToCanvas)([line.from, line.y], v);
                const [x2] = (0, geometry_1.sceneToCanvas)([line.to, line.y], v);
                ctx.moveTo(x1, Math.round(y) + 0.5);
                ctx.lineTo(x2, Math.round(y) + 0.5);
            }
            ctx.stroke();
            ctx.restore();
        }
        drawSelectionChrome(ctx, scene);
        drawLockBadges(ctx, scene);
        if (scene.marquee) {
            const b = (0, geometry_1.normalizeBounds)(scene.marquee);
            const [x, y] = (0, geometry_1.sceneToCanvas)([b.x, b.y], v);
            const screen = { x, y, w: b.w * v.zoom, h: b.h * v.zoom };
            ctx.fillStyle = palette.accentSoft;
            ctx.globalAlpha = 0.5;
            ctx.fillRect(screen.x, screen.y, screen.w, screen.h);
            ctx.globalAlpha = 1;
            strokeScreenRect(ctx, screen, palette.selection);
        }
    }
    ctx.restore();
}
/** Draws the elements alone, framed on `bounds` - what PNG export uses. */
function renderExport(ctx, elements, bounds, palette, scale, background) {
    ctx.save();
    ctx.scale(scale, scale);
    if (background) {
        ctx.fillStyle = background;
        ctx.fillRect(0, 0, bounds.w, bounds.h);
    }
    ctx.translate(-bounds.x, -bounds.y);
    const measure = measureWith(ctx);
    const frames = elements.filter((el) => el.type === 'frame');
    const rest = elements.filter((el) => el.type !== 'frame');
    for (const el of [...frames, ...rest])
        drawElement(ctx, el, palette, measure);
    ctx.restore();
}
const pathDataFor = (el) => (0, shapes_1.commandsToPathData)((0, shapes_1.linearCommands)(el));
exports.pathDataFor = pathDataFor;

  };
  modules["src/ui/icons.tsx"] = function (module, exports, require) {
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Icon = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// Every icon in the app, as inline SVG on a 24x24 grid, stroked in
// currentColor so one component works in both themes and on every button
// state. No icon font, no emoji: an emoji is somebody else's drawing at
// somebody else's size, and it cannot inherit a focus ring's colour.
const react_1 = __importDefault(require("react"));
const P = (d, key) => (0, jsx_runtime_1.jsx)("path", { d: d }, key);
const PATHS = {
    select: P('M5.5 3.2 18 11.2l-5.4 1.1-2.4 5.1z'),
    hand: P('M8 11V5.6a1.4 1.4 0 0 1 2.8 0V11m0-.4V4.6a1.4 1.4 0 0 1 2.8 0V11m0-.6V6a1.4 1.4 0 0 1 2.8 0v6.4c0 3.6-2 6.6-5.3 6.6-2.6 0-3.9-1.3-5.2-3.6l-1.6-2.9a1.4 1.4 0 0 1 2.3-1.6L8 12.4'),
    rect: P('M4 5.5h16v13H4z'),
    ellipse: P('M12 5.5c4.4 0 8 2.9 8 6.5s-3.6 6.5-8 6.5-8-2.9-8-6.5 3.6-6.5 8-6.5z'),
    diamond: P('m12 4 8 8-8 8-8-8z'),
    line: P('M4 19 20 5'),
    arrow: [P('M4 19 19 5', 'a'), P('M12.5 5H19v6.5', 'b')],
    draw: P('M4 17.5c3-1 4-8 6.2-8 1.6 0 .6 5 2.2 5 1.3 0 2-4.5 3.4-4.5 1.6 0 1.4 6.4 4.2 6.4'),
    text: [P('M5 6.5V4.8h14v1.7', 'a'), P('M12 5v14', 'b'), P('M9 19h6', 'c')],
    sticky: [P('M4.5 4.5h15v9.5l-5.5 5.5h-9.5z', 'a'), P('M19.5 14H14v5.5', 'b')],
    frame: [P('M7 3v18M17 3v18', 'a'), P('M3 7h18M3 17h18', 'b')],
    eraser: [P('m9.5 19-5-5a1.6 1.6 0 0 1 0-2.3l7.2-7.2a1.6 1.6 0 0 1 2.3 0l4.6 4.6a1.6 1.6 0 0 1 0 2.3L11.8 19z', 'a'), P('M9.5 19H19', 'b')],
    undo: [P('M4 9h10a5.5 5.5 0 0 1 0 11H8', 'a'), P('m8 4.5-4 4.5 4 4.5', 'b')],
    redo: [P('M20 9H10a5.5 5.5 0 0 0 0 11h6', 'a'), P('m16 4.5 4 4.5-4 4.5', 'b')],
    'zoom-in': [P('M11 4.5a6.5 6.5 0 1 1 0 13 6.5 6.5 0 0 1 0-13z', 'a'), P('m16 16 4 4', 'b'), P('M8.5 11h5M11 8.5v5', 'c')],
    'zoom-out': [P('M11 4.5a6.5 6.5 0 1 1 0 13 6.5 6.5 0 0 1 0-13z', 'a'), P('m16 16 4 4', 'b'), P('M8.5 11h5', 'c')],
    fit: P('M4 9V4.5h4.5M20 9V4.5h-4.5M4 15v4.5h4.5M20 15v4.5h-4.5'),
    map: [P('M4 6.5 9.5 4.5 15 6.5 20 4.5v13L15 19.5 9.5 17.5 4 19.5z', 'a'), P('M9.5 4.5v13M15 6.5v13', 'b')],
    grid: [P('M4 4.5h16v15H4z', 'a'), P('M9.3 4.5v15M14.7 4.5v15M4 9.5h16M4 14.5h16', 'b')],
    magnet: [P('M6 4.5v7a6 6 0 0 0 12 0v-7h-4v7a2 2 0 0 1-4 0v-7z', 'a'), P('M6 8.5h4M14 8.5h4', 'b')],
    lock: [P('M6 10.5h12v9H6z', 'a'), P('M8.8 10.5V7.8a3.2 3.2 0 0 1 6.4 0v2.7', 'b')],
    unlock: [P('M6 10.5h12v9H6z', 'a'), P('M8.8 10.5V7.8a3.2 3.2 0 0 1 6.2-1.1', 'b')],
    trash: [P('M5 6.5h14', 'a'), P('M7.5 6.5 8.3 20h7.4l.8-13.5', 'b'), P('M10 6.5V4h4v2.5', 'c')],
    copy: [P('M8.5 8.5h11v11h-11z', 'a'), P('M15.5 5.5v-1h-11v11h1', 'b')],
    group: [P('M7 7h10v10H7z', 'a'), P('M4 4h2.5M17.5 4H20M4 20h2.5M17.5 20H20M4 4v2.5M4 17.5V20M20 4v2.5M20 17.5V20', 'b')],
    ungroup: [P('M4 4h8v8H4z', 'a'), P('M12 12h8v8h-8z', 'b')],
    'to-front': [P('M8 4.5h11v11H8z', 'a'), P('M15.5 19.5h-11v-11', 'b')],
    'to-back': [P('M4.5 8.5h11v11h-11z', 'a'), P('M8.5 4.5h11v11', 'b')],
    forward: [P('M12 3.5 20 8l-8 4.5L4 8z', 'a'), P('m4 14 8 4.5 8-4.5', 'b')],
    backward: [P('M12 11.5 20 16l-8 4.5L4 16z', 'a'), P('m4 10 8-4.5L20 10', 'b')],
    'align-left': [P('M4 3.5v17', 'a'), P('M6.5 7h12v3.5h-12zM6.5 13.5h8V17h-8z', 'b')],
    'align-center-x': [P('M12 3.5v17', 'a'), P('M6 7h12v3.5H6zM8 13.5h8V17H8z', 'b')],
    'align-right': [P('M20 3.5v17', 'a'), P('M5.5 7h12v3.5h-12zM9.5 13.5h8V17h-8z', 'b')],
    'align-top': [P('M3.5 4h17', 'a'), P('M7 6.5h3.5v12H7zM13.5 6.5H17v8h-3.5z', 'b')],
    'align-center-y': [P('M3.5 12h17', 'a'), P('M7 6h3.5v12H7zM13.5 8H17v8h-3.5z', 'b')],
    'align-bottom': [P('M3.5 20h17', 'a'), P('M7 5.5h3.5v12H7zM13.5 9.5H17v8h-3.5z', 'b')],
    'distribute-x': [P('M3.5 4v16M20.5 4v16', 'a'), P('M9.5 7h5v10h-5z', 'b')],
    'distribute-y': [P('M4 3.5h16M4 20.5h16', 'a'), P('M7 9.5h10v5H7z', 'b')],
    'flip-x': [P('M12 3.5v17', 'a'), P('M9.5 7 4 12l5.5 5zM14.5 7l5.5 5-5.5 5z', 'b')],
    'flip-y': [P('M3.5 12h17', 'a'), P('M7 9.5 12 4l5 5.5zM7 14.5 12 20l5-5.5z', 'b')],
    download: [P('M12 4v11', 'a'), P('m7.5 10.5 4.5 4.5 4.5-4.5', 'b'), P('M4.5 19.5h15', 'c')],
    image: [P('M4 5.5h16v13H4z', 'a'), P('m4 15 4.5-4.5 4 4 3-2.5L20 16', 'b'), P('M9 9.5a1.2 1.2 0 1 0 0-.1z', 'c')],
    code: P('m9 7-5 5 5 5M15 7l5 5-5 5'),
    file: [P('M6 3.5h7.5L18 8v12.5H6z', 'a'), P('M13.5 3.5V8H18', 'b')],
    files: [P('M4.5 7.5h6l1.5 2h7.5v10h-15z', 'a'), P('M4.5 7.5v-3h5l1.5 2', 'b')],
    plus: P('M12 5v14M5 12h14'),
    check: P('m5 12.5 4.5 4.5L19 7'),
    close: P('M6 6l12 12M18 6 6 18'),
    help: [P('M12 3.5a8.5 8.5 0 1 1 0 17 8.5 8.5 0 0 1 0-17z', 'a'), P('M9.4 9.4a2.6 2.6 0 1 1 3.4 2.5c-.6.2-.8.7-.8 1.3v.6', 'b'), P('M12 17.2v.1', 'c')],
    search: [P('M11 4.5a6.5 6.5 0 1 1 0 13 6.5 6.5 0 0 1 0-13z', 'a'), P('m16 16 4 4', 'b')],
    cloud: P('M7 18.5a4 4 0 0 1-.4-8A5.5 5.5 0 0 1 17.4 10a3.9 3.9 0 0 1 .4 7.8z'),
    library: [P('M4 4.5h6v6H4zM14 4.5h6v6h-6zM4 13.5h6v6H4zM14 13.5h6v6h-6z', 'a')],
    more: [P('M6 12h.1', 'a'), P('M12 12h.1', 'b'), P('M18 12h.1', 'c')],
    pencil: [P('M4.5 19.5h4L19 9a2.1 2.1 0 0 0-3-3L5 16.5z', 'a'), P('m14.5 7.5 2 2', 'b')],
    upload: [P('M12 15V4', 'a'), P('m7.5 8.5 4.5-4.5 4.5 4.5', 'b'), P('M4.5 19.5h15', 'c')],
    sparkle: P('M12 4.5 13.6 10l5.4 1.6-5.4 1.6L12 18.6 10.4 13.2 5 11.6 10.4 10z'),
};
exports.Icon = react_1.default.memo(({ name, size = 18, className }) => ((0, jsx_runtime_1.jsx)("svg", { className: className, width: size, height: size, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 1.6, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": "true", focusable: "false", children: PATHS[name] })));
exports.Icon.displayName = 'Icon';

  };
  modules["src/lib/history.ts"] = function (module, exports, require) {
"use strict";
// Undo/redo over whole-scene snapshots, with coalescing so a continuous
// gesture (a drag, a slider, a run of typing) collapses into one step.
//
// Snapshots are cheap because every mutation in this app is structural:
// unchanged elements keep their object identity, so a snapshot is an
// array of mostly-shared references, not a deep copy.
Object.defineProperty(exports, "__esModule", { value: true });
exports.undoLabels = exports.seal = exports.currentSnapshot = exports.canRedo = exports.canUndo = exports.DEFAULT_WINDOW = exports.DEFAULT_LIMIT = void 0;
exports.createHistory = createHistory;
exports.sameSnapshot = sameSnapshot;
exports.record = record;
exports.undo = undo;
exports.redo = redo;
exports.DEFAULT_LIMIT = 200;
exports.DEFAULT_WINDOW = 900;
function createHistory(snapshot, limit = exports.DEFAULT_LIMIT, window = exports.DEFAULT_WINDOW) {
    return { past: [], present: { snapshot, label: 'Open', key: null, at: 0 }, future: [], limit, window };
}
/** Cheap structural comparison: identical arrays of identical references
 *  mean nothing happened, so no history entry is worth making. */
function sameSnapshot(a, b) {
    if (a === b)
        return true;
    if (a.elements.length !== b.elements.length)
        return false;
    for (let i = 0; i < a.elements.length; i++)
        if (a.elements[i] !== b.elements[i])
            return false;
    if (a.selectedIds.length !== b.selectedIds.length)
        return false;
    for (let i = 0; i < a.selectedIds.length; i++)
        if (a.selectedIds[i] !== b.selectedIds[i])
            return false;
    return true;
}
/** Same but ignoring selection: an undo step should not exist purely
 *  because the selection changed. */
function sameElements(a, b) {
    if (a.elements.length !== b.elements.length)
        return false;
    for (let i = 0; i < a.elements.length; i++)
        if (a.elements[i] !== b.elements[i])
            return false;
    return true;
}
function record(history, snapshot, opts = {}) {
    const now = opts.now ?? Date.now();
    const key = opts.coalesceKey ?? null;
    const label = opts.label ?? 'Edit';
    if (sameElements(history.present.snapshot, snapshot)) {
        // Selection-only change: update in place, never a new undo step.
        if (sameSnapshot(history.present.snapshot, snapshot))
            return history;
        return { ...history, present: { ...history.present, snapshot } };
    }
    const canCoalesce = key !== null && history.present.key === key && now - history.present.at <= history.window;
    if (canCoalesce) {
        return { ...history, present: { snapshot, label, key, at: now }, future: [] };
    }
    const past = [...history.past, history.present];
    while (past.length > history.limit)
        past.shift();
    return { ...history, past, present: { snapshot, label, key, at: now }, future: [] };
}
const canUndo = (h) => h.past.length > 0;
exports.canUndo = canUndo;
const canRedo = (h) => h.future.length > 0;
exports.canRedo = canRedo;
function undo(history) {
    if (!history.past.length)
        return history;
    const past = history.past.slice();
    const previous = past.pop();
    return { ...history, past, present: previous, future: [history.present, ...history.future] };
}
function redo(history) {
    if (!history.future.length)
        return history;
    const [next, ...rest] = history.future;
    return { ...history, past: [...history.past, history.present], present: next, future: rest };
}
const currentSnapshot = (h) => h.present.snapshot;
exports.currentSnapshot = currentSnapshot;
/** Ends the current coalescing run so the next edit always starts a new
 *  step (called on pointer-up, blur, and tool changes). */
const seal = (history) => ({ ...history, present: { ...history.present, key: null } });
exports.seal = seal;
/** Undo step labels, newest first - what a history menu would show. */
const undoLabels = (h) => h.past.map((e) => e.label).reverse();
exports.undoLabels = undoLabels;

  };
  modules["src/ui/Modal.tsx"] = function (module, exports, require) {
"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Modal = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
// One dialog shell for the file switcher, the export sheet, the shortcut
// list and the shape palette: a labelled dialog, Escape to close, focus
// moved in on open and returned on close, and Tab kept inside while it is
// open. Written once so every dialog behaves the same way.
const react_1 = __importStar(require("react"));
const icons_1 = require("./icons");
const FOCUSABLE = 'a[href], button:not([disabled]), input:not([disabled]), select, textarea, [tabindex]:not([tabindex="-1"])';
const Modal = ({ title, description, onClose, children, footer, wide }) => {
    const ref = (0, react_1.useRef)(null);
    const returnTo = (0, react_1.useRef)(null);
    (0, react_1.useEffect)(() => {
        returnTo.current = document.activeElement;
        const node = ref.current;
        const firstInput = node?.querySelector('input, textarea');
        (firstInput ?? node?.querySelector(FOCUSABLE) ?? node)?.focus();
        return () => returnTo.current?.focus?.();
    }, []);
    const onKeyDown = (0, react_1.useCallback)((e) => {
        e.stopPropagation();
        if (e.key === 'Escape') {
            e.preventDefault();
            onClose();
            return;
        }
        if (e.key !== 'Tab')
            return;
        const nodes = Array.from(ref.current?.querySelectorAll(FOCUSABLE) ?? []).filter((n) => n.offsetParent !== null);
        if (!nodes.length)
            return;
        const firstNode = nodes[0];
        const lastNode = nodes[nodes.length - 1];
        if (e.shiftKey && document.activeElement === firstNode) {
            e.preventDefault();
            lastNode.focus();
        }
        else if (!e.shiftKey && document.activeElement === lastNode) {
            e.preventDefault();
            firstNode.focus();
        }
    }, [onClose]);
    return ((0, jsx_runtime_1.jsx)("div", { className: "sk-backdrop", onPointerDown: (e) => e.target === e.currentTarget && onClose(), children: (0, jsx_runtime_1.jsxs)("div", { className: `sk-modal${wide ? ' is-wide' : ''}`, role: "dialog", "aria-modal": "true", "aria-label": title, ref: ref, tabIndex: -1, onKeyDown: onKeyDown, children: [(0, jsx_runtime_1.jsxs)("header", { className: "sk-modal-head", children: [(0, jsx_runtime_1.jsxs)("div", { children: [(0, jsx_runtime_1.jsx)("h2", { className: "sk-modal-title", children: title }), description ? (0, jsx_runtime_1.jsx)("p", { className: "sk-modal-desc", children: description }) : null] }), (0, jsx_runtime_1.jsxs)("button", { type: "button", className: "sk-icon-btn", onClick: onClose, title: "Close \u2014 Esc", children: [(0, jsx_runtime_1.jsx)(icons_1.Icon, { name: "close" }), (0, jsx_runtime_1.jsx)("span", { className: "sk-sr", children: "Close" })] })] }), (0, jsx_runtime_1.jsx)("div", { className: "sk-modal-body", children: children }), footer ? (0, jsx_runtime_1.jsx)("footer", { className: "sk-modal-foot", children: footer }) : null] }) }));
};
exports.Modal = Modal;

  };
  modules["src/canvas/png.ts"] = function (module, exports, require) {
"use strict";
// Raster and file output. PNG is drawn by the same renderer the screen
// uses (renderExport), at whatever scale is asked for, so what is
// exported is what was on the board - not a second implementation that
// drifts from it.
Object.defineProperty(exports, "__esModule", { value: true });
exports.blobToBytes = exports.svgBlob = void 0;
exports.renderPngBlob = renderPngBlob;
exports.downloadBlob = downloadBlob;
exports.copyBlobToClipboard = copyBlobToClipboard;
exports.copyTextToClipboard = copyTextToClipboard;
const render_1 = require("./render");
async function renderPngBlob(elements, bounds, opts) {
    const scale = opts.scale ?? 1;
    const canvas = document.createElement('canvas');
    canvas.width = Math.max(1, Math.round(bounds.w * scale));
    canvas.height = Math.max(1, Math.round(bounds.h * scale));
    const ctx = canvas.getContext('2d');
    if (!ctx)
        throw new Error('This browser would not give Sketch a 2D canvas to draw the export on.');
    (0, render_1.renderExport)(ctx, elements, bounds, opts.palette, scale, opts.background ?? null);
    const blob = await new Promise((resolve) => canvas.toBlob(resolve, 'image/png'));
    if (!blob)
        throw new Error('The PNG could not be encoded.');
    return blob;
}
const svgBlob = (svg) => new Blob([svg], { type: 'image/svg+xml;charset=utf-8' });
exports.svgBlob = svgBlob;
function downloadBlob(blob, filename) {
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    document.body.appendChild(a);
    a.click();
    a.remove();
    setTimeout(() => URL.revokeObjectURL(url), 2000);
}
/** True when the clipboard actually took it. Browsers differ wildly here
 *  (Firefox has no image support at the time of writing), so every caller
 *  has to be ready for a no. */
async function copyBlobToClipboard(blob) {
    try {
        const anyWindow = window;
        if (!navigator.clipboard || !anyWindow.ClipboardItem)
            return false;
        const item = new anyWindow.ClipboardItem({ [blob.type]: blob });
        await navigator.clipboard.write([item]);
        return true;
    }
    catch {
        return false;
    }
}
async function copyTextToClipboard(text) {
    try {
        if (!navigator.clipboard?.writeText)
            return false;
        await navigator.clipboard.writeText(text);
        return true;
    }
    catch {
        return false;
    }
}
const blobToBytes = async (blob) => new Uint8Array(await blob.arrayBuffer());
exports.blobToBytes = blobToBytes;

  };
  modules["src/lib/library.ts"] = function (module, exports, require) {
"use strict";
// The shape library: the pieces people reach for over and over, built
// from the same nine element types the tools draw, never from a special
// case in the renderer. A database is a rectangle between two ellipses; a
// cloud is one closed curve; a person is a head and four strokes - each
// returned as a group, so it moves, styles, exports and undoes exactly
// like anything drawn by hand.
//
// Pure: element factories only.
Object.defineProperty(exports, "__esModule", { value: true });
exports.libraryItem = exports.LIBRARY = void 0;
exports.insertLibraryItem = insertLibraryItem;
exports.searchLibrary = searchLibrary;
const document_1 = require("./document");
const layout_1 = require("./layout");
const scalePoints = (points, x, y, w, h) => points.map(([px, py]) => [x + px * w, y + py * h]);
/** One filled, closed curve - what the cloud is made of. */
function closedCurve(points, style, extra = {}) {
    const [ox, oy] = points[0];
    return (0, document_1.createElement)('line', {
        x: ox,
        y: oy,
        edge: 'curved',
        points: points.map(([px, py]) => [px - ox, py - oy]),
        stroke: style.stroke,
        fill: style.fill,
        fillStyle: style.fillStyle === 'none' ? 'solid' : style.fillStyle,
        ...extra,
    });
}
function labelled(type, x, y, w, h, text, style) {
    return (0, document_1.createElement)(type, {
        x,
        y,
        w,
        h,
        text,
        fontSize: style.fontSize,
        textAlign: 'center',
        stroke: style.stroke,
        fill: style.fill,
        fillStyle: 'solid',
        textColor: style.textColor,
    });
}
const CLOUD = [
    [0.1, 0.78],
    [0.03, 0.62],
    [0.09, 0.46],
    [0.22, 0.42],
    [0.24, 0.26],
    [0.38, 0.16],
    [0.54, 0.2],
    [0.65, 0.08],
    [0.81, 0.13],
    [0.88, 0.28],
    [0.97, 0.41],
    [0.94, 0.61],
    [0.85, 0.76],
    [0.66, 0.83],
    [0.4, 0.84],
    [0.19, 0.83],
    [0.1, 0.78],
];
function grouped(elements) {
    if (elements.length < 2)
        return elements;
    const groupId = (0, document_1.newId)('g');
    return elements.map((el) => ({ ...el, groupId }));
}
exports.LIBRARY = [
    {
        id: 'start',
        name: 'Start / End',
        keywords: ['start', 'end', 'terminator', 'flowchart', 'oval'],
        w: 160,
        h: 64,
        build: (x, y, style) => [labelled('ellipse', x, y, 160, 64, 'Start', style)],
    },
    {
        id: 'process',
        name: 'Process',
        keywords: ['process', 'step', 'action', 'flowchart', 'box'],
        w: 180,
        h: 80,
        build: (x, y, style) => [labelled('rect', x, y, 180, 80, 'Process', style)],
    },
    {
        id: 'decision',
        name: 'Decision',
        keywords: ['decision', 'branch', 'if', 'diamond', 'flowchart'],
        w: 190,
        h: 120,
        build: (x, y, style) => [labelled('diamond', x, y, 190, 120, 'Decision?', style)],
    },
    {
        id: 'database',
        name: 'Database',
        keywords: ['database', 'db', 'store', 'cylinder', 'storage'],
        w: 150,
        h: 130,
        build: (x, y, style) => {
            const w = 150;
            const h = 130;
            const cap = 34;
            const body = (0, document_1.createElement)('rect', {
                x,
                y: y + cap / 2,
                w,
                h: h - cap,
                stroke: 'transparent',
                fill: style.fill,
                fillStyle: 'solid',
                roundness: 0,
                text: 'Database',
                fontSize: style.fontSize,
                textColor: style.textColor,
            });
            const bottom = (0, document_1.createElement)('ellipse', { x, y: y + h - cap, w, h: cap, stroke: style.stroke, fill: style.fill, fillStyle: 'solid' });
            const left = (0, document_1.createElement)('line', {
                x,
                y: y + cap / 2,
                points: [
                    [0, 0],
                    [0, h - cap],
                ],
                stroke: style.stroke,
            });
            const right = (0, document_1.createElement)('line', {
                x: x + w,
                y: y + cap / 2,
                points: [
                    [0, 0],
                    [0, h - cap],
                ],
                stroke: style.stroke,
            });
            const top = (0, document_1.createElement)('ellipse', { x, y, w, h: cap, stroke: style.stroke, fill: style.fill, fillStyle: 'solid' });
            return grouped([bottom, body, left, right, top]);
        },
    },
    {
        id: 'cloud',
        name: 'Cloud',
        keywords: ['cloud', 'internet', 'service', 'network'],
        w: 210,
        h: 130,
        build: (x, y, style) => {
            const w = 210;
            const h = 130;
            const cloud = closedCurve(scalePoints(CLOUD, x, y, w, h), style);
            const label = (0, document_1.createElement)('text', {
                x: x + w * 0.2,
                y: y + h * 0.44,
                w: w * 0.6,
                h: 24,
                text: 'Cloud',
                fontSize: style.fontSize,
                textAlign: 'center',
                textColor: style.textColor,
                stroke: 'transparent',
            });
            return grouped([cloud, label]);
        },
    },
    {
        id: 'person',
        name: 'Person',
        keywords: ['person', 'user', 'actor', 'stick figure', 'human'],
        w: 96,
        h: 150,
        build: (x, y, style) => {
            const w = 96;
            const h = 150;
            const headR = 21;
            const cx = x + w / 2;
            const neck = y + headR * 2;
            const hip = neck + 52;
            const stroke = { stroke: style.stroke, strokeWidth: 3 };
            const head = (0, document_1.createElement)('ellipse', { x: cx - headR, y, w: headR * 2, h: headR * 2, ...stroke, fill: style.fill, fillStyle: 'solid' });
            const spine = (0, document_1.createElement)('line', { x: cx, y: neck, points: [[0, 0], [0, hip - neck]], ...stroke });
            const arms = (0, document_1.createElement)('line', { x: cx - 30, y: neck + 18, points: [[0, 0], [60, 0]], ...stroke });
            const legL = (0, document_1.createElement)('line', { x: cx, y: hip, points: [[0, 0], [-24, 44]], ...stroke });
            const legR = (0, document_1.createElement)('line', { x: cx, y: hip, points: [[0, 0], [24, 44]], ...stroke });
            const label = (0, document_1.createElement)('text', {
                x: x - 22,
                y: y + h - 22,
                w: w + 44,
                h: 24,
                text: 'Person',
                fontSize: style.fontSize,
                textAlign: 'center',
                textColor: style.textColor,
                stroke: 'transparent',
            });
            return grouped([head, spine, arms, legL, legR, label]);
        },
    },
];
const libraryItem = (id) => exports.LIBRARY.find((i) => i.id === id);
exports.libraryItem = libraryItem;
/** Builds an item centred on a scene point. */
function insertLibraryItem(id, center, style = {}) {
    const item = (0, exports.libraryItem)(id);
    if (!item)
        return [];
    const s = { ...layout_1.DEFAULT_DIAGRAM_STYLE, ...style };
    return item.build(Math.round(center[0] - item.w / 2), Math.round(center[1] - item.h / 2), s);
}
/** Fuzzy-ish search for the slash palette: name first, then keywords. */
function searchLibrary(query) {
    const q = query.trim().toLowerCase();
    if (!q)
        return exports.LIBRARY;
    const score = (item) => {
        const name = item.name.toLowerCase();
        if (name.startsWith(q))
            return 0;
        if (name.includes(q))
            return 1;
        if (item.keywords.some((k) => k.startsWith(q)))
            return 2;
        if (item.keywords.some((k) => k.includes(q)))
            return 3;
        return Infinity;
    };
    return exports.LIBRARY.map((item) => ({ item, s: score(item) }))
        .filter((e) => e.s !== Infinity)
        .sort((a, b) => a.s - b.s)
        .map((e) => e.item);
}

  };
  modules["src/lib/text.ts"] = function (module, exports, require) {
"use strict";
// Text measuring and wrapping, with the measurer injected so the same
// wrapping runs on the canvas (real `ctx.measureText`) and in the SVG
// exporter / diagram layout (a table-driven estimate, no DOM needed).
Object.defineProperty(exports, "__esModule", { value: true });
exports.TEXT_PADDING = exports.estimateMeasure = exports.lineHeightFor = exports.LINE_HEIGHT_RATIO = void 0;
exports.estimateTextWidth = estimateTextWidth;
exports.wrapText = wrapText;
exports.measureBlock = measureBlock;
exports.LINE_HEIGHT_RATIO = 1.25;
const lineHeightFor = (fontSize) => Math.round(fontSize * exports.LINE_HEIGHT_RATIO);
exports.lineHeightFor = lineHeightFor;
/** Average advance width as a fraction of the font size, per family.
 *  Calibrated against the browser stacks in types.ts - good to a few per
 *  cent, which is all layout and SVG sizing need. */
const AVERAGE_ADVANCE = { sans: 0.52, serif: 0.5, mono: 0.6 };
const WIDE = new Set('MMWWmw@%'.split(''));
const NARROW = new Set("iljtfrI!.,;:'|()[]{}".split(''));
/** Estimated width of `text` with no DOM available. */
function estimateTextWidth(text, fontSize, font = 'sans') {
    const base = AVERAGE_ADVANCE[font] ?? 0.52;
    if (font === 'mono')
        return text.length * fontSize * base;
    let units = 0;
    for (const ch of text) {
        if (WIDE.has(ch))
            units += 0.95;
        else if (NARROW.has(ch))
            units += 0.42;
        else if (ch >= 'A' && ch <= 'Z')
            units += 0.78;
        else if (ch === ' ')
            units += 0.55;
        else
            units += 1;
    }
    return units * fontSize * base;
}
const estimateMeasure = (fontSize, font = 'sans') => (text) => estimateTextWidth(text, fontSize, font);
exports.estimateMeasure = estimateMeasure;
/** Greedy word wrap honouring explicit newlines; a single word longer
 *  than the line is broken by characters rather than overflowing. */
function wrapText(text, maxWidth, measure) {
    const out = [];
    const limit = Math.max(maxWidth, 1);
    for (const paragraph of text.split('\n')) {
        if (paragraph === '') {
            out.push('');
            continue;
        }
        const words = paragraph.split(' ');
        let line = '';
        for (const word of words) {
            const candidate = line === '' ? word : `${line} ${word}`;
            if (measure(candidate) <= limit || line === '') {
                if (measure(candidate) > limit && line === '' && measure(word) > limit) {
                    // Break the oversized word itself.
                    let chunk = '';
                    for (const ch of word) {
                        if (chunk && measure(chunk + ch) > limit) {
                            out.push(chunk);
                            chunk = ch;
                        }
                        else {
                            chunk += ch;
                        }
                    }
                    line = chunk;
                    continue;
                }
                line = candidate;
            }
            else {
                out.push(line);
                line = word;
            }
        }
        out.push(line);
    }
    return out.length ? out : [''];
}
/** Width and height of a wrapped block. */
function measureBlock(lines, fontSize, measure) {
    let width = 0;
    for (const line of lines)
        width = Math.max(width, measure(line));
    return { width, height: lines.length * (0, exports.lineHeightFor)(fontSize) };
}
/** Padding between a container's edge and the text inside it. */
exports.TEXT_PADDING = 10;

  };
  modules["src/lib/shapes.ts"] = function (module, exports, require) {
"use strict";
// The drawing primitives the canvas renderer and the SVG exporter share:
// path commands for a line/arrow/freehand stroke, arrowhead geometry and
// dash patterns. Keeping them here is what makes an exported SVG match
// what is on screen - both consumers walk the SAME command list, one into
// ctx.bezierCurveTo, the other into a `d` attribute.
//
// Pure: no DOM, no canvas, no React.
Object.defineProperty(exports, "__esModule", { value: true });
exports.cornerRadius = exports.ARROW_HEAD_SIZE = exports.commandsToPathData = void 0;
exports.linearCommands = linearCommands;
exports.dashPattern = dashPattern;
exports.arrowHeadGeometry = arrowHeadGeometry;
exports.headsFor = headsFor;
const geometry_1 = require("./geometry");
/** Path for a line, arrow or freehand stroke: straight segments, an
 *  orthogonal elbow route, or a Catmull-Rom curve, exactly as the element
 *  asks for. Freehand is always smoothed. */
function linearCommands(el) {
    const pts = (0, geometry_1.linearPathPoints)(el);
    if (pts.length === 0)
        return [];
    if (pts.length === 1)
        return [['M', pts[0][0], pts[0][1]]];
    const smooth = el.type === 'draw' || (el.edge === 'curved' && pts.length >= 2);
    if (!smooth)
        return [['M', pts[0][0], pts[0][1]], ...pts.slice(1).map((p) => ['L', p[0], p[1]])];
    const out = [['M', pts[0][0], pts[0][1]]];
    for (const [c1, c2, end] of (0, geometry_1.curveSegments)(pts))
        out.push(['C', c1[0], c1[1], c2[0], c2[1], end[0], end[1]]);
    return out;
}
const fmt = (n) => {
    const r = Math.round(n * 100) / 100;
    return Object.is(r, -0) ? '0' : String(r);
};
/** A `d` attribute from the same commands the canvas draws. */
const commandsToPathData = (commands) => commands.map((c) => (c[0] === 'Z' ? 'Z' : c[0] + c.slice(1).map((n) => fmt(n)).join(' '))).join(' ');
exports.commandsToPathData = commandsToPathData;
/** Dash pattern in scene units for a stroke style, scaled with the pen so
 *  a bold dashed line still reads as dashed. */
function dashPattern(style, strokeWidth) {
    if (style === 'dashed')
        return [strokeWidth * 3.2, strokeWidth * 2.4];
    if (style === 'dotted')
        return [0.01, strokeWidth * 2.1];
    return [];
}
exports.ARROW_HEAD_SIZE = 5.2;
/** Geometry for one end of a linear element. `tip` is the endpoint,
 *  `from` the neighbouring vertex the line arrives from. */
function arrowHeadGeometry(tip, from, kind, strokeWidth) {
    if (kind === 'none')
        return null;
    const dx = tip[0] - from[0];
    const dy = tip[1] - from[1];
    const len = Math.hypot(dx, dy);
    const ux = len === 0 ? 1 : dx / len;
    const uy = len === 0 ? 0 : dy / len;
    const size = exports.ARROW_HEAD_SIZE * Math.max(1, strokeWidth * 0.75) + strokeWidth * 1.6;
    if (kind === 'dot') {
        const radius = Math.max(2.4, strokeWidth * 1.5);
        return { kind, points: [], center: tip, radius, inset: radius * 0.6 };
    }
    if (kind === 'bar') {
        const half = Math.max(4, size * 0.6);
        return {
            kind,
            points: [
                [tip[0] - uy * half, tip[1] + ux * half],
                [tip[0] + uy * half, tip[1] - ux * half],
            ],
            center: tip,
            radius: 0,
            inset: 0,
        };
    }
    const spread = 0.42;
    const cos = Math.cos(spread);
    const sin = Math.sin(spread);
    const back = [tip[0] - ux * size, tip[1] - uy * size];
    const bx = back[0] - tip[0];
    const by = back[1] - tip[1];
    return {
        kind,
        points: [
            [tip[0] + bx * cos - by * sin, tip[1] + bx * sin + by * cos],
            [tip[0], tip[1]],
            [tip[0] + bx * cos + by * sin, tip[1] - bx * sin + by * cos],
        ],
        center: tip,
        radius: 0,
        inset: size * 0.55,
    };
}
/** Both ends of a linear element, already aimed along the drawn path. */
function headsFor(el) {
    const pts = (0, geometry_1.linearPathPoints)(el);
    if (pts.length < 2)
        return { start: null, end: null };
    return {
        start: arrowHeadGeometry(pts[0], pts[1], el.startArrow, el.strokeWidth),
        end: arrowHeadGeometry(pts[pts.length - 1], pts[pts.length - 2], el.endArrow, el.strokeWidth),
    };
}
/** Corner radius in scene units for a rectangle-ish element. */
const cornerRadius = (el) => Math.max(0, Math.min(el.roundness * Math.min(Math.abs(el.w), Math.abs(el.h)), Math.min(Math.abs(el.w), Math.abs(el.h)) / 2));
exports.cornerRadius = cornerRadius;

  };
  return runModule("src/index.tsx");
})();
export default __appExports.default;
export const tools = __appExports.tools;
export const intents = __appExports.intents;
export const onInstall = __appExports.onInstall;
export const onUninstall = __appExports.onUninstall;
